---
lab:
   title: 'ラボ 03: アプリケーションの管理'
   description: 'このラボでは、Microsoft Store アプリのデプロイ、Win32 アプリケーションのパッケージ化とデプロイ、Microsoft 365 Apps の構成、Enterprise App Catalog の利用、アプリの置き換え (supersedence) の構成、アプリ保護ポリシーの適用を行います。'
   duration: 100 minutes
   level: 200
   islab: true
   primarytopics:
      - Microsoft Intune
      - Windows
      - Microsoft Store
      - Enterprise App Catalog
---

# ラボ 03: アプリケーションの管理

## ラボのシナリオ

あなたは Contoso Healthcare のモダン エンドポイント管理者 **Jordan Chen** です。デバイスの登録と管理 (ラボ 01～02) が完了したので、次はユーザーとデバイスへアプリケーションをデプロイする必要があります。ここでは、Microsoft Store アプリ (モダン アプリ)、Win32 パッケージ (レガシ アプリケーション)、Microsoft 365 Apps (生産性スイート)、Enterprise App Catalog (キュレーションされたサード パーティ製アプリ) という複数のデプロイ方法を使用します。また、モバイル デバイスや未登録デバイス上の企業データを保護するために、アプリ保護ポリシーも構成します。

このラボを完了すると、次のことができるようになります。
- Microsoft Store アプリをデプロイする
- カスタム検出規則を使用して Win32 アプリケーションをパッケージ化してデプロイする (ラボ 01 の Pharmacy スコープ タグを付与)
- 更新チャネルを構成して Microsoft 365 Apps をデプロイする
- Enterprise App Catalog からアプリを追加して割り当てる
- アプリの置き換え (supersedence) を構成してアプリケーションを自動的にアップグレードする (引き続きパイロット グループを対象とし、Pharmacy タグを付与)
- モバイル デバイス向けのアプリ保護ポリシーを作成する
- アプリのデプロイ状態を監視する

## ラボの所要時間

**推定所要時間:** 90 分

## 手順

### 始める前に

このラボには次が必要です。
- **ラボ 01** の完了 (デバイスの登録、グループの構成)
- Contoso の Microsoft 365 テナント (`<TenantPrefix>.onmicrosoft.com`) へのアクセス
- グローバル管理者または Intune 管理者の資格情報
- **SEA-DEV1** (登録済みデバイス、Megan Bowen でサインイン済み)
- **SEA-DEV2** (登録済みデバイス、Joni Sherman でサインイン済み)
- **演習 2** 用に、**7-zip.org** から公式の 7-Zip インストーラーを直接ダウンロードするための、**SEA-DEV1** 上のインターネット アクセス。事前にパッケージ化されたアプリ アセットはなく、実際のインストーラーを自分でダウンロードします。
- **Microsoft Intune Suite の試用版が有効** (**ラボ 01** の前提条件で有効化済み)。演習 4 (Enterprise App Catalog) に必要です。

## 演習 1: Microsoft Store アプリのデプロイ

### シナリオ

Microsoft Store アプリは、Microsoft Store を通じて配布されるモダンな Windows アプリケーションです。Intune を使用すると、ユーザーが直接 Store にアクセスしなくても、Store アプリを管理対象デバイスへデプロイできます。

### タスク 1: Microsoft Store アプリの追加

1. SEA-DEV1 で **Microsoft Edge** を開き、**Microsoft Intune 管理センター** (**https://intune.microsoft.com**) を開きます。

1. **admin@`<TenantPrefix>`.onmicrosoft.com** としてサインインします。

1. サインインの維持を確認する画面が表示された場合は、**いいえ** を選択します。

1. **Microsoft Intune 管理センター** で **アプリ** を選択し、続いて **すべてのアプリ** を選択します。

1. 上部のツールバーで **作成** を選択します。

1. **アプリの種類の選択** ペインで、**プラットフォーム** を **Windows** に設定し、続いて **アプリの種類** を **Microsoft Store アプリ (新規)** に設定します。**選択** を選択します。

   > [!NOTE]
   > このポータルのフローは 2 段階の選択方式です。最初に **プラットフォーム** (Windows / iOS/iPadOS / macOS / Android) を選ぶと、**アプリの種類** の一覧がそのプラットフォーム向けに絞り込まれます。「新規」の Microsoft Store アプリの種類は Microsoft Store for Business のバックエンドを使用し、従来のコネクタよりも信頼性に優れています。

1. **アプリ情報** タブで、**Microsoft Store アプリ (新規) を検索する** を選択します。

1. **Microsoft Store アプリ (新規) を検索** ペインで、`Microsoft To Do` を検索します。

1. 検索結果から **Microsoft To Do: Lists, Tasks & Reminders** を選択します。

1. **選択** を選択します。

1. **アプリ情報** ページで、アプリの詳細を確認します。
   
   - **名前:** Microsoft To Do (違っていたら修正してください)
   - **発行元:** Microsoft Corporation (違っていたら修正してください)
   - **説明:** (Store から自動入力)
   
1. **次へ** を選択し、続いてもう一度 **次へ** を選択して **スコープ タグ** タブをスキップします。

1. **割り当て** ページの **必須** で、**グループの追加** を選択します。

1. **dyn-Windows-Devices** を検索して選択します。

1. **選択** を選択します。

   > [!NOTE]
   > 「必須」として割り当てると、グループ内のすべてのデバイスにアプリが自動的にインストールされます。「使用可能」にした場合は、ユーザーが自分でインストールできるように **Company Portal** にアプリが表示されます。

1. **次へ** を選択します。

1. **レビューと作成** タブで **作成** を選択します。

**Microsoft Store アプリの追加と割り当てが正常に完了しました。**

---

### タスク 2: SEA-DEV1 でのアプリ インストールの確認

1. **SEA-DEV1** に切り替えます、アプリが自動的にインストールされるまで 5～10 分待ちます。

   > [!NOTE]
   > Intune は既定で 8 時間ごと、またはデバイスの同期時に新しいアプリの割り当てを確認します。同期を強制的に実行することで、インストールを早めることができます。

1. デバイスの同期を強制するために、スタートメニューから **Settings** を開きます (Windows + I キーを押します)。

1. **Accounts** → **Access work or school** の順に移動します。

1. **Connected to Contoso** のエントリ (表示名が異なる場合は **Connected to `<TenantName>`**) を選択します。

1. **Info** を選択し、下へスクロールして **Sync** を選択します。

1. 同期が完了するまで待ちます (通常 1～2 分)。

1. 同期後、**Start** メニューを開き、`Microsoft To Do` を検索します。

1. アプリが検索結果に表示され、起動できることを確認します。

**Microsoft Store アプリのインストールが正常に確認できました。**

---

## 演習 2: Win32 アプリケーションのパッケージ化とデプロイ

### シナリオ

Win32 アプリは、従来型の Windows デスクトップ アプリケーション (`.exe`、`.msi` インストーラー) です。ここでは、Microsoft Win32 Content Prep Tool を使用して Win32 アプリをパッケージ化し、検出規則を作成して、管理対象デバイスへデプロイします。

### タスク 1: Win32 アプリ パッケージの準備

> [!IMPORTANT]
> **実際のインストーラーを自分でダウンロードします。事前にパッケージ化されたアプリ アセットは使用しません。** コンパイル済みの `.exe`/`.msi` を研修コンテンツに埋め込むことはサプライ チェーン上のリスクです。その出所を検証できず、セキュリティ レビューで (当然ながら) 指摘されるような成果物だからです。このタスクでは、サード パーティ製サイトの再パッケージ化された「ポータブル」ビルドではなく、ベンダーから直接、**公式** の 7-Zip インストーラーをダウンロードします。

1. **SEA-DEV1** で、Win32 Content Prep Tool が `C:\Program Files\IntuneWinAppUtil\IntuneWinAppUtil.exe` に存在することを確認します。

   > [!NOTE]
   > ツールが存在しない場合は、**SEA-DEV1** のEdgeブラウザーで、https://github.com/microsoft/Microsoft-Win32-Content-Prep-Tool/releases にアクセスして(「Source code(zip) 」のリンクから)ダウンロードし、指定されたパスへ展開します。

1. **Microsoft Edge** を開き、公式の 7-Zip ダウンロード ページである **https://www.7-zip.org/download.html** (実際の作者である Igor Pavlov が公開) を開きます。

1. 現在の **64-bit Windows x64 .msi** パッケージをダウンロードします (`.exe` インストーラーや、サード パーティ ミラーの「ポータブル」版ではありません)。

   ダウンロード後、MSIを実行しないでください(次の移動コマンドが、ファイル使用中で失敗します)。

1. ソース フォルダーを作成し、ダウンロードした MSI をその中へ移動します。

   ```powershell
   New-Item -ItemType Directory -Path "C:\LabAssets\Win32-App\Source" -Force
   Move-Item "$env:USERPROFILE\Downloads\7z*.msi" "C:\LabAssets\Win32-App\Source\"
   ```

1. ダウンロードしたファイルの正確なファイル名 (7-Zip のリリースごとに変わります。例: `7z2408-x64.msi`) を控えておきます。次のコマンドで必要になります。

   > [!NOTE]
   > このラボでは、例となるペイロードとして `7z-portable.exe` を使用します。別のインストーラー (例: Notepad++ の `npp.8.9.7.Installer.x64.exe`) を使用する場合は、演習 2、5、7 を通して **ファイル名**、**アプリ名/発行元**、**インストール/アンインストール コマンド**、**検出パス** を一貫して置き換えてください。

1. **Terminal (Admin)** を開きます (Start を右クリック → Terminal (Admin))。Windows 11 では、PowerShell タブが開いた Windows Terminal が起動します。

1. **Do you want to allow this app to make changes to your device?** というプロンプトで **Yes** を選択します。

1. Win32 Content Prep Tool のディレクトリへ移動します。

   ```powershell
   cd "C:\Program Files\IntuneWinAppUtil"
   ```

1. コンテンツ準備ツールを実行してアプリをパッケージ化します (`<filename>` は前の手順で確認した実際の `.msi` ファイル名に置き換えます)。

   ```powershell
   .\IntuneWinAppUtil.exe -c "C:\LabAssets\Win32-App\Source" -s "<filename>.msi" -o "C:\LabAssets\Win32-App\Output"
   ```

   - `-c`: アプリ ファイルを格納するソース フォルダー
   - `-s`: セットアップ ファイル (MSI インストーラー)
   - `-o`: `.intunewin` パッケージの出力フォルダー

   > [!NOTE]
   > セットアップ ファイルが MSI であるため、コンテンツ準備ツールは MSI の製品コード、バージョン、その他のメタデータを自動的に読み取り、`.intunewin` パッケージへ埋め込みます。これにより、タスク 2 で手動のファイル パス チェックではなく、MSI ベースの自動検出が可能になります。

   確認があった場合は **Y** で受け入れてください。

1. パッケージ化が完了するまで待ちます (通常 10～30 秒)。

1. `.intunewin` ファイルが作成されたことを確認します (`<filename>` は同じファイル名に置き換えます)。

    ```powershell
    Test-Path "C:\LabAssets\Win32-App\Output\<filename>.intunewin"
    ```

    出力は **True** を返すはずです。

**Intune Win32 Content Prep Tool を使用して Win32 アプリを正常にパッケージ化しました。**

---

### タスク 2: Win32 アプリの Intune への追加

1. **SEA-DEV1** の **Microsoft Edge** で **Microsoft Intune 管理センター** を開き、**アプリ** → **すべてのアプリ** の順に移動します。

   > [!NOTE]
   > このタスクでは、SEA-DEV1 上に作成した `.intunewin` パッケージをアップロードするため、ブラウザー操作をラボ環境内の SEA-DEV1 で実施します。

1. 上部のツールバーで **作成** を選択します。

1. **アプリの種類の選択** ペインで、**プラットフォーム** を **Windows** に、**アプリの種類** を **Windows アプリ (Win32)** に設定します。**選択** を選択します。

1. **アプリ情報** タブで **アプリ パッケージ ファイルの選択** を選択します。

1. **アプリ パッケージ ファイル** ペインで、**ファイルの選択** の横にあるフォルダー アイコンを選択し、`C:\LabAssets\Win32-App\Output\` へ移動します。

1. タスク 1 で作成した `.intunewin` ファイルを選択し、**OK** を選択します。

   > [!NOTE]
   > ソースが MSI だったため、Intune はパッケージのメタデータから **名前**、**説明**、**発行元**、**バージョン** の各フィールドを直接読み取り、次のページにあらかじめ入力します。ゼロから入力するのではなく、正確かどうかを確認してください。

1. **アプリ情報** ページで、自動入力されたフィールドを確認します。また、以下の個所を記入します。

   - **説明:** `7-Zip file archiver for Windows`
   - **発行元:** `Igor Pavlovs`

1. **次へ** を選択します。

1. **プログラム** ページでは、**インストール コマンド** と **アンインストール コマンド** が MSI パッケージのメタデータから自動入力されています。正しいことを確認します。
   
   - **インストール コマンド:** `msiexec /i "<filename>.msi" /qn`
   - **アンインストール コマンド:** `msiexec /x "{<product-code-GUID>}" /qn`
   
   > [!NOTE]
   > `/qn` は MSI をサイレント インストール (ユーザーへのプロンプトなし) します。アンインストール コマンドは元のファイル名ではなく MSI の **製品コード** (GUID) を参照します。Windows インストーラーは、元のインストーラー ファイルがデバイス上になくても、製品コードだけで MSI ベースのアプリをアンインストールできます。
   
1. **インストールの動作** が **システム** に設定されていることを確認します (このパッケージでは既定でそうなっています)。

1. **デバイスの再起動の動作** を、あらかじめ選択されている既定値 (**アプリのインストールでデバイスの再起動が強制される場合があります**) から **リターン コードを基に動作を決定する** に変更します。

   > [!NOTE]
   > **インストーラーの種類** / **アンインストーラーの種類** (どちらも **コマンド ライン**)、**必要なインストール時間 (分)**、**使用可能なアンインストールを許可** の各設定と、**リターン コード** の表 (0 と 1707 = 成功、3010 = ソフト再起動、1641 = ハード再起動) は、あらかじめ入力された既定値のままにします。これらは Intune の組み込みの MSI 処理によるもので、構成する必要はありません。

1. **次へ** を選択します。

1. **要件** ページで、次を構成します。
   - **オペレーティング システムのアーキテクチャの確認:** **はい。アプリをインストールできるシステムを指定してください。**
   - アーキテクチャのチェック ボックスで、**x64 システムにインストールする** のみをオンにします (**x86** と **ARM64** はオフのままにします)。
   - **最小オペレーティング システム:** Windows 10 1607

   > [!NOTE]
   > **必要なディスク領域**、**必要な物理メモリ**、**必要な論理プロセッサの最小数**、**必要な最小 CPU 速度** は空欄のままにします。このアプリにはいずれも該当しません。**追加の要件規則の構成** も空のままにします。

1. **次へ** を選択します。

1. **検出規則** タブで、次を構成します。
   - **規則の形式:** 検出規則を手動で構成する

1. **検出規則** の下で **追加** を選択します。

1. **検出規則** ペインで、次を構成します。
   - **規則の種類:** MSI
   - **MSI 製品コード:** 自動入力のままにします (Intune が `.intunewin` パッケージに埋め込まれた MSI メタデータから読み取ります)。

   > [!NOTE]
   > これは、ポータブル/手動インストーラーではなく MSI をパッケージ化することの本質です。Intune はパッケージから製品コードを既に把握しているため、検出は「このファイルが存在するか」という脆弱な推測ではなく、実際の Windows インストーラー登録に対する信頼性の高いバージョン チェックになります。

1. **OK** を選択します。

1. **次へ** を選択します。

1. **依存関係** タブで **次へ** を選択します (依存関係は不要です)。

1. **置き換え (Supersedence)** タブで **次へ** を選択します (置き換えは後のタスクで構成します)。

1. **スコープ タグ** タブで **スコープ タグの選択** を選択し、**Pharmacy** (**ラボ 01 演習 2 タスク 6** で作成) を追加して **選択** を選択し、続いて **次へ** を選択します。

   > [!NOTE]
   > 7-Zip は、Contoso の臨床ドキュメント ワークフロー (研究データのエクスポート、匿名化された DICOM バンドル) における標準的なアーカイブ ツールです。デプロイに `Pharmacy` タグを付けておくことで、Pharmacy Helpdesk ロール (**ラボ 05 演習 3** で割り当て) から引き続き参照できるようになります。

1. **割り当て** タブの **必須** で、**グループの追加** を選択します。

1. **sg-Intune-Pilot-Users** を検索して選択します。

1. **選択** を選択します。

1. **次へ** を選択します。

1. **レビューと作成** タブで **作成** を選択します。

**カスタム検出規則を使用して Win32 アプリを正常に追加しました。**

---

### タスク 3: Win32 アプリのインストールの監視

1. **SEA-DEV1** で、デバイスの同期を強制します。
   - スタートメニューから **Settings** → **Accounts** → **Access work or school** → **Connected to Contoso** → **Info** → **Sync**

1. アプリがインストールされるまで 10～15 分待ちます。

   > [!NOTE]
   > Win32 アプリのインストールは、Intune がパッケージをダウンロードし、インストーラーを実行し、検出規則を検証する必要があるため、Store アプリよりも時間がかかることがあります。

1. **Microsoft Intune 管理センター** で、**アプリ** → **すべてのアプリ** → **7-Zip** の順に移動します。

1. 左側のナビゲーションから **デバイスのインストール状態** を選択します。

1. 各デバイスのインストール状態を確認します。
   - **インストール済み:** アプリが正常にインストールされ、検出規則に合格しました。
   - **進行中:** インストールが実行中です。
   - **失敗:** インストールが失敗しました (エラー コードとメッセージを確認します)。
   - **該当なし:** デバイスが割り当ての対象ではありません。

1. インストールが失敗した場合は、失敗したデバイスを選択して詳細なエラー情報を表示します。

**Win32 アプリのインストール状態を正常に監視しました。**

---

## 演習 3: Microsoft 365 Apps のデプロイ

### シナリオ

Microsoft 365 Apps (旧称 Office 365 ProPlus) は、Word、Excel、PowerPoint、Outlook などの生産性ツールを提供します。ここでは、特定の更新チャネルを構成して、このスイートを管理対象デバイスへデプロイします。

### タスク 1: Microsoft 365 Apps の追加

1. **Microsoft Intune 管理センター** で、**アプリ** → **すべてのアプリ** の順に移動します。

1. 上部のツールバーで **作成** を選択します。

1. **アプリの種類の選択** ペインで、**プラットフォーム** を **Windows** に、**アプリの種類** を **Microsoft 365 Apps for Windows 10 and later** に設定します。**選択** を選択します。

1. **アプリ スイート情報** タブで、次を構成します。
   - **スイート名:** `Microsoft 365 Apps (Current Channel)`
   - **スイートの説明:** `Microsoft 365 Apps with Current Channel updates`

1. **次へ** を選択します。

1. **アプリ スイートの構成** ページで、**構成設定の形式** を **構成デザイナー** のままにします。

1. **Office アプリの選択** で、ドロップダウンを開いて次をオンにします。
   - **Excel**
   - **Outlook**
   - **PowerPoint**
   - **Teams**
   - **Word**

   > [!NOTE]
   > この一覧には単独の「OneDrive」項目はありません。Excel、Outlook、OneNote、PowerPoint、Access、Publisher、Skype for Business、Teams、Word のみが選択肢です。**その他の Office アプリの選択 (ライセンスが必要)** は **0 個選択** のままにします。このドロップダウンは、Project や Visio のように独自のライセンスが必要なアプリ向けで、このデプロイには含まれません。

1. **アプリ スイート情報** で、次を構成します。
   - **アーキテクチャ:** **64-bit** (トグル、既定で選択済み)
   - **既定のファイル形式:** **Office Open XML 形式** — このフィールドは必須です。選択するまでページに検証エラーが表示されます。
   - **更新チャネル:** **Current Channel (Preview)** — これも必須です。
   - **他のバージョンを削除:** はい (既定)
   - **インストールするバージョン:** 最新 (既定。**特定のバージョン** は無効のままにします)

1. **プロパティ** で、次を構成します。
   - **共有コンピューターのライセンス認証を使用:** いいえ (既定)
   - **ユーザーの代理で Microsoft ソフトウェア ライセンス条項に同意する:** はい
   - **Microsoft Search in Bing のバックグラウンド サービスをインストールする:** いいえ

1. 下へスクロールし、**言語** で **English (United States)** を選択します。

   > [!NOTE]
   > Current Channel は、新機能がリリースされ次第すぐに提供されます。Monthly Enterprise Channel は、テスト用のリード タイムを長く取った月次更新を提供します。

1. **次へ** を選択します。

1. **スコープ タグ** ページで **次へ** を選択します (このデプロイにスコープ タグは不要です)。

1. **割り当て** ページの **必須** で、**グループの追加** を選択します。

1. **dyn-Windows-Devices** を検索して選択します。

1. **選択** を選択します。

1. **次へ** を選択します。

1. **確認と作成** タブで **作成** を選択します。

**Microsoft 365 Apps を正常に構成して割り当てました。**

---

### タスク 2: Microsoft 365 Apps のインストールの監視

1. **SEA-DEV1** で、デバイスの同期を強制します。

1. Microsoft 365 Apps がダウンロードされてインストールされるまで 15～30 分待ちます。

   > [!NOTE]
   > Microsoft 365 Apps はダウンロード サイズが大きく (約 3 GB)、ネットワーク速度とデバイス性能によってはインストールに 20～40 分かかることがあります。ラボでは、次の演習に進み、後でインストール状態を確認してもかまいません。

1. **Microsoft Intune 管理センター** で、**アプリ** → **すべてのアプリ** → **Microsoft 365 Apps (Current Channel)** の順に移動します。

1. 左側のナビゲーションから **デバイスのインストール状態** を選択します。

1. 各デバイスのインストールの進行状況を確認します。

1. インストールが完了したら、**SEA-DEV1** で **Start** メニューを開き、次のアプリが存在することを確認します。
   - **Excel**
   - **Word**
   - **PowerPoint**
   - **Outlook**

**Microsoft 365 Apps のインストールを正常にデプロイして監視しました。**

---

## 演習 4: （オプション）Enterprise App Catalog の利用

### シナリオ

Enterprise App Catalog (Microsoft Intune Suite の一部) は、事前構成されたインストーラー、検出規則、アイコンを備えた、キュレーション済みのサード パーティ製アプリケーション ライブラリを提供します。ここでは、カタログからアプリを追加してデバイスへデプロイします。

**Enterprise App Catalog** は、Microsoft Intune Suite の機能である **Microsoft Intune Enterprise Application Management** の一部です。スイートの試用版は **ラボ 01** の前提条件で有効化済みのため、この演習は完全にハンズオンで実施できます。

### タスク 1: Enterprise App Catalog の参照

1. **Microsoft Intune 管理センター** で、**アプリ** → **すべてのアプリ** の順に移動します。

1. 上部のツールバーで **作成** を選択します。

1. **アプリの種類の選択** ペインで、**プラットフォーム** を **Windows** に、**アプリの種類** を **エンタープライズ アプリ カタログ アプリ** に設定します。**選択** を選択します。

   > [!NOTE]
   > Enterprise App Catalog アプリは一般提供 (GA) になりました (以前表示されていた「(プレビュー)」の接尾辞はなくなりました)。これは Intune Suite の機能である **Enterprise App Management** の一部で、ラボ 01 の前提条件で開始したスイートの試用版により有効になっています。この選択肢が表示されない場合、スイートの試用版のプロビジョニングがまだ完了していない可能性があります。有効化から 5～10 分待って更新してください。試用版が **アクティブ** に切り替わってから、機能タイルが表示されるまでに数分かかることがあります。

1. **アプリの選択** ページで、**エンタープライズ アプリ カタログ を検索** を選択して、カタログで利用可能なアプリを参照します。
   カタログには、次のような一般的なエンタープライズ アプリが含まれています。

   - **Google Chrome**
   - **Mozilla Firefox**
   - **Zoom**
   - **Adobe Acrobat Reader**
   - **VLC Media Player**
   - **Notepad++**

1. 一覧から **Google Chrome** を検索または選択します。

1. **次へ** を選択します。

1. **構成** タブで、次の内容が行に表示されていることを確認します。

   - **パッケージ名:** `googlechromestandaloneenterprise64.msi`
   - **言語:** en-US
   - **アーキテクチャ:** x64
   - **バージョン:** (現在のリリース。例: 150.0.7871.129)

   > [!NOTE]
   > Enterprise App Catalog は、Google が公開しているものと同じ公式インストーラーをパッケージ化しています。このタブでは、デプロイするブランチ/アーキテクチャ/言語のビルドを選択できます。

1. Google Chrome の行を **選択** して、**次へ** を選択します。

1. 「更新プログラム」タブの **更新方法** で、**置き換えに更新** を選択します。

   > [!NOTE]
   > **自動的に更新する** は、カタログから直接アプリを最新の状態に保ちますが、カスタムのインストール/アンインストール スクリプトをリセットしてブロックします。**置き換えによる更新** では、カスタム設定を維持したまま、ガイド付きの置き換え関係を通じて新しいバージョンをプッシュできます。これは、**演習 5** で 7-Zip アプリを管理する方法と一貫しています。

1. 読み取り専用の **アプリ情報** (アプリ名、パッケージ名、バージョン、発行元、アーキテクチャ、アプリケーション サイズ、プライバシー URL、アプリ ストア URL) と **アプリ コマンド** (**インストール コマンド**。`"%SystemRoot%\System32\msiexec.exe" /i "googlechromestandaloneenterprise64.msi" /qn` としてあらかじめ作成済み) を確認します。いずれも編集は不要です。

1. **選択** を選択します。

**Enterprise App Catalog を正常に参照してアプリを選択しました。**

---

### タスク 2: アプリの構成と割り当て

1. **アプリ情報** ページで、あらかじめ入力された詳細を確認します。
   - **名前:** Google Chrome
   - **説明:** (自動入力)
   - **発行元:** Google
   
1. **次へ** を選択します。

1. **プログラム** ページで、自動入力されたフィールドを確認します。いずれも編集は不要です。
   - **インストーラーの種類:** コマンド ライン
   - **インストール コマンド:** `"%SystemRoot%\System32\msiexec.exe" /i "googlechromestandaloneenterprise64.msi"` (タスク 1 の **更新** タブで確認したインストール コマンドと一致)
   - **アンインストーラーの種類:** コマンド ライン
   - **アンインストール コマンド:** `"%SystemRoot%\System32\msiexec.exe" /X {<product-code-GUID>}`
   - **必要なインストール時間 (分):** 60
   - **使用可能なアンインストールを許可:** はい
   - **インストールの動作:** このアプリではグレーアウト/編集不可のため、そのままにします。
   - **デバイスの再起動の動作:** **リターン コードを基に動作を決定する** — **演習 2** で手動構成した Win32 アプリとは異なり、ここでは既定でこの値になっています。

   > [!NOTE]
   > このページの上部にあるバナー (「このアプリは自動で更新できます...」) は、Chrome がインストール後に自己更新することを Enterprise App Catalog が知らせているものです。これは、タスク 1 で **置き換えによる更新** を選択して考慮した、自己更新の動作と同じです。

1. **次へ** を選択します。

1. **必要条件** ページで、事前構成された要件を確認して **次へ** を選択します。

1. **検出規則** タブで、事前構成された検出規則を確認します。
   - **規則の種類:** レジストリ
   - **パス/コード:** Chrome のインストール パスを確認します

1. **次へ** を選択します。

1. **スコープ タグ** ページで **次へ** を選択します (このアプリにスコープ タグは不要です)。

1. **置き換え (Supersedence)** ページで **次へ** を選択します (置き換え関係は不要です。これはこのアプリの最初のバージョンです)。

1. **割り当て** ページの **登録済みデバイスで使用可能** で、**グループの追加** を選択します。

1. **sg-Intune-Pilot-Users** を検索して選択します。

1. **選択** を選択します。

   > [!NOTE]
   > 「使用可能」として割り当てると、アプリが **Company Portal** アプリに表示され、ユーザーがオンデマンドでインストールできるようになります。これは省略可能なソフトウェアに役立ちます。

1. **次へ** を選択します。

1. **確認と作成** タブで **アプリを追加する** を選択します。

**Enterprise App Catalog からアプリを正常に追加して割り当てました。**

---

### タスク 3: Company Portal でのアプリの利用可否の確認

1. **SEA-DEV1** で **Start** メニューを開き、`Company Portal` を検索します。

1. **Company Portal** アプリを起動します。

   > [!NOTE]
   > デバイスに **Company Portal** アプリがない場合は、**Microsoft Edge** を開いて **https://apps.microsoft.com/detail/9wzdncrfj3pz?hl=en-GB&gl=PT** を開き、Microsoft Store から Company Portal アプリをインストールします。

1. **MeganB@`<TenantPrefix>`.OnMicrosoft.com** としてサインインします (まだサインインしていない場合)。

1. **Apps** セクションへ移動します。

1. 利用可能なアプリの一覧に **Google Chrome** が表示されることを確認し、クリックします。

1. **Install** を選択してアプリをインストールします。

1. インストールが完了するまで待ちます。

1. **Start** メニューを開き、**Google Chrome** が存在することを確認します。

**Company Portal からアプリを正常にインストールしました。**

---

## 演習 5: （オプション）アプリの置き換え (Supersedence) の構成

### シナリオ

アプリの置き換え (supersedence) を使用すると、アプリケーションを自動的にアップグレードまたは置き換えることができます。ユーザーが古いバージョンをインストールしている場合、Intune はそれを自動的にアンインストールし、新しいバージョンをインストールします。

### タスク 1: Win32 アプリの 2 つ目のバージョンの作成

このタスクでは、同じパッケージから 2 つ目の Win32 アプリのエントリを作成することで、新しいバージョンをシミュレートします。

運用環境では、「v2.0」は MSI 製品コードが異なる、実際に新しいインストーラーになります。ラボでは、**演習 2 タスク 1** で作成したものと同じ `.intunewin` パッケージを、新しいアプリ名で再利用します。これは、2 つの実際の 7-Zip リリースを用意しなくても、置き換えの仕組み (Intune が 1 つのアプリ オブジェクトをアンインストールし、別のアプリ オブジェクトをインストールすること) を示すのに十分です。

1. **SEA-DEV1** の **Microsoft Edge** で **Microsoft Intune 管理センター** を開き、**アプリ** → **すべてのアプリ** → **作成** の順に移動します。

   > [!NOTE]
   > このタスクでは、SEA-DEV1 上の `.intunewin` パッケージを再度アップロードするため、ブラウザー操作をラボ環境内の SEA-DEV1 で実施します。

1. **プラットフォーム** を **Windows** に、**アプリの種類** を **Windows アプリ (Win32)** に設定します。**作成** を選択します。

1. **演習 2 タスク 2** とまったく同じようにウィザード (**プログラム**、**要件**、**検出規則**、**依存関係**、**割り当て**) を進めます。ただし、次の違いがあります。
   - **アプリ情報:** **アプリ パッケージ ファイルの選択** を選び、`C:\LabAssets\Win32-App\Output\` から同じ `<filename>.intunewin` パッケージをアップロードします。続いて **名前** を `7-Zip v2.0` に、**説明** を `Updated version of 7-Zip` に設定します (**発行元** は **Igor Pavlov** のまま、自動入力されます)。
   - **置き換え (Supersedence):** **以前のバージョンのアンインストール** で **はい** を選択し、**次へ** を選択します。これにより、**7-Zip v2.0** をインストールする前に元の **7-Zip** アプリが削除されます。
   - **スコープ タグ:** **Pharmacy** を追加します (元のアプリと同じ)。両方のバージョンで Pharmacy の委任を一貫させます。
   - **割り当て:** **必須** を **sg-Intune-Pilot-Users** に割り当てます (元のアプリと同じ)。

1. **レビューと作成** タブで **作成** を選択します。

**v1 から v2 へ自動的にアップグレードするアプリの置き換えを正常に構成しました。**

---

### タスク 2: アプリの置き換え動作の確認

1. **SEA-DEV1** で、デバイスの同期を強制します。

1. Intune が置き換え関係を検出してアプリをアップグレードするまで 10～15 分待ちます。

1. **Microsoft Intune 管理センター** で、**アプリ** → **すべてのアプリ** → **7-Zip v2.0** の順に移動します。

1. **デバイスのインストール状態** を選択し、SEA-DEV1 が **インストール済み** と表示されることを確認します。

1. **7-Zip** (元のアプリ) へ移動し、**デバイスのインストール状態** を選択します。

1. 状態が **置き換え済み** または **インストールされていません** と表示されることを確認します。

**アプリの置き換えにより、古いアプリが新しいバージョンへ自動的に置き換えられたことを正常に確認しました。**

---

## 演習 6: アプリ保護ポリシーの作成

### シナリオ

アプリ保護ポリシー (APP) は、デバイスの完全な登録を必要とせずに、モバイル デバイスや BYOD (私物デバイスの持ち込み) のシナリオで企業データを保護します。ここでは、コピー/貼り付けを禁止し、PIN を要求し、条件付きアクセスを適用する iOS/Android 向けのアプリ保護ポリシーを作成します。

### タスク 1: iOS アプリ保護ポリシーの作成

1. **Microsoft Intune 管理センター** で **アプリ** を選択し、続いて **保護** を選択します。

1. **作成** → **iOS/iPadOS** を選択して、新しいポリシーを作成します。

1. **基本情報** タブで、次を構成します。
   - **名前:** `APP - iOS Data Protection`
   - **説明:** `Protects corporate data in Microsoft apps on iOS devices`

1. **次へ** を選択します。

1. **アプリ** タブで **パブリック アプリの選択** を選択します。

1. アプリの選択画面で、次を検索して選択します。
   - **Microsoft Outlook**
   - **Microsoft Teams**
   - **Microsoft Word**
   - **Microsoft Excel**
   - **Microsoft PowerPoint**
   - **Microsoft OneDrive**
   - **Microsoft 365 Copilot**

1. **選択** を選択します。

1. **次へ** を選択します。

1. **データ保護** タブで、次を構成します。
   - **データ転送:**
     - **他のアプリに組織データを送信:** ポリシーマネージドアプリ
     - **組織データのコピーを保存:** ブロック
     - **選択したサービスにユーザーがコピーを保存することを許可:** OneDrive for Business、SharePoint
     - **他のアプリからデータを受信:** ポリシーマネージドアプリ
     
     - **他のアプリとの間で切り取り、コピー、貼り付けを制限する:** 貼り付けを使用する、ポリシーマネージドアプリ
   - **暗号化:**
     - **組織データを暗号化:** 必要
   - **機能:**
     
     - **ポリシー マネージド アプリ データとネイティブ アプリまたはアドインの同期:** ブロック
     - **組織データの出力:** ブロック
     - **その他のアプリでの Web コンテンツの転送を制限する:** Microsoft Edge
   
1. **次へ** を選択します。

1. **アクセス要件** タブで、次を構成します。
   - **アクセスのための PIN:** 必要
   - **PIN の種類:** 数値
   - **最小 PIN 長を選択:** 6
   - **タイムアウト後は PIN で生体認証をオーバーライドする:** 必要
   - **アクセスに職場または学校アカウントの資格情報:** 必要
   - **アクセス要件を再確認する間隔 (非アクティブな分数):** 30

1. **次へ** を選択します。

1. **条件付き起動** ページで、既定の条件を確認します。
   - **アプリの条件:**
     - **PIN の最大試行回数:** 5 (アクション: PIN のリセット)
     - **オフラインの猶予期間:** 1440 分 (アクション: アクセスのブロック)
     - **オフラインの猶予期間:** 90 日 (アクション: データをワイプ)
   - **デバイスの条件:**
     - **脱獄またはルート化されたデバイス:** (アクション: アクセス禁止)

   > [!NOTE]
   > これら 4 行はあらかじめ設定された既定値で、追加する必要はありません。各表 (アプリの条件/デバイスの条件) の下部にある **1 つ選択** ドロップダウンは、さらに条件 (例: 最小 OS バージョン) を追加したい場合に使用しますが、このラボでは省略可能で必須ではありません。

1. **次へ** を選択し、**スコープ タグ** をスキップします。

1. **割り当て** ページの **包含されたグループ** で、**グループの追加** を選択します。

1. **sg-Intune-Pilot-Users** を検索して選択します。

1. **選択** を選択します。

   > [!NOTE]
   > このページには、組み込みの「すべてのユーザー」や「すべてのデバイス」の仮想割り当てはありません。**包含されたグループ** と **除外されたグループ** のどちらも **グループの追加** のみを提供します。このラボでは、ポリシーを全体的に割り当てるのではなく、ラボ 03 全体で使用してきたものと同じパイロット グループを対象とします。運用環境では通常、アプリ保護ポリシーの対象専用のグループを作成します。

1. **次へ** を選択します。

1. **確認及び作成** タブで **作成** を選択します。

**iOS アプリ保護ポリシーを正常に作成しました。**

---

### タスク 2: Android アプリ保護ポリシーの作成

1. **アプリ | 保護** ページで、**作成** → **Android** を選択して、新しいポリシーを作成します。

1. **基本情報** ページで、次を構成します。
   - **名前:** `APP - Android Data Protection`
   - **説明:** `Protects corporate data in Microsoft apps on Android devices`

1. **次へ** を選択します。

1. **アプリ** タブで **パブリック アプリの選択** を選択します。

1. iOS ポリシーと同じ Microsoft アプリ (Outlook、Teams、Word、Excel、PowerPoint、OneDrive) を検索して選択します。

1. **選択** を選択し、続いて **次へ** を選択します。

1. **データ保護** ページで、次を構成します。
   - **Android バックアップ サービスに組織データをバックアップ:** 許可 (既定)
   - **他のアプリに組織データを送信:** 既定の **すべてのアプリ** から **ポリシーマネージドアプリ** に変更します
   - **組織データのコピーを保存:** ブロック
   - **電話通信データの転送先:** 任意の電話アプリ (既定)
   - **メッセージング データの転送先:** 任意のメッセージング アプリ (既定)
   - **他のアプリとの間で切り取り、コピー、貼り付けを制限する:** 貼り付けを使用する、ポリシーマネージドアプリ
   
   - **組織データを暗号化:** 必要 (既定)
   - **登録済みデバイスで組織データを暗号化:** 必須 (既定)
   - **ポリシー マネージド アプリ データとネイティブ アプリまたはアドインと同期:** 既定の **許可** から **ブロック** に変更します
   - **組織データを出力する:** 既定の **許可** から **ブロック** に変更します
   - **その他のアプリでの Web コンテンツ転送を制限:** 既定の **任意のアプリ** から **Microsoft Edge** に変更します
   
   > [!NOTE]
   > **組織データの通知** (**許可**) と **アプリの起動時に Microsoft Tunnel 接続を開始する** (**いいえ**) は既定のままにします。このラボでは Microsoft Tunnel を使用しません。
   
1. **次へ** を選択します。

1. **アクセス要件** ページで、次を構成します。
   - **アクセスに PINを使用:** 必要 (既定)
   - **PIN の種類:** 数値 (既定)
   - **単純な PIN:** 許可 (既定)
   - **最小 PIN 長を選択:** 既定の **4** から **6** に変更します
   - **アクセスに PIN の代わりに生体認証を使用:** 許可 (既定)
   - **タイムアウト後は PIN で生体認証をオーバーライドする**: 必須 (既定)
   - **タイムアウト (非アクティブ分数):** 30 (既定)
   - **クラス 3 生体認証 (Android 9.0 以降):** 不要 (既定)
   - **PIN をリセットするまでの日数:** いいえ (既定)
   - **維持対象の以前の PIN 値の数を選択する:** 0 (既定)
   - **デバイス PIN が設定されている場合のアプリ PIN:** 必須 (既定)

   > [!NOTE]
   > iOS ポリシーとは異なり、Android の **アクセス要件** ページには **アクセスのための職場または学校のアカウントの資格情報** や **アクセス要件を再確認する間隔 (非アクティブな分数)** の設定はありません。ここでは探さないでください。

1. **次へ** を選択します。

1. **条件付き起動** タブで、既定の条件を確認して **次へ** を選択します。

1. **スコープ タグ** タブで **次へ** を選択します。

1. **割り当て** ページの **包含されたグループ** で、**グループの追加** を選択し、**sg-Intune-Pilot-Users** を選択します。

1. **次へ** → **作成** を選択します。

**Android アプリ保護ポリシーを正常に作成しました。**

---

### タスク 3: アプリ保護ポリシーの適用の理解

アプリ保護ポリシーは、デバイス レベルではなくアプリケーション レベルで適用されます。動作は次のとおりです。

1. **ユーザーが管理対象アプリをインストールします** (例: Outlook)。App Store または Google Play から入手します。

1. **ユーザーが職場アカウントでサインインします** (`user@<TenantPrefix>.onmicrosoft.com`)。

1. **Intune が管理対象 ID を認識し**、アプリ保護ポリシーを適用します。

1. **ユーザーに PIN の設定を求めます** (6 桁以上)。

1. **アプリ保護の制御が適用されます。**
   - ユーザーは Outlook から個人用アプリ (例: Gmail) へデータをコピーできません
   - ユーザーは OneDrive または SharePoint 以外に添付ファイルを保存できません
   - アプリ データは保存時に暗号化されます
   - デバイスがジェイルブレイク/ルート化されている場合、アプリは消去されます

1. **条件付きアクセスの統合** (省略可能): 条件付きアクセス ポリシーと組み合わせた場合、準拠していないユーザーはサインインをブロックされます。

> [!NOTE]
> アプリ保護ポリシーはデバイスの登録を必要としません。IT にデバイスの完全な制御を与えることなく、BYOD デバイス上の企業データを保護します。

**これで、アプリ保護ポリシーがモバイル デバイス上でどのようにデータ保護を適用するかを理解できました。**

---

## 演習 7: （参考）アプリのデプロイの監視と失敗のトラブルシューティング

**※　今回は該当データがないため、基本的に以下のレポートは結果が表示されません。手順の参考としてください。**

### シナリオ

Intune 管理センターを使用して、すべてのデバイスにわたるアプリのデプロイを監視し、失敗したインストールを特定し、一般的な問題をトラブルシューティングします。

### タスク 1: アプリ概要ダッシュボードの確認

1. **Microsoft Intune 管理センター** で、**アプリ** → **監視** の順に移動します。

1. **アプリの保護状態** ダッシュボードを確認します。
   - **iOS:** 保護されたアプリを持つユーザー数
   - **Android:** 保護されたアプリを持つユーザー数
   - **Windows:** (アプリ保護ポリシーは Windows には適用されません)

1. **アプリのインストール状態** タイルで、**失敗のあるアプリ** の数を確認します。

1. **アプリのインストール状態** を選択してレポートを開きます。レポートには失敗のあるアプリが一覧表示され、次の列が含まれます。
   - **アプリ名**
   - **発行元**
   - **プラットフォーム**
   - **バージョン**
   - **インストール失敗率 (%)**
   - **デバイスの失敗数**
   - **ユーザーの失敗数**

1. **アプリ名** を選択してアプリを開き、続いて **監視** で **デバイスのインストール状態** を選択します。

1. 各デバイスの **状態** 列を確認します (失敗の理由は **状態の詳細** を参照)。
   - **インストール済み:** アプリが正常にインストールされ、検出規則に合格しました。
   - **失敗:** インストールが失敗しました。**状態の詳細** でエラーを確認します。
   - **保留中:** インストールが進行中か、次のデバイス同期を待っています。
   - **インストールされていません:** アプリはデバイスにインストールされていません。

**アプリ概要ダッシュボードを正常に確認しました。**

---

### タスク 2: 失敗したアプリ インストールの調査

1. **Microsoft Intune 管理センター** で、**アプリ** → **すべてのアプリ** の順に移動します。

1. インストールの失敗が表示されているアプリ (例: **7-Zip**) を選択します。

1. 左側のナビゲーションから **デバイスのインストール状態** を選択します。

1. 状態が **失敗** のデバイスを見つけて選択します。

1. エラーの詳細を確認します。
   - **エラー コード:** 数値コード (例: `0x80070005` = アクセスが拒否されました)
   - **エラー メッセージ:** 失敗の説明
   - **最終更新日時:** 最後のインストール試行のタイムスタンプ

1. 一般的な失敗の原因と解決策は次のとおりです。
   - **`0x80070005` (アクセスが拒否されました):** アプリ インストーラーに昇格が必要です。インストールの動作を「ユーザー」ではなく「システム」に設定します。
   - **検出規則が満たされない:** アプリは正常にインストールされましたが、検出規則が失敗しました。検出規則のロジックを確認します。
   - **ダウンロードの失敗:** デバイスにインターネット接続がないか、Intune エンドポイントに到達できません。
   - **ディスク領域の不足:** デバイスにインストールのための十分な空き領域がありません。

**失敗したアプリ インストールを正常に調査しました。**

---

### タスク 3: アプリのインストール状態の CSV へのエクスポート

1. **Microsoft Intune 管理センター** で、**アプリ** → **監視** → **アプリのインストール状態** の順に移動します。

1. 一覧からアプリ (例: **Microsoft 365 Apps (Current Channel)**) を選択します。

1. **デバイスのインストール状態** ページで、上部のツールバーから **エクスポート** を選択し、続いて **はい** を選択して確定します。

1. エクスポートが完了するまで待ちます (通常 1～2 分)。

1. エクスポートされた **.zip** ファイル (例: `DeviceInstallStatusByApp_<id>.zip`) を開きます。この中に CSV レポートが含まれています。

1. **Excel** で CSV を開き、次の列を確認します。
   - `Device name`
   - `UserPrincipalName`
   - `Platform`
   - `AppInstallState_Ioc (status)` (Installed、Failed、In Progress)
   - `AppInstallStateDetails (error message)`
   - `Last ModifiedDate`

**レポート用にアプリのインストール データを正常にエクスポートしました。**

---

## ラボのまとめ

おめでとうございます。ラボ 03: アプリケーションの管理を完了しました。

このラボでは、次のことを行いました。

**演習 1: Microsoft Store アプリのデプロイ**
- Microsoft Store アプリ (Microsoft To Do) を Intune に追加した
- デバイスに自動的にインストールされるように、アプリを必須として割り当てた
- 管理対象デバイスでインストールを確認した

**演習 2: Win32 アプリケーションのパッケージ化とデプロイ**
- Intune Win32 Content Prep Tool を使用して Win32 アプリをパッケージ化した
- カスタムのファイル ベースの検出規則を作成した
- 委任管理のために、デプロイに Pharmacy スコープ タグを付与した
- パイロット ユーザーにアプリをデプロイし、インストール状態を監視した

**演習 3: Microsoft 365 Apps のデプロイ**
- Current Channel の更新で Microsoft 365 Apps を構成した
- スイートをすべての Windows デバイスに割り当てた
- 大規模なアプリのデプロイ プロセスを監視した

**演習 4: Enterprise App Catalog の利用**
- Enterprise App Catalog (Intune Suite の機能) を参照した
- 事前構成された設定と検出規則を使用して Google Chrome を追加した
- Company Portal で使用可能としてアプリをデプロイした

**演習 5: アプリの置き換え (Supersedence) の構成**
- アプリの新しいバージョンを作成した (Pharmacy タグを付与)
- 古いバージョンを自動的に置き換える置き換え関係を構成した
- デバイスで自動アップグレードの動作を確認した

**演習 6: アプリ保護ポリシーの作成**
- iOS と Android のアプリ保護ポリシーを作成した
- データ保護の制御 (コピー/貼り付けの制限、暗号化、PIN 要件) を構成した
- デバイスの登録なしで APP がデータ保護を適用する仕組みを理解した

**演習 7: アプリのデプロイの監視と失敗のトラブルシューティング**
- アプリ概要ダッシュボードを確認した

- 失敗したアプリ インストールを調査し、エラー コードを解釈した

- レポート用にアプリのインストール状態データを CSV へエクスポートした

  

**重要なポイント:**
- Microsoft Store アプリは、モダンで軽量なアプリケーション デプロイを提供します
- Win32 アプリは、Content Prep Tool によるパッケージ化とカスタム検出規則を必要とします
- Microsoft 365 Apps のデプロイには、段階的な展開のための更新チャネル構成が含まれます
- Enterprise App Catalog (Intune Suite) は、事前構成されたインストーラーによってサード パーティ製アプリのデプロイを簡素化します
- アプリの置き換えは、手動のアンインストール/再インストールなしでアプリケーションのアップグレードを自動化します
- アプリ保護ポリシーは、完全な登録なしでモバイル/BYOD デバイス上の企業データを保護します
- Intune は、アプリのデプロイに対する包括的な監視とトラブルシューティングを提供します

**次のステップ:** ラボ 04 では、Microsoft Defender for Endpoint の統合、エンドポイント セキュリティ ポリシー、BitLocker 暗号化、Microsoft Tunnel Gateway、Microsoft Cloud PKI を使用してデバイスを保護します。

**ラボ終了**
