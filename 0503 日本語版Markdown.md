---
lab:
  title: '演習ラボ 0503: デバイス コンプライアンスの構成と検証'
  description: このラボでは、管理対象デバイスの状態を判定するコンプライアンス ポリシーと関連する条件付きアクセス ルールを構成し、デバイス コンプライアンスを検証します。
  duration: 60 minutes
  level: 200
  islab: true
---

# 演習ラボ 0503: デバイス コンプライアンスの構成と検証

## 概要

このラボでは、管理対象デバイスの状態を判定するコンプライアンス ポリシーと関連する条件付きアクセス ルールを構成し、デバイス コンプライアンスを検証します。

### 前提条件

このラボを開始する前に、次のラボを完了しておく必要があります。

- 0101-Managing Identities in Entra ID
- 0102-Synchronizing identities by using Entra Connect
- 0203-Manage Device Enrollment into Intune
- 0204-Enrolling devices into Intune
- 0301-Creating and Deploying Configuration Profiles

  注: Entra ID への Windows Hello サインイン認証を保護するために使用するテキスト メッセージを受信できる携帯電話も必要です。

## 演習 1: コンプライアンス ポリシーの構成

### シナリオ

Contoso では、Intune に登録された Windows デバイスが最小構成要件を満たしていることを確認する必要があります。要件は次のとおりです。

- Windows オペレーティング システムの最小バージョン: 10.0.19041.329
- Microsoft Defender Antimalware が必須

デバイスがこれらの要件を満たす場合は準拠としてマークし、満たさない場合は非準拠としてマークします。

### タスク 1: コンプライアンス ポリシーを作成して割り当てる

1. 手元の PC で Microsoft Edge を起動し、アドレス バーに **https://intune.microsoft.com** と入力して **Enter** キーを押します。
2. 既定のテナント パスワードを使用して、**`admin@yourtenant.onmicrosoft.com`** としてサインインします。
3. ナビゲーション ペインで **デバイス** を選択し、**コンプライアンス** を選択します。
4. **デバイス | コンプライアンス** ブレードの詳細ペインで、**ポリシーの作成** を選択します。
5. **ポリシーの作成** ブレードで次の値を指定し、**作成** を選択します。
   - プラットフォーム: **Windows 10 以降**
   - プロファイルの種類: **Windows 10/11 compliance policy**
6. **基本** タブで次の値を指定し、**次へ** を選択します。
   - 名前: **Compliance1**
7. **コンプライアンス設定** タブで **デバイスの正常性** を展開し、使用できる設定を確認します。
8. **コンプライアンス設定** タブで **デバイスのプロパティ** を展開し、**最小 OS バージョン** フィールドに **10.0.19041.329** と入力します。
9. **コンプライアンス設定** タブで **システム セキュリティ** を展開し、**Microsoft Defender Antimalware** を **Require** に設定します。
10. **次へ** を選択します。**非準拠に対するアクション** タブで、**デバイスを非準拠としてマークする**の既定値が **immediately** であることを確認します。

    > デバイスを非準拠としてマークするまでの日数と、追加のアクションを構成する方法を確認します。

11. **次へ** を選択します。**割り当て** タブの **包含されたグループ** で **グループの追加** を選択します。**Windows Devices**、**選択**、**次へ** の順に選択します。

    > 注: **Windows Devices** グループは、ラボ 0301: Creating and Deploying Configuration Profiles で作成しました。

12. **確認と作成** タブで設定を確認し、**作成** を選択します。
13. ナビゲーション メニューで **デバイス** を選択し、デバイスのナビゲーション ペインで **コンプライアンス** を選択します。
14. **デバイス | コンプライアンス** ブレードで、**コンプライアンス設定** タブを選択します。
15. **コンプライアンス設定** ページで、**コンプライアンス状態の有効期間**に **30** が表示されていることを確認します。表示されていない場合は、その値を入力します。
16. **コンプライアンス設定** ページで、**コンプライアンス ポリシーが割り当てられていないデバイスをマークする**を **Not compliant** に切り替え、**保存** を選択します。

    > この設定により、コンプライアンス ポリシーが割り当てられていないデバイスは非準拠に設定されます。

**結果**: この演習を完了すると、コンプライアンス ポリシーが正常に構成されます。

## 演習 2: コンプライアンスを強制する条件付きアクセス ポリシーの作成

### シナリオ

非準拠としてマークされたデバイスを使用するユーザーは、電子メールへアクセスできないようにする必要があります。この規則を強制する条件付きアクセス ポリシーを構成し、想定どおりに機能することを確認します。場合によっては、サインインを繰り返し求められることがあります。

### タスク 1: 条件付きアクセス ポリシーを作成する

1. 手元の PC の **Microsoft Intune 管理センター**で **デバイス** を選択し、**条件付きアクセス** を選択します。
2. **条件付きアクセス | 概要** ブレードで、**ポリシー** を選択します。
3. **条件付きアクセス | ポリシー** ブレードで、**新しいポリシー** を選択します。
4. **新規** ブレードの **名前** テキスト ボックスに **Conditional1** と入力し、**0 users or agents (Preview) selected** を選択します。
5. **含める** で、**すべてのユーザー** ラジオ ボタンを選択します。
6. **ターゲット リソース** セクションで、**No target resources selected** を選択します。
7. **含める** で **リソースの選択** ラジオ ボタンを選択し、**特定のリソースを選択する**で **なし** を選択します。
8. **リソース** ページの検索ボックスに **Office 365** と入力し、**Office 365 Exchange Online** のチェック ボックスをオンにして、**選択** を選択します。
9. **条件** セクションで、**0 conditions selected** を選択します。
10. 条件の一覧の **デバイス プラットフォーム**で **構成されていません** を選択します。**構成** セクションで **はい** を選択し、**デバイス プラットフォームの選択** ラジオ ボタン、**Windows** チェック ボックス、**完了**の順に選択します。
11. **許可** セクションで **0 controls selected** を選択します。**デバイスは準拠しているとしてマーク済みである必要があります** チェック ボックスをオンにし、**選択** を選択します。
12. **新規** ブレードの **ポリシーの有効化**で **オン** を選択し、**作成** を選択します。

### タスク 2: 条件付きアクセス ポリシーが機能していることを確認する

1. **SEA-WS3** に切り替え、パスワード **Pa55w.rd** を使用して **Admin** としてサインインします。
2. **SEA-WS3** のタスク バーで **Microsoft Edge** を選択します。
3. Microsoft Edge に **outlook.office.com** と入力し、**Enter** キーを押します。
4. アカウントの選択ダイアログで、**Aaron@yourtenant.onmicrosoft.com** を選択します。
5. **Enter password** ページで **Pa55w.rd1234\!** と入力し、**Sign in** を選択します。

   > 注: Microsoft Edge のパスワード保存プロンプトが表示された場合は、**Update** を選択します。

6. Edge プロファイルの切り替えを求めるメッセージで、**Switch Edge profile** を選択します。
7. **Continue with your work or school account** というメッセージが表示されたら、**Sign in to sync data** を選択します。
8. パスワードの入力を再度求められたら、**Pa55w.rd1234\!** と入力し、**Sign in** を選択します。
9. **Automatically sign in to all desktops apps and websites on this device?** ダイアログで、**No, sign in to this app only** を選択します。

   > 注: **Let's set up your profile to access org resources** ページが表示されます。SEA-WS3 は Entra ID に参加しておらず、Intune で管理されていないため、このデバイスから Aaron のメールボックスへアクセスできません。

10. すべてのウィンドウを閉じ、**SEA-WS3** からサインアウトします。
11. **SEA-WS1** に切り替え、PIN **102938** を使用して Aaron Nicholls としてサインインします。

    > 注: SEA-WS1 は Intune に登録された管理対象の Windows 11 デバイスです。

12. タスク バーで **Microsoft Edge** を選択します。
13. Microsoft Edge に **outlook.office.com** と入力し、**Enter** キーを押します。
14. Aaron のメールボックスへアクセスできることを確認します。

    > 注: SEA-WS1 は管理対象デバイスであり、準拠としてマークされているためです。

15. Microsoft Edge を閉じ、SEA-WS1 からサインアウトします。

### タスク 3: 条件付きアクセス ポリシーを無効にする

1. 手元の PC の Microsoft Intune 管理センターに切り替えます。
2. ナビゲーション ペインで **デバイス**、**すべてのデバイス** の順に選択します。

   > SEA-WS1 は準拠しているため、Aaron はメールボックスへのアクセスを許可されました。

3. ナビゲーション ペインで **デバイス**、**条件付きアクセス** の順に選択します。
4. **条件付きアクセス** ページで、**ポリシー**、**Conditional1** の順に選択します。
5. **Conditional1** ページの下部で **オフ** を選択し、**保存** を選択します。

**結果**: この演習を完了すると、デバイス コンプライアンスを判定する条件付きアクセス ポリシーが正常に構成されます。

**ラボ終了**
