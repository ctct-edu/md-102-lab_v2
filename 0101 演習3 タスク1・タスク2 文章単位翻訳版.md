## 演習 3: グループの作成と管理、およびライセンス割り当ての検証

### シナリオ

3人の新しいユーザーをセキュリティ グループに追加し、次の表に従ってライセンスを割り当てます。

| 名前           | 所属先           | 割り当てるライセンス                                                       |
| -------------- | ---------------- | -------------------------------------------------------------------------- |
| Edmund Reeve   | Contoso_Managers | グループ メンバーシップによる Office 365 E5、Enterprise Mobility + Security E5 |
| Miranda Snider | Contoso_Managers | グループ メンバーシップによる Office 365 E5、Enterprise Mobility + Security E5 |
| Cody Godinez   | Contoso_Sales    | グループ メンバーシップと直接割り当てによる Office 365 E5、Enterprise Mobility + Security E5 |

さらに、サインイン ページの会社ブランドを変更します。

### タスク 1: Microsoft Entra 管理センターを使用してグループを作成する

1. 手元の PC の Microsoft Entra 管理センターで、ナビゲーション ペインから **グループ**を選択します。

2. **新しいグループ**を選択します。

3. **新しいグループ**ページで、次の値を入力または選択します。

    - グループの種類: **セキュリティ**
    - グループ名: **Contoso_Managers**
    - メンバーシップの種類: **割り当て済み**

4. **メンバー**で **メンバーが選択されていません**を選択します。

5. **メンバーの追加**ページで **Edmund Reeve** と **Miranda Snider** を追加し、**選択**を選択します。

6. **作成**を選択します。

### タスク 2: PowerShellを使用してグループを作成する

1. SEA-SVR1でPowerShell 7に切り替えます。

2. **PowerShell 7**ウィンドウで次のコードを入力して新しいグループを作成し、**Enter**キーを押します。

    ```powershell
    New-MgGroup -DisplayName "Contoso_Sales" -Description "Contoso Sales team users" -MailEnabled:$false -Mailnickname "Contoso_Sales" -SecurityEnabled
    ```

3. **PowerShell 7**ウィンドウで次のコマンドを入力し、**Enter**キーを押します。

    ```powershell
    Get-MgGroup
    ```

4. 作成したContoso_Salesグループを含む、テナント内のグループ一覧が表示されることを確認します。

5. **PowerShell 7**ウィンドウで次のコードを入力し、Contoso_Salesグループを変数として定義して、**Enter**キーを押します。

    ```powershell
    $group = Get-MgGroup | Where-Object {$_.DisplayName -eq "Contoso_Sales"}
    ```

6. **PowerShell 7**ウィンドウで次のコードを入力し、ユーザーを別の変数として定義して、**Enter**キーを押します。

    ```powershell
    $user = Get-MgUser | Where-Object {$_.DisplayName -eq "Cody Godinez"}
    ```

7. **PowerShell 7**ウィンドウで次のコードを入力し、定義した変数を使用してCodyをContoso_Salesへ追加して、**Enter**キーを押します。

    ```powershell
    New-MgGroupMember -GroupId $group.Id -DirectoryObjectId $user.Id
    ```

8. **PowerShell 7**ウィンドウで次のコードを入力し、**Enter**キーを押します。

    ```powershell
    Get-MgGroupMember -GroupId $group.Id | FL
    ```

9. **AdditionalProperties**の値として**Cody Godinez**が表示されることを確認します。

10. PowerShell 7を閉じます。
