---
lab:
  title: 'Lab 02: Manage and maintain devices'
  description: 'In this lab, you create device configuration profiles, compliance policies, and Windows Update rings, analyze Group Policy Objects for migration, and enable Endpoint analytics and proactive remediations.'
  duration: 100 minutes
  level: 200
  islab: true
  primarytopics:
    - Microsoft Intune
    - Windows
    - Windows Update for Business
---

# ラボ 02: デバイスの管理と保守

## ラボのシナリオ

あなたは Contoso Healthcare のモダン エンドポイント管理者である **Jordan Chen** です。デバイスは (ラボ 01 で) 既に Intune に登録されているため、デバイス構成プロファイル、コンプライアンス ポリシー、Windows Update の管理を実装する必要があります。また、Intune への移行に向けて既存のグループ ポリシー オブジェクトを分析し、プロアクティブな監視のためにエンドポイント分析を有効にし、デバイスの正常性を維持するために修復スクリプトを展開します。

このラボが終わるころには、次のことが完了しています。
- 設定カタログとテンプレートを使用して構成プロファイルを作成した
- `Pharmacy` スコープ タグ (**ラボ 01 演習 2 タスク 6** で作成) を、構成、コンプライアンス、更新プログラムの各ポリシーに適用した
- 包含モードと除外モードの両方を使用して複合的な割り当てフィルターを構築した
- 競合する構成プロファイルを意図的に作成し、**設定ごとの状態** で競合を解決した
- 非準拠に対する猶予期間アクションを含むコンプライアンス ポリシーを構成した
- 準拠デバイスを必須とする条件付きアクセス ポリシーを作成した (**レポート専用** モード。ラボ 04 で **オン** に切り替えます)
- グループ ポリシー分析を使用して、グループ ポリシー オブジェクトの移行準備状況を分析した
- 更新プログラム リング、機能更新プログラム プロファイル、迅速な品質更新プログラム ポリシーを構成した
- エンドポイント分析を有効にし、デバイスのパフォーマンスに関するインサイトを確認した
- プロアクティブな修復スクリプトを展開した
- トラブルシューティング ブレードを使用して、デバイスの状態の調査、ポリシー競合の診断、条件付きアクセスの影響の確認を行った

---

## ラボの所要時間

**推定所要時間:** 90 分

---

## 手順

### 開始する前に

このラボには次が必要です。
- **ラボ 01** の完了 (デバイスが登録され、グループが構成されている)
- Contoso の Microsoft 365 テナント (`<TenantPrefix>.onmicrosoft.com`) へのアクセス
- グローバル管理者または Intune 管理者の資格情報
- **SEA-DEV1** (登録済みデバイス、Megan Bowen がサインイン済み)
- **SEA-DEV2** (登録済みデバイス、Joni Sherman がサインイン済み)
- グループ ポリシーのバックアップ XML ファイル (ラボ アセットで提供)

> [!NOTE]
> **Intune の デバイス ワークロードが再編成されました。** 構成、コンプライアンス、スクリプト、グループ ポリシー分析の各画面は、[デバイス] の左側ナビゲーション内の **デバイスの管理** グループにまとまりました。**Windows 更新プログラム** は **プラットフォーム別 > Windows** の下にあります。**割り当てフィルター** は **テナント管理 > 割り当てフィルター** に移動しました。このラボでは全体を通して現在のナビゲーション パスを使用します。
>
> **演習 5 (修復) のテナント前提条件:** プロアクティブな修復には **Windows ライセンス認証** が必要で、これは **演習 5 タスク 2** の一環として有効にします。ラボ テナントが必要な Windows/Microsoft 365 ライセンスのいずれかを保有していない場合でも、ウィザードを一通り進めることはできますが、スクリプト パッケージはデバイス上で実行されません。

---

## 演習 1: 構成プロファイルの作成

### シナリオ

構成プロファイルを使用すると、デバイスの設定を大規模に管理できます。設定カタログ (設定ごとにきめ細かく制御) と組み込みのテンプレート (事前構成された設定のバンドル) を使用してプロファイルを作成します。

### タスク 1: 設定カタログ プロファイルの作成

設定カタログでは、Windows、macOS、iOS、Android にわたる数千もの個別の設定にアクセスできます。

1. SEA-DEV1 で **Microsoft Edge** を開き、**https://intune.microsoft.com** にアクセスします。

2. **admin@<TenantPrefix>.onmicrosoft.com** としてサインインします。

3. **Microsoft Intune 管理センター** で **デバイス** を選択し、**デバイスの管理** の下で **構成** を選択します。

   > [!NOTE]
   > ページ ヘッダーには **デバイス | 構成** と表示され、既定で **ポリシー** タブが開きます。その他のタブは **ADMX のインポート** と **監視** です。

4. **作成** → **新しいポリシー** を選択します。

5. **プロファイルの作成** ペインで、次のように構成します。
   - **プラットフォーム:** Windows 10 以降
   - **プロファイルの種類:** 設定カタログ

6. **作成** を選択します。

7. **基本情報** タブで、次を入力します。
   
   - **名前:** `Config - Power Settings`
   - **説明:** `Manages power plan and display timeout settings for corporate devices`
   
8. **次へ** を選択します。

9. **構成設定** タブで、**+ 設定の追加** を選択します。

10. 設定ピッカーで `power` を検索します。

11. **電源** を展開し、次の設定をオンにします。
    
    - **無人スリープ タイムアウト (電源接続時)**
    - **無人スリープ タイムアウト (バッテリ使用時)**
    
12. 設定ピッカーの右上隅にある **x** を選択して設定ピッカーを閉じ、構成設定に戻ります。

13. 設定を構成します。
    - **無人スリープ タイムアウト (電源接続時):** `0` (0 秒 = 無人時にスリープしない)
    - **無人スリープ タイムアウト (バッテリ使用時):** `1800` (秒 = 30 分)

    > [!NOTE]
    > これらの設定は分ではなく **秒** 単位で構成します。これは基盤となる CSP (`Policy CSP - Power`) がそのように定義されているためで、設定カタログは単位を変換しません。値 `0` は、無人時に Windows が自動的にスリープしないことを意味し (電源に接続されたキオスクや常時稼働のデバイスに便利)、0 以外の値は電源が接続されていないときにバッテリーを節約します。

14. **次へ** を選択します。

15. **スコープ タグ** タブで **+ スコープ タグの選択** を選択し、**Pharmacy** スコープ タグ (**ラボ 01 演習 2 タスク 6** で作成) を追加して、**選択** を選択します。続いて **次へ** を選択します。

    > [!NOTE]
    > ポリシー作成時にスコープ タグを適用することで、委任管理が実際に機能するようになります。ラボ 01 で作成した Pharmacy ヘルプデスク ロールは、このタグにより、このポリシーを (**ラボ 05 演習 3** で) 表示および操作できるようになります。
    >
    > **Default スコープ タグもオンのままにして、削除しないでください。** ロール *定義* 自体のスコープ タグ (ラボ 01 演習 2 タスク 6。そのロールは Pharmacy 専用であるため Default の削除が理にかなっていました) とは異なり、これはテナント内のすべての Windows デバイスに割り当てられる一般的なポリシーであり、Pharmacy 専用の成果物ではありません。Default と並べて Pharmacy を追加することで、Pharmacy ヘルプデスク管理者にもこのポリシーが見えるようになるだけで、ポリシーが実際に適用される対象は変わりません。この同じルールは、ラボ シリーズ全体で Pharmacy スコープ タグをポリシー、アプリ、プロファイルに追加する他のすべての箇所にも適用されます。

16. **割り当て** タブで、**包含されたグループ** の下にある **グループの追加** を選択します。

17. **dyn-Windows-Devices** を検索して選択します。

18. **選択** を選択します。

19. **次へ** を選択します。

20. **レビューと作成** タブで設定を確認し、**作成** を選択します。

**設定カタログ構成プロファイルが正常に作成されました。**

---

### タスク 2: テンプレートを使用したデバイス制限プロファイルの作成

テンプレートは、一般的なシナリオ向けに事前構成された設定のバンドルを提供します。

1. **Microsoft Intune 管理センター** の **構成** ページで、**作成** → **新しいポリシー** を選択します。

2. **プロファイルの作成** ペインで、次のように構成します。
   - **プラットフォーム:** Windows 10 以降
   - **プロファイルの種類:** テンプレート → **デバイスの制限**

3. **作成** を選択します。

4. **基本情報** タブで、次を入力します。
   - **名前:** `Config - Device Restrictions`
   - **説明:** `Restricts Windows features and user capabilities on corporate devices`

5. **次へ** を選択します。

6. **構成設定** タブで **全般** を展開し、次のように構成します。
   - **画面キャプチャ (モバイルのみ):** ブロック
   - **コピー/貼り付け (モバイルのみ):** 構成されていません
   - **手動での登録解除:** ブロック

7. **パスワード** を展開し、次のように構成します。
   - **パスワード:** 必要
   - **必要なパスワードの種類:** 英数字
   - **パスワードの複雑さ:** 数字、小文字、大文字、特殊文字が必要
   - **パスワードの最小文字数:** 8
   - **デバイスがワイプされるまでのサインイン失敗回数:** 10

8. **Microsoft Defender ウイルス対策** を展開し、次のように構成します。
   - **リアルタイム監視:** 有効にする
   - **動作の監視:** 有効にする

9. **次へ** を選択します。

10. **スコープ タグ** タブで **+ スコープ タグの選択** を選択し、**Pharmacy** を追加して **選択** を選択します。続いて **次へ** を選択します。

11. **割り当て** タブで、**包含されたグループ** の下にある **グループの追加** を選択します。

12. **sg-Intune-Pilot-Users** を検索して選択します。

13. **選択** を選択します。

14. **次へ** を選択します。

15. **適用性ルール** タブで **次へ** を選択します。

16. **レビューと作成** タブで **作成** を選択します。

**デバイス制限プロファイルが正常に作成されました。**

---

### タスク 3: 複合的な割り当てフィルターの作成 (包含モードと除外モード)

割り当てフィルターを使用すると、新しいグループを作成しなくても、デバイスのプロパティに基づいてポリシーの対象を絞り込めます。フィルターの中上級的な使い方とは、複数のプロパティを 1 つのフィルターにまとめ (**複合ルール**)、同じポリシーで **包含と除外** のモードを使い分けて対象を重ね合わせることです。ここでは、複合的な包含フィルターと、タスク 4 で再利用する単純な除外フィルターの 2 つを作成します。

#### フィルター 1 — 複合的な包含フィルター (企業所有の Microsoft デバイス、キオスクを除く)

1. **Microsoft Intune 管理センター** の左側ナビゲーションで **テナント管理** を選択し、続いて **割り当てフィルター** を選択します。

   > [!NOTE]
   > **割り当てフィルター** は以前は **デバイス** の下にありました。現在のポータルでは、**テナント管理** の下にあるテナント全体の設定です。

2. **作成** → **マネージド デバイス** を選択します。

3. **基本情報** タブで、次を入力します。
   - **名前:** `Filter - Microsoft corporate, no kiosks`
   - **説明:** `Compound rule: Microsoft-manufactured corporate devices that are not categorized as Kiosk`
   - **プラットフォーム:** Windows 10 以降

4. **次へ** を選択します。

5. **ルール** タブで **ルール構文エディター** に切り替え (**編集** を選択)、次を入力します。

   ```text
   (device.manufacturer -eq "Microsoft") -and (device.deviceCategory -ne "Kiosk")
   ```

   > [!NOTE]
   > 複合フィルター ルールは、動的グループ ルールと同じ `-and` / `-or` / かっこの構文を使用します。複合フィルターを作成する唯一の方法はルール構文エディターであり、単純なプロパティ/演算子/値のピッカーは 1 つの句しか指定できません。

6. **OK** を選択してルールを保存します。

7. **次へ** を選択し、**スコープ タグ** タブはスキップします。

8. **レビューと作成** タブで **作成** を選択します。

#### フィルター 2 — 単純な除外フィルター (SEA-DEV1)

1. **割り当てフィルター** ページで **作成** → **マネージド デバイス** を選択します。

2. **基本情報** タブで、次を入力します。
   - **名前:** `Filter - SEA-DEV1 Exclude`
   - **説明:** `Excludes device SEA-DEV1 from policy assignments`
   - **プラットフォーム:** Windows 10 以降

3. **次へ** を選択します。

4. **ルール** タブで、次のように構成します。
   - **プロパティ:** Device Name (デバイス名)
   - **演算子:** Equals
   - **値:** `SEA-DEV1`

   > [!NOTE]
   > ここでは **等しい** を使用して SEA-DEV1 を対象にしています。包含と除外の判断は、タスク 4 でこのフィルターをポリシーに **適用** するときに行います (**割り当てからフィルター対象のデバイスを除外する** を選択します)。

5. **次へ** を選択し、**スコープ タグ** タブはスキップします。

6. **レビューと作成** タブで **作成** を選択します。

> [!IMPORTANT]
> **包含と除外は、フィルター自体ではなく適用時に設定します。** フィルターは単にデバイスの集合を定義するだけです。フィルターをポリシーの割り当てにアタッチするときに、そのデバイスにポリシーを適用するか (**包含**)、スキップするか (**除外**) を選択します。同じフィルターを、異なるポリシーでどちらのモードでも使用できます。

**複合フィルターと単純な割り当てフィルターが正常に作成されました。**

---

### タスク 4: 割り当てフィルターをプロファイルに適用する

タスク 3 で作成したフィルターを使用して、SEA-DEV1 を除外するようにデバイス制限プロファイルを変更します。

1. **Microsoft Intune 管理センター** で **デバイス** → **デバイスの管理** → **構成** の順に移動します。

2. 一覧から **Config - Device Restrictions** を選択します。

3. **プロパティ** が表示されるまで下にスクロールします。

4. **割り当て** セクションで **編集** を選択します。

5. **sg-Intune-Pilot-Users** グループの割り当ての下で **フィルターの編集** を選択し、続いて **割り当てでフィルター処理されたデバイスを除外する** を選択します。

6. **Filter - SEA-DEV1 Exclude** を選び、**選択** を選択します。

7. **レビューと保存** → **保存** を選択します。

   > [!NOTE]
   > デバイス制限プロファイルは、SEA-DEV1 を **除く** `sg-Intune-Pilot-Users` 内のすべてのデバイスに適用されるようになります。これはタスク 3 の包含と除外のパターンです。フィルターが「SEA-DEV1」を定義し、適用時のモード (**除外**) がその意味を反転させます。同じ `Filter - SEA-DEV1 Exclude` を別のポリシーで **包含** モードで使用すれば、SEA-DEV1 *だけ* を対象にすることもできます。

**割り当てフィルターが構成プロファイルに正常に適用されました。**

---

### タスク 5: （オプション）パイロット コホート向けに意図的に競合する 2 つのプロファイルを作成する

実際の Intune 環境では、時間の経過とともにさまざまな管理者がポリシーを作成するため、重複するポリシーが蓄積されます。同じグループの同じ設定を対象とする 2 つのプロファイルは、**設定ごとの状態** ビューに現れる **競合** を引き起こします。ここでは意図的にその状況を作り出し、**演習 6 タスク 2** で診断して解決できるようにします。

パイロット コホート向けに、1 つの設定 **Allow camera** で相反する 2 つの設定カタログ プロファイルを作成します。(なぜカメラなのか? Pharmacy の臨床エリアでは、患者記録の近くにあるカメラに HIPAA に基づく制限がありますが、より広範なパイロット コホートには Teams 会議でカメラを必要とする管理アシスタントがいます。善意の 2 人の管理者が、同じグループに対して正反対のポリシーを作成する可能性があります。)

**プロファイル 1 — `WIN - Camera - Enabled (Pilot)`**

1. **Microsoft Intune 管理センター** の **構成** ページで、**作成** → **新しいポリシー** を選択します。

2. **プロファイルの作成** ペインで、次のように構成します。
   - **プラットフォーム:** Windows 10 以降
   - **プロファイルの種類:** 設定カタログ

3. **作成** を選択します。

4. **基本情報** タブで、次を入力します。
   - **名前:** `WIN - Camera - Enabled (Pilot)`
   - **説明:** `Allow camera use on pilot devices (Teams meetings)`

5. **次へ** を選択します。

6. **構成設定** タブで **+ 設定の追加** を選択します。

7. 設定ピッカーで `camera` を検索します。**カメラ** (ポータルのビルドによっては **Devices > Camera**) を展開し、**カメラを許可する** 設定をオンにします。右上の **x** をクリックしてピッカーを閉じます。

8. **カメラを許可する** を **許可されています** に設定します。

9. **次へ** を選択します。

10. **スコープ タグ** タブで **Pharmacy** を追加し、**次へ** を選択します。

11. **割り当て** タブで、**包含されたグループ** の下にある **グループの追加** を選択し、**sg-Intune-Pilot-Users** を検索して選択し、**選択** を選択します。**次へ** を選択します。

12. **レビューと作成** ページで **作成** を選択します。

**プロファイル 2 — `WIN - Camera - Disabled (Pilot)`** (競合するもう一方)

1. **構成** ページで **作成** → **新しいポリシー** を選択します。

2. **プラットフォーム:** Windows 10 以降。**プロファイルの種類:** 設定カタログ。**作成** を選択します。

3. **基本情報** ページで、次を入力します。
   - **名前:** `WIN - Camera - Disabled (Pilot)`
   - **説明:** `Block camera on pilot devices (Pharmacy clinical areas — HIPAA)`

4. **次へ** を選択します。

5. **構成設定** タブで **+ 設定の追加** を選択し、設定ピッカーで `camera` を検索します。**カメラ** (ポータルのビルドによっては **Devices > Camera**) を展開し、**カメラを許可する** 設定をオンにします。右上の **x** をクリックしてピッカーを閉じます。

6. **カメラを許可する** を **許可されていません** に設定します。

7. **次へ** を選択します。

8. **スコープ タグ** タブで **+ スコープ タグの選択** を選択し、**Pharmacy** を追加して **選択** を選択します。続いて **次へ** を選択します。

9. **割り当て** タブで **sg-Intune-Pilot-Users** (同じグループ) に割り当てます。**次へ** を選択します。

10. **レビューと作成** タブで **作成** を選択します。

> [!IMPORTANT]
> 同じグループ (`sg-Intune-Pilot-Users`) の **Allow Camera** 設定で **競合** する 2 つのプロファイルを意図的に作成しました。Intune はこれをひそかにマージしたり優先するものを選んだり **しません**。代わりに **設定ごとの状態** ビューに競合を表示し、デバイス上の該当設定は **競合** と表示され、どちらの値も適用されません。この競合は **演習 6 タスク 2** で診断して解決します。今は修正しないでください。

**パイロット コホート向けに、意図的に競合する 2 つの構成プロファイルが正常に作成されました。**

---

## 演習 2: コンプライアンス ポリシーの構成

### シナリオ

コンプライアンス ポリシーは、デバイスのセキュリティと正常性の要件を定義します。非準拠のデバイスは Microsoft Entra ID でそのようにマークでき、条件付きアクセス ポリシーがトリガーされて企業リソースへのアクセスがブロックされます。

### タスク 1: コンプライアンス通知メッセージ テンプレートの作成

**エンド ユーザーにメールを送信する** という非準拠アクションには、送信するメッセージ テンプレートが必要です。組み込みの **Default** テンプレートはないため、タスク 2 で選択できるようにするには、事前に 1 つ作成しておく必要があります。

1. **Microsoft Intune 管理センター** で **デバイス** を選択し、**デバイスの管理** の下で **コンプライアンス** を選択します。

2. **通知** タブを選択します。

3. **+ 通知の作成** を選択します。

4. **基本情報** タブで、次を入力します。
   - **名前:** `Default`

5. **次へ** を選択します。

6. **ヘッダーとフッターの設定** タブで、**次へ** を選択します。
   
8. **通知メッセージ テンプレート** タブで **追加** を選択し、次のように構成します。
   - **ロケール:** 英語 (米国)
   - **件名:** `Your device needs attention`
   - **メッセージ:** `Your device is out of compliance with Contoso's security policy. Please contact the Service Desk or take the recommended action in the Company Portal app to restore access.`
   - **既定のロケールに設定する** をオンにします

9. **保存** を選択し、**次へ** を選択します。

10. **次へ** を選択し、**スコープ タグ** はスキップします。

11. **レビューと作成** タブで **作成** を選択します。

**コンプライアンス通知メッセージ テンプレートが正常に作成されました。**

---

### タスク 2: Windows コンプライアンス ポリシーの作成

1. **Microsoft Intune 管理センター** で **デバイス** を選択し、**デバイスの管理** の下で **コンプライアンス** を選択します。

   > [!NOTE]
   > ページ ヘッダーには **デバイス | コンプライアンス** と表示され、既定で **ポリシー** タブが開きます。その他のタブは **通知**、**非準拠デバイスの廃止**、**コンプライアンス設定**、**スクリプト**、**監視** です。

2. **ポリシー** タブを選択し、続いて **ポリシーの作成** を選択します。

3. **ポリシーの作成** ペインで、次のように構成します。
   - **プラットフォーム:** Windows 10 以降
   - **プロファイルの種類:** Windows 10/11 コンプライアンス ポリシー

4. **作成** を選択します。

5. **基本情報** タブで、次を入力します。
   - **名前:** `Compliance - Windows Security Baseline`
   - **説明:** `Requires BitLocker encryption, antivirus, firewall, and secure boot`

6. **次へ** を選択します。

7. **コンプライアンス設定** タブで **デバイスの正常性** を展開し、次のように構成します。
   - **BitLocker:** 必要
   - **セキュア ブート:** 必要
   - **コード整合性:** 必要

8. **デバイスのプロパティ** を展開し、次のように構成します。
   - **最小 OS バージョン:** `10.0.19045` (Windows 11 22H2 または Windows 10 21H2)

9. **システム セキュリティ** を展開し、次のように構成します。
   - **モバイル デバイスのロックを解除する際にパスワードを要求する:** 必要
   - **単純なパスワード:** ブロック
   - **パスワードの種類:** 英数字
   - **パスワードの複雑さ:** 数字、小文字、大文字、特殊文字が必要にする
   - **パスワードの最小長:** 8
   - **デバイス上のデータ ストレージの暗号化が必要です。:** 必要
   - **ファイアウォール:** 必要
   - **ウイルス対策:** 必要
   - **スパイウェア対策:** 必要
   - **Microsoft Defender マルウェア対策:** 必要
   - **Microsoft Defender マルウェア対策の最小バージョン:** 空白のままにします (任意のバージョン)
   - **最新の Microsoft Defender マルウェア対策セキュリティ インテリジェンス:** 必要
   - **リアルタイム保護:** 必要

10. **次へ** を選択します。

11. **非コンプライアンススケジュール (コンプライアンス違反となってからの日非対応に対するアクション** タブで、既定のアクションを構成します。
    
    - **デバイスに非準拠のマークを付ける:** 7 日
    
13. 新しいアクションを構成します。
    - **アクション:** メールをエンド ユーザーに送信する
    - **スケジュール (コンプライアンス違反となってからの日:** 1
    - **メッセージ テンプレート:** **Default** を選択します (またはカスタム テンプレートを作成します)
    - **追加の受信者:** 設定不要

    > [!NOTE]
    > これにより、デバイスが非準拠になってから 1 日後に、デバイスのプライマリ ユーザーにメールが送信され、問題を修正する時間が与えられます。
    >
    > 使用できる Default メッセージ テンプレートがない場合は、先に **コンプライアンス** ページの **通知** タブに移動し、**+ 通知の作成** を選択してこのアクションを構成する前に作成します。

14. **次へ** を選択し、**スコープ タグ** タブで **Pharmacy** を追加して **次へ** を選択します。

15. **割り当て** ページで、**包含されたグループ** の下にある **グループの追加** を選択します。

16. **dyn-Windows-Devices** を検索して選択します。

17. **選択** を選択し、続いて **次へ** を選択します。

18. **レビューと作成** ページで **作成** を選択します。

**猶予期間と通知アクションを含む Windows コンプライアンス ポリシーが正常に作成されました。**

---

### タスク 3: デバイス コンプライアンスを必須とする条件付きアクセス ポリシーの作成 (レポート専用)

コンプライアンス ポリシーだけでは何もブロックされません。デバイスが準拠または非準拠としてマークされるだけです。実際の効力は、企業リソースへのアクセスに **準拠としてマーク済み** の状態を必須とする **条件付きアクセス (CA)** ポリシーから生まれます。ここでその CA ポリシーを作成しますが、**オン** に切り替える前にこのラボと **ラボ 04 演習 6** でその影響を観察できるよう、**レポート専用** モードで開始します。

1. 新しいブラウザー タブを開き、**https://entra.microsoft.com** (Microsoft Entra 管理センター) にアクセスします。求められた場合は **admin@<TenantPrefix>.onmicrosoft.com** としてサインインします。

2. 左側ナビゲーションの **Entra ID** の下で **条件付きアクセス** を選択します。

3. 左側ナビゲーションで **ポリシー** を選択し、続いて **+ 新しいポリシー** を選択します。

4. **新規** ポリシー ページで、次のように構成します。
   
   - **名前:** `CA - Require compliant device (Pharmacy pilot)`
   
5. **割り当て** → **ユーザーまたはエージェント** で、**0 個のユーザーとグループが選択されました** を選択します。
   
   - **対象** タブで **ユーザーとグループの選択** を選択し、**ユーザーとグループ** をオンにして、**sg-Intune-Pilot-Users** を選択し、**選択** を選択します。
   - **対象外** タブで **ユーザーとグループ** を選択し、**admin@<TenantPrefix>.onmicrosoft.com** (またはサインインに使用したアカウント) を選択して、**選択** を選択します。
   
   > [!WARNING]
   > **サインインをブロックする可能性のある条件付きアクセス ポリシーからは、少なくとも 1 つのグローバル管理者 (緊急アクセス用アカウント) を必ず除外してください。** レポート専用モードでは適用されませんが、このポリシーは **ラボ 04 演習 6** で **オン** に切り替わります。除外はその切り替え *より前* に設定しておく必要があります。そうしないと、テナントから締め出されるおそれがあります。
   
6. **割り当て** → **ターゲット リソース** で、**ターゲット リソースが選択されていません** を選択します。
   - **このポリシーの適用対象を選択します:** リソース (以前の「すべてのクラウド アプリ」)
   - **対象:** すべてのリソース (以前の「すべてのクラウド アプリ」)
   - すべてのアプリを含めることに関する警告を確認します。

7. **割り当て** → **条件** で、**0 個の条件が選択されました** → **クライアント アプリ** で、 **構成されていません** → **構成: はい** を選択し、**ブラウザー** と **モバイル アプリとデスクトップ クライアント** の両方をオンにして、**完了** を選択します。

8. **アクセス制御** → **許可** で、**0 個のコントロールが選択されました** を選択します。
   
   - **アクセス権の付与** を選択します。
   - **デバイスは準拠しているとしてマーク済みである必要があります** をオンにします。
   - **複数のコントロールの場合** は **選択したコントロールすべてが必要** のままにします。
   - **選択** を選択します。
   
9. **ポリシーの有効化** で **レポート専用** を選択します。オレンジの警告画面「準拠デバイスが必要なレポート専用モードのポリシーでは～」は確認だけしてください。

   > [!NOTE]
   > **レポート専用** は、サインインごとにポリシーを評価して結果 (成功 / 失敗 / 適用されない / ユーザー操作が必要) をログに記録しますが、適用は **しません**。これは、サインインをブロックする可能性のある CA ポリシーに対する、安全なロールアウトの定番パターンです。

10. **作成** を選択します。

> [!NOTE]
> この CA ポリシーは **レポート専用** モードで作成されました。このラボの **演習 6 タスク 4** でサインイン ログへの影響を確認し、レポートを確認した後に **ラボ 04 演習 6** で **オン** に切り替えます。

**デバイス コンプライアンスを必須とする条件付きアクセス ポリシー (レポート専用モード) が正常に作成されました。**

---

## 演習 3: グループ ポリシー オブジェクトの分析

### シナリオ

Contoso には、オンプレミスの Active Directory 環境からの既存のグループ ポリシー オブジェクト (GPO) があります。グループ ポリシー分析を使用して、どの GPO 設定が Intune でサポートされているかを特定し、移行レポートを生成します。

### タスク 1: グループ ポリシーのバックアップのインポート

1. **SEA-DEV1** に切り替えます。

2. コースの GitHub ラボ リポジトリから GPO バックアップ ファイルを `C:\LabAssets\GPO-Backups\` にダウンロードします。

   管理者特権の **Terminal (Admin)** ウィンドウを開き (Start を右クリックし、Terminal (Admin) を選択します。Windows Terminal は既定で PowerShell タブを開きます)、次を実行します。

   ```powershell
   $dest = "C:\LabAssets\GPO-Backups"
   New-Item -Path $dest -ItemType Directory -Force | Out-Null
   $url = "https://raw.githubusercontent.com/MicrosoftLearning/MD-102T00-Microsoft-365-Endpoint-Administrator/master/Instructions/Labs/Assets/GPO_Desktop_Settings.xml"
   Invoke-WebRequest -Uri $url -OutFile "$dest\GPO_Desktop_Settings.xml"
   ```

   別の方法として、ブラウザーでダウンロードすることもできます。GitHub で [Instructions/Labs/Assets/GPO_Desktop_Settings.xml](https://github.com/MicrosoftLearning/MD-102T00-Microsoft-365-Endpoint-Administrator/blob/master/Instructions/Labs/Assets/GPO_Desktop_Settings.xml) を開き、**Download raw file** を選択して、`C:\LabAssets\GPO-Backups\` に保存します。

   > [!NOTE]
   > ラボ環境で GPO バックアップ ファイルが `C:\LabAssets\GPO-Backups\` に既に用意されている場合は、ダウンロードをスキップして提供されたコピーを使用できます。

3. (手元PCではなく) **SEA-DEV1 の Microsoft Edge** で、**Microsoft Intune 管理センター** にアクセスします。

4.  **デバイス** を選択し、**デバイスの管理** の下で **グループ ポリシー分析** を選択します。

   > [!NOTE]
   > グループ ポリシー分析には **(プレビュー)** のフラグが付かなくなりました。現在のポータルでは一般提供されている機能です。

5. 上部のツール バーから **インポート** を選択します。

6. **GPO ファイルのアップロード** タブで **ファイルの選択** を選択し、参照して `C:\LabAssets\GPO-Backups\` に移動します。

7. **GPO_Desktop_Settings.xml** を選択し、**Open** を選択します。

8. **次へ** を選択します。

9. **スコープ タグ** ページで **Default** スコープ タグをそのままにします (ここでは Pharmacy を追加しないでください。この GPO 分析は、後で ラボ 05 で使用する Pharmacy ヘルプデスク委任のスレッドには含まれません)。**次へ** を選択します。

   > [!NOTE]
   > ここでスコープ タグを選択しない場合、Default が自動的に適用されます。選択したタグにスコープ設定された管理者だけが、この分析一覧でインポートした GPO を表示できます。Default のままにすると、Default スコープを持つすべての管理者 (実質的に、より狭いカスタム ロールを持たないすべての人) がそれを表示できます。

10. **レビューと作成** タブで **作成** を選択します。

11. インポートが完了するまで待ちます (通常 1～2 分)。

12. インポート後、GPO が一覧に表示されます。**MDM サポート** 列には、サポートされている設定の数が表示され、ページ上部にある集計された **グループ ポリシーの移行準備状況** のバーには、**移行準備完了**、**サポート対象外**、**非推奨** の数がまとめられます。

**グループ ポリシーのバックアップがグループ ポリシー分析に正常にインポートされました。**

---

### タスク 2: 移行準備状況レポートの確認とサポートされている設定の移行

1. **グループ ポリシー分析** ページで、一覧から **GPO_Desktop_Settings** を選択します。

2. **設定** タブが開き、設定ごとに 1 行のテーブルが表示されます。次の列を確認します。
   - **設定名** と **グループ ポリシー設定のカテゴリ**
   - **MDM サポート** — 緑色の **はい** または黄色の **いいえ** アイコン (クリックできるドリルダウンではありません。行自体には、これらの列以外の詳細情報はありません)
   - **値**、**スコープ** (ユーザー/デバイス)、**最小 OS バージョン**、**CSP 名**、**CSP マッピング** (OMA-URI。**はい** の行にのみ表示)

   > [!NOTE]
   > 設定ごとの詳細パネルや「推奨構成の表示」ページはありません。グループ ポリシー分析が設定について把握している情報はすべて、この行に既に含まれています。`GPO_Desktop_Settings` では、2 つの設定が **はい** と表示されます。**Remove Run menu from Start Menu** と **Prevent changes to Taskbar and Start Menu Settings** です (どちらも `Policy` CSP 設定にマップされます)。残りは **いいえ** と表示されます。これらは、どの MDM プロバイダーにも公開されていないか、サポートされている CSP 一覧 (Policy、PassportForWork、BitLocker、Firewall、AppLocker、Group Policy Preferences) の対象外です。

3. **戻る** を選択して **グループ ポリシー分析** の一覧に戻ります。

4. **GPO_Desktop_Settings** の横にあるチェック ボックスをオンにし、ツール バーから **Migrate** を選択します。

5. **移行する設定** タブで、サポートされている 2 つの設定についてのみ **移行** チェック ボックスをオンにします。
   
   - **Remove Run menu from Start Menu**
   - **Prevent changes to Taskbar and Start Menu Settings**
   
   **いいえ** のサポートの設定はオフのままにします。移行しても、どのみち機能する設定にはなりません。**次へ** を選択します。
   
6. **構成** タブで、インポートされた値 (GPO から引き継がれたもの) を確認し、**次へ** を選択します。

7. **プロファイル情報** タブで、次を入力します。
   - **名前:** `Migrated - GPO Desktop Settings`
   - **説明:** `Settings Catalog profile migrated from the on-premises GPO_Desktop_Settings GPO`

8. **次へ** を選択します。

9. **スコープ タグ** タブで **+ スコープ タグの選択** を選択し、**Pharmacy** を追加して **選択** を選択します。続いて **次へ** を選択します。

10. **割り当て** タブで、**包含されたグループ** の下にある **グループの追加** を選択し、**dyn-Windows-Devices** を検索して選択し、**選択** と **次へ** を選択します。

11. **確認して展開** ページで設定を確認し、**デプロイ** を選択します。

    > [!NOTE]
    > グループ ポリシー分析は、どの設定を直接移行できるか、どの設定に代替手段 (カスタム スクリプト、サードパーティ ツール、または再設計) が必要かを特定することで、GPO から Intune への移行計画を支援します。**移行** 機能はベスト エフォートです。一部の設定は、類似しているが同一ではない設定カタログの同等物に変換され、AppLocker/Firewall の GPO 設定は Endpoint Security 経由で構成されるため、**移行** は完全に無効になります。

**グループ ポリシーの移行準備状況レポートを確認し、サポートされている設定を新しい設定カタログ プロファイルに正常に移行しました。**

---

### タスク 3: 分析結果のエクスポート

1. Microsoft Intune 管理センターで「デバイス」 -「グループ ポリシー分析」と選択するなどして、**グループ ポリシー分析** の一覧ページページに戻ります。 **GPO_Desktop_Settings** の行を選択し、上部のツール バーから **エクスポート** を選択します。

2. ダウンロードが CSV ファイルとしてすぐに開始されます。`C:\LabAssets\GPO-Analysis-Results.csv` に保存します。

3. **Excel** または **Notepad** で CSV ファイルを開き、エクスポートされたデータを確認します。

   CSV には次が含まれます。
   - 設定名
   - 設定のカテゴリ
   - 構成された値
   - 移行準備状況
   - Intune の同等の設定 (利用可能な場合)

**グループ ポリシー分析結果が正常にエクスポートされました。**

---

## 演習 4: Windows Update 管理の構成

### シナリオ

Windows Update for Business ポリシー (更新プログラム リング) を使用して、デバイスが機能更新プログラムと品質更新プログラムを受け取るタイミングを制御します。段階的なロールアウト (パイロット、標準、保守的) のために複数のリングを作成します。

### タスク 1: パイロット更新プログラム リングの作成

1. **Microsoft Edge** を開き、**https://intune.microsoft.com** にアクセスします。 **デバイス** を選択し、**プラットフォーム別** の下で **Windows** を選択して、Windows ブレードで **Windows の更新プログラム** を選択します。

   > [!NOTE]
   > ページ ヘッダーには **デバイス | Windows 更新プログラム** と表示されます。タブは **リリース**、**更新プログラム リング**、**機能更新プログラム**、**品質更新プログラム**、**ドライバー更新プログラム**、**監視** です。ページは **リリース** で開きます。次の手順でタブを切り替えます。
   >
   > このページには、ラボでは無視して問題のない 2 つのバナーが表示される場合もあります。
   >
   > - **Hotpatch の有効化** — 対象デバイスは Hotpatch 品質更新プログラムを自動的に受け取ります。**オプトアウト** ボタンはそのままにします。
   > - **Windows 10 は 2025 年 10 月 14 日にサポートが終了しました** — 情報提供です。ラボでは引き続き Windows 10 を参照します。

2. **更新リング** タブを選択します。

3. **+ プロファイルの作成** を選択します。

4. **基本情報** タブで、次を入力します。
   - **名前:** `Update Ring - Pilot`
   - **説明:** `Pilot ring for early adopters—receives updates immediately`

5. **次へ** を選択します。

6. **更新リングの設定** タブで、次のように構成します。
   
   - **Microsoft 製品の更新プログラム:** 許可
   - **Windows ドライバー:** 許可
   - **品質更新プログラムの延期期間 (日数):** 0
   - **機能更新プログラムの延期期間 (日数):** 0
   - **機能更新プログラムのアンインストール期間 (2～60 日) の設定:** 30
   
7. **ユーザー エクスペリエンスの設定** の下で、次のように構成します。
   - **自動更新の動作:** メンテナンス時に自動的にインストールおよび再起動する
   - **アクティブ時間の開始:** 午前 8:00
   - **アクティブ時間の終了:** 午後 5:00
   - **Windows 更新プログラムを一時停止するためのオプション:** 無効にする
   - **Windows 更新プログラムのチェックのオプション:** 有効にする

8. **次へ** を選択します。

9. **スコープ タグ** タブで **Pharmacy** を追加し、**次へ** を選択します。

   > [!NOTE]
   > パイロット リングに `Pharmacy` のタグを付けておくと、**ラボ 05 演習 3** でロールを割り当てるときに、(臨床の更新プログラムを最初にパイロットする) Pharmacy ヘルプデスクから引き続き表示できます。

10. **割り当て** タブで、**包含されたグループ** の下にある **グループの追加** を選択します。

11. **sg-Intune-Pilot-Users** を検索して選択します。

12. **選択** を選択します。

13. **次へ** を選択します。

14. **レビューと作成** タブで **作成** を選択します。

**パイロット更新プログラム リングが正常に作成されました。**

---

### タスク 2: 標準更新プログラム リングの作成

1. **更新リング** タブで **+ プロファイルの作成** を選択します。

2. **基本情報** ページで、次を入力します。
   
   - **名前:** `Update Ring - Standard`
   - **説明:** `Standard ring for general users—defers updates by 7 days`
   
3. **次へ** を選択します。

4. **更新プログラム リングの設定** タブで、次のように構成します。
   - **品質更新プログラムの延期期間 (日数):** 7
   
   - **機能更新プログラムの延期期間 (日数):** 14
   
   - **自動更新の動作:** メンテナンス時に自動的にインストールおよび再起動する
   
   - **アクティブ時間の開始:** 午前 8:00
   
   - **アクティブ時間の終了:** 午後 5:00
   
   - **Windows 更新プログラムを一時停止するためのオプション:** 有効にする
   
      (ユーザーは最大 7 日間、更新プログラムを一時停止できます)
   
5. **次へ** を選択します。

6. **スコープ タグ** タブで **次へ** を選択します。

7. **割り当て** タブで、**包含されたグループ** の下にある **グループの追加** を選択します。

8. **dyn-Windows-Devices** を検索して選択します。

9. **選択** を選択します。

10. **除外されたグループ** の下で **グループの追加** を選択します。

11. **sg-Intune-Pilot-Users** を検索して選択します (パイロット リングが既に割り当てられているパイロット ユーザーを除外するため)。

12. **選択** を選択します。

13. **次へ** を選択します。

14. **レビューと作成** タブで **作成** を選択します。

**延期を含む標準更新プログラム リングが正常に作成されました。**

---

### タスク 3: （オプション）Windows Update の展開状態を監視する

パイロット コホート (**ラボ 01 演習 1** で作成した `sg-Intune-Pilot-Users`) は、**ラボ 01 演習 4** でブロック用の ESP を受け取り、このラボの演習 1 でパイロット構成プロファイルを受け取ったのと同じグループです。この 1 つのコホートは、このラボ シリーズのすべてのロールアウトを貫いています。ここでの更新プログラム リング、ラボ 04 の ASR ルール、ラボ 05 の修復ロールアウト、ラボ 06 の EPM です。リング全体で 1 つの一貫したパイロット コホートを監視することが、運用環境で段階的ロールアウトを機能させる秘訣です。

1. **Microsoft Intune 管理センター** で **レポート** → **Windows 品質更新プログラム** の順に移動します。

   ページ ヘッダーには **レポート | Windows 更新プログラム** と表示され、**概要** タブが開きます。ここには、**Windows 機能更新プログラム** と **Windows 迅速な品質更新プログラム** の 2 つの集計レポートが表示されます。同じページの 2 つ目の **レポート** タブには、詳細なドリルイン レポートが一覧表示されます。

2. **概要** タブの **Windows 品質更新プログラム** セクションで、**品質更新プログラムの状態** を選択します。

3. レポート データを確認します。
   - **デバイス名**
   - **更新の状態**
   - **インストールされているリリース**
   - **利用可能なターゲットリリース**
   - **適用されたポリシー**

**Windows Update の展開状態が監視されました。**

---

### タスク 4: （オプション）機能更新プログラム プロファイルの作成

更新プログラム リングは、更新プログラムをインストールする *タイミング* を制御します。**機能更新プログラム プロファイル** は、デバイスを固定する Windows の *バージョン* を制御するもので、別の軸です。ここでは、パイロット コホートがパイロット更新プログラム リングを介して先行する一方で、より広範なフリートを Windows 11 24H2 に固定する機能更新プログラム プロファイルを作成します。

1. **Microsoft Intune 管理センター** の **デバイス** → **Windows 更新プログラム** で、**機能更新プログラム** タブを選択します。

2. **+ 作成 > 機能更新プログラム ポリシーの作成** を選択します。

3. **デプロイの設定** タブで、次のように構成します。
   
   - **名前:** `Feature Update - Win11 25H2`
   - **説明:** `Pin Contoso fleet to Windows 11 25H2`
   - **展開する機能更新プログラム:** Windows 11, version 25H2
   - **必要な更新プログラムとしてユーザーが利用できるようにする** をオンにします
   
   > [!NOTE]
   > 運用環境では、**段階的ロールアウト** オプションを使用して、スケジュールに従ってフリートのサブセットに機能更新プログラムをリリースします。このラボでは、即時利用可能にすることでフローをシンプルに保ちます。
   
4. **次へ** を選択します。

5. **スコープ タグ** タブで **Default** スコープ タグをそのままにします (このプロファイルはテナント全体です)。**次へ** を選択します。

6. **割り当て** タブで **dyn-Windows-Devices** に割り当てます。**グループの除外** の下で **sg-Intune-Pilot-Users** を追加します (パイロット コホートはパイロット更新プログラム リングを介して先行するため、ここで除外することで、機能更新プログラム プロファイルによって足止めされるのを防ぎます)。

7. **次へ** を選択します。

8. **レビューと作成** タブで **作成** を選択します。

**Windows 機能更新プログラム ポリシーが正常に作成されました。**

---

### タスク 5: （オプション）迅速な品質更新プログラム ポリシーの作成

**迅速な品質更新プログラム** は、通常の延期期間よりも早く帯域外のセキュリティ パッチをプッシュします。これは、進行中のゼロデイに対する適切な対応策です。ここでは、通常の更新プログラム リングが適用するあらゆる延期を上書きして、最新の重要なセキュリティ パッチを 2 日以内にインストールするポリシーを作成します。

1. **デバイス** → **Windows 更新プログラム** で、**品質更新プログラム** タブを選択します。

2. **+ 作成 > 優先ポリシー** を選択します。

3. **設定** タブで、次のように構成します。
   - **名前:** `Quality Update - Expedited critical patches`
   - **説明:** `Push out-of-band security patches within 2 days, overriding ring deferrals`
   - **迅速化する品質更新プログラムを選択します** — 利用可能な最新の更新プログラムを選択します。
   - **再起動が必要な場合は、適用されるまでの日数を選択します:** 2 日

4. **次へ** を選択します。

5. **スコープ タグ** タブで **Default** をそのままにします。**次へ** を選択します。

6. **割り当て** タブで **dyn-Windows-Devices** に割り当てます (除外なし。迅速なセキュリティ更新プログラムは、パイロットを含むすべてのユーザーに適用されます)。

7. **次へ** を選択します。

8. **レビューと作成** タブで **作成** を選択します。

   > [!NOTE]
   > 更新プログラム リング + 機能更新プログラム プロファイル + 迅速な品質更新プログラム ポリシーは、Intune における Windows Update for Business の 3 つのレイヤーです。リングは通常の品質更新プログラムのタイミングを制御し、機能更新プログラム プロファイルは提供される Windows のバージョンを制御し、迅速な品質更新プログラム ポリシーはセキュリティ上重要なパッチのタイミングを上書きします。

**迅速な品質更新プログラム ポリシーが正常に作成されました。**

---

## 演習 5: エンドポイント分析とプロアクティブな修復の有効化

### シナリオ

エンドポイント分析は、デバイスのパフォーマンス、起動時間、ユーザー エクスペリエンスに関するインサイトを提供します。プロアクティブな修復は、ユーザーが問題を報告する前に、一般的な問題を自動的に検出して修正します。

### タスク 1: エンドポイント分析の有効化

1. **Microsoft Intune 管理センター** で **レポート** を選択し、**分析** グループを展開して、**エンドポイント分析** を選択します。

2. テナントで初めてエンドポイント分析にアクセスすると、**エンドポイント分析 | 概要** ページが表示されます。**デバイス データの収集元** を **すべてのクラウドマネージドデバイス** に設定したままにし、**開始** を選択してデータ収集を有効にします。

   > [!NOTE]
   > 以前の管理者が既にデータ収集を開始している場合、**開始** ボタンは表示されず、代わりに **概要** が表示されます。次の手順にスキップします。

3. 左側ナビゲーションで **設定** を選択します。

4. **Intune データ収集ポリシー** セクションを確認します。

   > [!NOTE]
   > エンドポイント分析では、デバイスが診断データを Microsoft に送信する必要があります。これは、Intune に登録されたデバイスに対して自動的に有効になります。

5. **エンドポイント分析 | 概要** ページに戻ります。

   > [!NOTE]
   > エンドポイント分析では、意味のあるインサイトを表示する前に、24～48 時間分のデバイス テレメトリが必要です。新しいラボ環境では、ダッシュボードに表示されるデータは限られています。それでも、ダッシュボードの構造を確認し、追跡されるメトリックを理解することはできます。

6. 左側ナビゲーションで **レポート** グループを展開し、利用可能なレポートを確認します。
   - **起動パフォーマンス:** 起動時間とログオンの所要時間
   - **アプリケーションの信頼性:** アプリのクラッシュとハング
   - **どこからでも作業:** クラウド接続と推奨されるアクション
   - **リソースのパフォーマンス:** CPU とメモリの使用率
   - **バッテリーの正常性:** バッテリーの状態と稼働時間

**エンドポイント分析が正常に有効化されました。**

---

### タスク 2: Windows ライセンス認証の有効化

プロアクティブな修復では、スクリプト パッケージを作成または実行する前に、テナント レベルで Windows ライセンス認証を有効にする必要があります。タスク 3 で修復を構築する前に、ここで有効にします。

1. **SEA-DEV1** に切り替えます。

2. 手元PCではなく) **SEA-DEV1 の Microsoft Edge** で **Microsoft Intune 管理センター** で **テナント管理** を選択し、続いて **コネクタとトークン** を選択します。

3. **Windows データ** を選択します。

4. **Windows ライセンス認証** の下で、**自分のテナントがこれらのライセンスのいずれかを所有していることを確認しました。** をオンにします。

   > [!NOTE]
   > これには、**グローバル管理者** または **Intune サービス管理者** である必要があります。これは、テナントが次のいずれかを保有していることを確認します。Windows 10/以降 Enterprise E3/E5 (または Microsoft 365 F3/E3/E5)、Windows 10/以降 Education A3/A5 (または Microsoft 365 A3/A5)、Windows Virtual Desktop Access E3/E5 のいずれかです。これは Intune Suite や修復アドオンには関連付けられていません。これらのライセンスのいずれも持たないラボ テナントでは、タスク 3 で修復ウィザードを一通り進めることはできますが、スクリプト パッケージはデバイス上で実行されません。

5. 変更を加えた場合は **保存** を選択します。

**Windows ライセンス認証が正常に有効化されました。**

---

### タスク 3: プロアクティブな修復スクリプト パッケージの作成

プロアクティブな修復は、デバイス上で PowerShell スクリプトを実行して、問題を自動的に検出して修正します。

1. (手元PCではなく) **SEA-DEV1 の Microsoft Edge** を使用します。 **Microsoft Intune 管理センター** で **デバイス** を選択し、**デバイスの管理** の下で **スクリプトと修復** を選択します。

2. **修復** タブを選択します (ページは既定でこのタブで開きます)。

3. **+ 作成** を選択します。

4. **基本** タブで、次を入力します。

   - **名前:** `Remediation - Clear Temp Files`
   - **説明:** `Detects and clears temporary files older than 30 days`

5. **次へ** を選択します。

6. **設定** ページで、次のように構成します。
   - **検出スクリプト ファイル:** **ファイルの選択** を選択し、参照して `C:\AllFiles\Detect-TempFiles.ps1` (ラボ アセットで提供) に移動します。

     検出スクリプトの例:
     ```powershell
     # Create the folder if it doesn't exist
     New-Item -Path "C:\LabAssets\Remediations" -ItemType Directory -Force
     
     # Detection script
     @'
     $tempPath = "$env:TEMP"
     $oldFiles = Get-ChildItem -Path $tempPath -Recurse -File | Where-Object { $_.LastWriteTime -lt (Get-Date).AddDays(-30) }
     if ($oldFiles.Count -gt 0) {
         Write-Output "Found $($oldFiles.Count) old temp files"
         exit 1  # Issue detected
     } else {
         Write-Output "No old temp files found"
         exit 0  # Compliant
     }
     '@ | Set-Content -Path "C:\LabAssets\Remediations\Detect-TempFiles.ps1" -Encoding UTF8
     ```
     
   - **修復スクリプト ファイル:** **ファイルの選択** を選択し、参照して `C:\AllFIles\Remediate-TempFiles.ps1` に移動します。

     修復スクリプトの例:
     ```powershell
     # Remediation script
     @'
     $tempPath = "$env:TEMP"
     try {
         Get-ChildItem -Path $tempPath -Recurse -File | Where-Object { $_.LastWriteTime -lt (Get-Date).AddDays(-30) } | Remove-Item -Force
         Write-Output "Cleared old temp files"
         exit 0  # Success
     } catch {
         Write-Error "Failed to clear temp files: $_"
         exit 1  # Failure
     }
     '@ | Set-Content -Path "C:\LabAssets\Remediations\Remediate-TempFiles.ps1" -Encoding UTF8
     ```

   - **この スクリプトをログオンしている資格情報を使用して実行する:** いいえ (SYSTEM として実行)
   - **スクリプトの署名チェックを強制する:** いいえ
   - **64 ビット PowerShell でスクリプトを実行する:** はい

7. **次へ** を選択し、**Pharmacy** スコープ タグを追加して **次へ** を選択します。

8. **割り当て** タブで、**包含されたグループ** の下にある **+ 含めるグループの選択** を選択します。

9. **dyn-Windows-Devices** を検索して選択します。

10. **選択** を選択します。

11. **次へ** を選択します。

12. **レビューと作成** タブで **作成** を選択します。

**プロアクティブな修復スクリプト パッケージが正常に作成されました。**

---

### タスク 4: 修復の実行を監視する

1. **Microsoft Intune 管理センター** の **修復** タブで、一覧から **Remediation - Clear Temp Files** を選択します。

2. **デバイスの状態** タブを選択します。

3. デバイスの実行結果を確認します。
   - **検出状態:** 問題が検出されたかどうかを示します
   - **修復状態:** 修復が成功したか失敗したかを示します
   - **最終チェックイン:** 最後のスクリプト実行のタイムスタンプ

   > [!NOTE]
   > プロアクティブな修復はスケジュール (既定: 1 日 1 回) で実行されます。最初のポリシー展開後、最初の実行まで 1～2 時間待ってから、結果を確認します。

**プロアクティブな修復の実行が正常に監視されました。**

---

## 演習 6: トラブルシューティング ブレードの使用

### シナリオ

トラブルシューティング ブレードは、ユーザーのデバイス、ポリシー、アプリのインストール、登録状態を統合したビューを提供します。これを使用して、デバイスのコンプライアンスとポリシーの割り当てを調査します。

### タスク 1: ユーザーのデバイスの状態を調査する

1. **Microsoft Intune 管理センター** で **トラブルシューティングとサポート** を選択し、**トラブルシューティング** を選択します。
2. **ユーザー** フィールド (プレースホルダー テキスト「Search by display name or email」) で、**Megan Bowen** を検索して選択します。
3. ユーザーを選択すると、スクロール可能なセクションではなく、タブの行が表示されます。**概要**、**デバイス**、**グループ**、**ポリシー**、**アプリケーション**、**アプリ保護ポリシー**、**更新プログラム** で、さらに **...** オーバーフロー メニューの下にもあります。
4. **デバイス** タブを選択します。
5. Megan Bowen にパイロット コホートのデバイス (**SEA-DEV1** または **SEA-DEV2**) が一覧表示されていることを確認します。
6. 一覧からそのデバイスを選択して、デバイス ブレードを開きます。
7. 次を確認します。
   - **登録者**
   - **最終チェックイン時刻**
   - **コンプライアンス**
   - **プライマリ ユーザー**
   - **管理名**

**ユーザーのデバイスの状態が正常に調査されました。**

---

### タスク 2: （オプション）設定ごとの状態を使用してポリシーの競合を診断して解決する

**演習 1 タスク 5（オプション）** では、`sg-Intune-Pilot-Users` グループの **Allow Camera** 設定で競合する 2 つの構成プロファイル (`WIN - Camera - Enabled (Pilot)` と `WIN - Camera - Disabled (Pilot)`) を意図的に作成しました。ここでは、その競合をポータルで見つけて解決します。

> [!IMPORTANT]
> **デバイスの前提条件。** **競合** の状態は、Windows デバイスが競合するポリシーを適用した状態で実際にチェックインした後にのみ表示されます。パイロット コホートのデバイス (**SEA-DEV1** または **SEA-DEV2**) がオンラインで、2 つのカメラ プロファイルが割り当てられた状態で少なくとも 1 回同期している必要があります。以下の手順で **競合** ではなく **保留中** または **評価されていません** しか表示されない場合は、**デバイス** に移動してデバイスを選択し、**同期** を選択して 5～10 分待ちます。

1. **タスク 1** の最後に開いたデバイス ブレードにまだいるはずです。そうでない場合は、**デバイス** → **すべてのデバイス** の順に移動し、同じパイロット コホートのデバイス (**SEA-DEV1** または **SEA-DEV2**) を再度選択します。

2. デバイス ブレードの左側ナビゲーションで、**モニター** の下にある **デバイスの構成** を選択します。

3. **状態** 列を確認します。このレポートには、デバイスに割り当てられたすべてのポリシー (**ポリシー**、**ログイン ユーザー**、**ポリシーの種類**、**状態**) が、実際のポリシーごとの状態とともに一覧表示されます。これは、状態をまったく表示しないトラブルシューティング ブレードの **ポリシー** タブよりも、すっきりとした直接的なビューです。

4. `WIN - Camera - Enabled (Pilot)` と `WIN - Camera - Disabled (Pilot)` を見つけます。どちらも **状態: 競合** と表示されるはずです。

   > [!NOTE]
   > この一覧では、他の無関係なポリシーにも **競合** または **エラー** が表示される場合があります (たとえば、2 つの更新プログラム リングがどちらもこのデバイスを対象にしている場合や、以前のプロファイルに実際の構成ミスがある場合など)。これは、このラボ シリーズで展開してきた他のすべてのものによる想定内のノイズです。このタスクでは、2 つのカメラ プロファイルだけに注目してください。

5. `WIN - Camera - Disabled (Pilot)` を選択して **ポリシー設定** ページを開きます。

   > [!NOTE]
   > **ポリシー設定** ページには、プロファイル内の各設定が、その解決状態とともに **名前**、**状態**、**エラー コード** の列のテーブルに一覧表示されます (状態値には **成功**、**保留中**、**エラー**、**競合**、**適用不可** が含まれます)。**競合** の状態は、2 つ以上のポリシーが同じ設定を異なる値に設定しようとしていることを意味します。Intune はこれを解決できないため、どちらも適用せず、デバイスは既存のローカル値を保持します。

6. **Allow Camera** の行を見つけ、**状態** 列に **競合** と表示されていることを確認します。

7. 競合を解決します。Contoso では Pharmacy の臨床規制が優先され (臨床エリアではカメラをオフにする)、**Disabled** プロファイルを残して **Enabled** プロファイルを削除します。
   - **デバイス** → **デバイスの管理** → **構成** に戻ります。
   - **WIN - Camera - Enabled (Pilot)** を選択します。
   - ツール バーから **削除** を選択し、**OK** を選択して確定します。

8. デバイスの同期をトリガーし (**デバイス** → **すべてのデバイス** で同じデバイスを選択し、**同期** を選択)、デバイスが再評価するまで 2～5 分待ちます。

9. デバイスの **監視** → **デバイスの構成** レポートに戻り、`WIN - Camera - Disabled (Pilot)` が (**競合** ではなく) **状態: 成功** と表示され、**Disabled** の値が適用されていることを確認します。

   > [!NOTE]
   > 運用環境で使用できた代替の解決策: (a) 一方のプロファイルの割り当てを変更して、2 つが同じグループで重複しないようにする、(b) 競合する設定を一方のプロファイルから完全に取り除く、(c) (サポートされている場合) ポリシーを並べ替えて **設定カタログの優先順位** を使用する。負けた方を削除するのが最もシンプルですが、実際のフリートでは、削除する前に、競合する各プロファイルを誰がなぜ作成したかを監査してください。

**設定ごとの状態を使用して、実際のポリシーの競合を正常に診断して解決しました。**

---

### タスク 3: トラブルシューティング ブレードからデバイスの同期を強制する

1. **トラブルシューティング + サポート | トラブルシューティング** ページ (Megan Bowen が選択された状態) の **デバイス** タブで、**SEA-DEV1** を選択します。

2. デバイス アクションのツール バーから **同期** を選択し、**はい** を選択します。

3. 同期が完了するまで待ちます (通常 1～2 分)。

4. ページを更新し、**最終チェックイン** のタイムスタンプが更新されたことを確認します。

   > [!NOTE]
   > 同期アクションは、デバイスに Intune へ即座にチェックインさせ、新しいポリシーを取得させ、現在の状態を報告させます。これは、ポリシー展開の遅延をトラブルシューティングするときに便利です。

**トラブルシューティング ブレードからデバイスの同期が正常に強制されました。**

---

### タスク 4: （オプション）コンプライアンスの状態と条件付きアクセス (レポート専用) の影響を調査する

**演習 2 タスク 4** で作成した条件付きアクセス ポリシー `CA - Require compliant device (Pharmacy pilot)` は、**レポート専用** モードで動作しています。適用はされませんが、サインインごとに何が *起きたはずか* をログに記録します。ここでは、**ラボ 04 演習 6** で **オン** に切り替える前に、それらのログを確認してポリシーの影響を確認します。

1. **トラブルシューティング + サポート | トラブルシューティング** ページで、パイロット コホートのユーザー (Megan Bowen または別の `sg-Intune-Pilot-Users` のメンバー) が選択された状態で、**コンプライアンス** セクションまでスクロールします。

2. ユーザーのデバイスのコンプライアンス状態を確認します。**エラー** または **評価されていません** の状態は、適用モードの CA ポリシーがサインインをブロックすることを意味します。

3. 新しいブラウザー タブで **https://entra.microsoft.com** を開き、**監視と正常性** → **サインイン ログ** の順に移動します。

4. **ユーザー サインイン (対話型)** タブで、**フィルターの追加** を選択して同じパイロット ユーザーの **ユーザー プリンシパル名** でフィルター処理し、**日付範囲** フィルターを **過去 24 時間** に設定します。

5. **アプリケーション** 列に実際のクラウド アプリまたはリソース (たとえば **Microsoft Intune 管理センター**、**Office 365**、**Microsoft 365 admin portal** など) が表示されているサインイン エントリを選択します。**Device Registration Service** や同様のシステム/登録アプリのエントリは避けます。

   > [!NOTE]
   > 条件付きアクセスは、クラウド リソースへのサインインのみを評価します。ローカルの Windows サインイン プロセス、Windows Hello for Business、デバイスの登録サインイン (「ブートストラップ シナリオ」。循環依存を避けるために除外) には適用されません。これらのエントリのいずれかを選択すると、ポリシーの構成に関係なく、条件付きアクセス タブに **適用不可** と表示されます。これはそのエントリでは想定内であり、ポリシーの構成ミスの兆候ではありません。実際の **レポート専用** の結果を確認するには、実際のクラウド アプリのリソースを持つ別のエントリを選択してください。

6. 最近のサインイン エントリを選択して、**アクティビティの詳細: サインイン** ペインを開きます。

7. 詳細ペインで **条件付きアクセス** タブに切り替えます。`CA - Require compliant device (Pharmacy pilot)` が、**結果** が **レポート専用: 成功**、**レポート専用: 失敗**、**レポート専用: 適用されない**、**レポート専用: ユーザー操作が必要**、または **適用されない** として一覧表示されるはずです。

   > [!NOTE]
   > **レポート専用の結果のデコーダー:**
   > - **成功** — ユーザー/デバイスは許可条件を満たしたはずです (たとえば、デバイスが準拠している)。今ポリシーを適用しても、このサインインはブロックされません。
   > - **失敗** — 許可要件 (コンプライアンス) が満たされ *ませんでした*。今適用すると、このサインインは **ブロックされます**。これが注目すべき点です。
   > - **適用されない** — ポリシーがサインインのユーザー/アプリ/条件の基準に一致しませんでした。非パイロット ユーザーでは想定内です。
   > - **ユーザー操作が必要** — ユーザーが修正できます (たとえば、MFA を完了する)。コンプライアンスのみの許可では、あまり一般的ではありません。
   > - **適用不可** — この特定のサインイン イベントは、条件付きアクセスから完全に除外されました (Windows Hello for Business、デバイスの登録、または別のブートストラップ シナリオ)。レポート専用モードかどうかにかかわらず、評価されることはありません。パイロット ユーザーのサインインのほとんどがこれを示す場合は、代わりに実際のクラウド アプリに関連付けられたエントリを選択してください。

8. `sg-Intune-Pilot-Users` の *外部* のユーザー (たとえば admin アカウント) からの 2 つ目のサインイン エントリを開きます。ポリシーがパイロット グループのみにスコープ設定されているため、CA ポリシーに **レポート専用: 適用されない** と表示されることを確認します。

   > [!IMPORTANT]
   > レポート専用 → オンは、意図的な 2 段階のロールアウトです。少なくとも数時間 (運用環境では数日) レポートを監視し、**失敗** の数が想定どおり (つまり、非準拠のデバイスのみ) であることを確認してから、**オン** に切り替えます。この切り替えは、ラボ 04 のエンドポイント セキュリティ ポリシーによってさらに多くのデバイスが確実に準拠した後、**ラボ 04 演習 6** で実行します。

**条件付きアクセス ポリシーのレポート専用の影響と、その背後にあるコンプライアンス状態が正常に調査されました。**

---

## ラボのまとめ

おめでとうございます! ラボ 02: デバイスの管理と保守 を完了しました。

このラボでは、次のことを達成しました。

**演習 1: 構成プロファイルの作成**
- 電源管理用の設定カタログ プロファイルを作成した (`Pharmacy` でタグ付け)
- 組み込みのテンプレートを使用してデバイス制限プロファイルを作成した (`Pharmacy` でタグ付け)
- 複合フィルターと単純な割り当てフィルターを作成し、1 つを **除外** モードで適用した
- パイロット コホート向けに競合する 2 つのカメラ プロファイルを意図的に作成した (演習 6 で解決)

**演習 2: コンプライアンス ポリシーの構成**
- デバイスの正常性とセキュリティの要件を含む Windows コンプライアンス ポリシーを作成した (`Pharmacy` でタグ付け)
- 非準拠に対する猶予期間と通知アクションを構成した
- 条件付きアクセス ポリシー (`CA - Require compliant device (Pharmacy pilot)`) を **レポート専用** モードで作成した (ラボ 04 演習 6 で **オン** に切り替え)
- 登録済みデバイスのコンプライアンス ポリシーの結果を監視した

**演習 3: グループ ポリシー オブジェクトの分析**
- グループ ポリシーのバックアップ XML をグループ ポリシー分析にインポートした
- 移行準備状況レポートを確認して、サポートされている/されていない設定を特定した
- 計画のために分析結果をエクスポートした

**演習 4: Windows Update 管理の構成**
- 早期採用者向けに延期なしのパイロット更新プログラム リングを作成した (`Pharmacy` でタグ付け)
- 品質更新プログラム 7 日、機能更新プログラム 14 日の延期を含む標準更新プログラム リングを作成した
- フリートを Windows 11 24H2 に固定する機能更新プログラム プロファイルを作成した (パイロット コホートは先行できるように除外)
- 帯域外のセキュリティ パッチ用に迅速な品質更新プログラム ポリシーを作成した
- デバイス全体で Windows Update の展開状態を監視した

**演習 5: エンドポイント分析とプロアクティブな修復の有効化**
- デバイスのパフォーマンスとユーザー エクスペリエンスを監視するためにエンドポイント分析を有効にした
- 修復のテナント レベルの前提条件である Windows ライセンス認証を有効にした
- 古い一時ファイルを検出してクリアするプロアクティブな修復スクリプト パッケージを作成した
- 修復の実行結果を監視した

**演習 6: トラブルシューティング ブレードの使用**
- ユーザーのデバイスの状態とポリシーの割り当てを調査した
- **設定ごとの状態** を使用して、実際のポリシーの競合を診断して解決した
- 新しいポリシーをすぐに取得するためにデバイスの同期を強制した
- サインイン ログを介して条件付きアクセス ポリシーの **レポート専用** の影響を確認した

**重要なポイント:**
- 構成プロファイルは、設定カタログ (きめ細かな制御) またはテンプレート (事前構成されたバンドル) を使用して作成できます
- 割り当てフィルターは、追加のグループを作成せずにポリシーの対象を絞り込みます
- 猶予期間を含むコンプライアンス ポリシーは、アクセスがブロックされる前に問題を修正する時間をユーザーに提供します
- グループ ポリシー分析は、サポートされている設定を特定することで、オンプレミスからクラウドへの移行計画を支援します
- Windows Update リングは、安定性のための延期を伴う段階的なロールアウトを可能にします
- エンドポイント分析とプロアクティブな修復は、プロアクティブなデバイス管理と問題解決を可能にします
- トラブルシューティング ブレードは、効率的なトラブルシューティングのためにデバイス、ポリシー、アプリの状態を統合します

**次のステップ:**
ラボ 03 では、Microsoft Store アプリ、Win32 パッケージ、Microsoft 365 Apps、アプリ保護ポリシーを使用して、マネージド デバイスにアプリケーションを展開します。

---

**ラボ終了**
