---
lab:
  title: 'ラボ 01: 基盤 — ID、登録、Autopilot'
  description: 'このラボでは、Microsoft Entra ID の ID ガバナンス、デバイスの登録および登録ポリシー、Windows Autopilot を構成し、Intune によるデバイス管理に向けて Microsoft 365 テナントを準備します。'
  duration: 90 minutes
  level: 200
  islab: true
  primarytopics:
    - Microsoft Intune
    - Microsoft Entra ID
    - Windows
    - Windows Autopilot
---

# ラボ 01: 基盤 — ID、登録、Autopilot

## ラボのシナリオ

あなたは Contoso Healthcare のモダン エンドポイント管理者である **Jordan Chen** です。Contoso は、Microsoft Intune と Microsoft Entra ID を使用したクラウド ファーストのエンドポイント管理戦略を採用しようとしています。最初のタスクは、ID ガバナンス（ユーザー、グループ、ロール）、デバイス登録ポリシー、Windows 登録ポリシー、Windows Autopilot を構成して、デバイス管理に向けて Microsoft 365 テナントを準備することです。この基盤となる構成により、後続のラボでのデバイス登録とポリシー展開の作業が可能になります。

このラボを完了すると、次のことができているようになります:
- 組織単位でのターゲット設定のために、ユーザーと動的グループ（複合ルールの動的グループを含む）を構成する
- Microsoft Entra ID のロール、管理単位、およびスコープ タグ付きのカスタム Intune RBAC ロールを使用して、管理アクセスを委任する
- デバイス登録ポリシーを設定し、Microsoft Entra Local Administrator Password Solution (LAPS) を有効にする
- Intune への自動登録を確認し、登録状態ページ (Enrollment Status Page) プロファイルを構成する
- 個人所有の Android デバイスの登録をブロックする
- Microsoft Entra 参加を使用して 2 台の Windows 11 デバイスを登録する
- 展開プロファイルを使用して、デバイスを Windows Autopilot に登録する

---

## ラボの所要時間

**推定時間:** 90 分

---

## 手順

### 開始する前に

このラボには次が必要です:
- Contoso の Microsoft 365 テナント (`<TenantPrefix>.onmicrosoft.com` または同等のもの) へのアクセス
- **グローバル管理者**の資格情報
- 4 台の仮想マシン: **SEA-DEV1**、**SEA-DEV2**、**SEA-DEV3**、**LIN-SRV1**
- すべての仮想マシンからのインターネット接続

**重要:** これは MD-102 ラボ シリーズの基盤となるラボです。後続のすべてのラボは、このラボで完了した構成 (登録済みデバイス、ユーザー、グループ、ポリシー) を前提とします。

---

> [!IMPORTANT]
> **Lab 03 を開始する前に、今すぐ Microsoft Intune Suite の 90 日間試用版を有効化してください。** 後続のいくつかの演習 (Lab 03 演習 4、Lab 04 演習 4～5、Lab 06 演習 1～3) では Intune Suite の機能が必要です。事前に試用版を有効化しておくと、以降のすべてのラボが問題なく動作し、ラボの途中で予期しないブロッカーが発生するのを防げます。この試用版は無料で、期間は 90 日間、テナントあたり最大 250 ユーザーまで利用でき、既存のテナントの課金関係を再利用します。支払い方法は不要です。
>
> **手順 (約 2 分かかります):**
>
> 1. **Microsoft Intune 管理センター** (`intune.microsoft.com`) で、**テナント管理**を選択し、続いて **Intune アドオン**を選択します。
> 2. **すべてのアドオン**タブを選択します。
> 3. **Microsoft Intune Suite** の行で、**試用または購入**列の下にある **詳細を表示します**を選択します。
> 4. 詳細ペインで、**試用または購入するには、Microsoft 365 管理センターに移動してください**を選択します。新しいタブが開き、Microsoft 365 管理センターの製品ページが表示されます。
> 5. **Microsoft Intune Suite** のオファー ページで、**無料試用の開始**を選択します。
> 6. **チェックアウト**ページで、次の内容を確認します: **Microsoft Intune Suite Trial**、90 日間の期間、250 ライセンス、**USD 0.00**、支払い方法は不要。
> 7. **編集**を選択し、組織プロファイル フォームに次の情報を入力してから、**保存**を選択します:
> - **名**: `MOD`
> - **姓**: `Administrator1`
> - **町名、番地**: `1 Microsoft Way`
> - **市区町村**: `Redmond`
> - **都道府県**: `ワシントン`
> - **郵便番号**: `98052`
> - **電話**: `425-555-1234`
> - **メール アドレス**: `admin@<TenantPrefix>.onmicrosoft.com`
> 8. **今すぐ試す**を選択して試用版を有効化します。
> 9. Intune 管理センターに戻ります。**テナント管理 → Intune アドオン**を更新します。**自分のアドオン**タブを選択すると、数分以内に **Microsoft Intune Suite Trial** が **購入済み数量** **250** で一覧に表示されます。Suite には次が含まれます: **Intune Plan 2**、**Remote Help**、**Endpoint Privilege Management**、**Enterprise App Management**、**Advanced Analytics**、**Cloud PKI**。
>
> **すべてのアドオン タブに惑わされないでください。** **Microsoft Intune Suite** の行では、サブスクリプションの状態列に **「試用版は残り約 90 日」** と表示されますが、個々の機能の行 (Intune Plan 2、Endpoint Privilege Management、Remote Help、Enterprise App Management、Advanced Analytics、Cloud PKI) は引き続き **「試用または購入が可能」** と表示されます。これは想定どおりの動作です。これらは *スタンドアロン* のアドオン SKU であり、Suite 試用版はそれらすべてを Suite レベルでバンドルしています。サブスクリプションの状態列に試用版の残りが約 89～90 日と表示されていることを確認して、Microsoft Intune Suite 試用版が有効であることを確認してください。
>
> 試用版の期間は 90 日間で、その後 30 日間の猶予期間が続きます。**試用版はテナントごとに 1 回のみ開始できます**。そのため、この期間内に Labs 02～06 を完了できるように計画してください。試用版がすでに有効になっている場合は、サブスクリプションの状態列に **有効** と表示され、上記の手順をスキップできます。

---

## 演習 1: ユーザーとグループの構成

### シナリオ

Contoso には、複数の部署 (マーケティング、法務、IT、営業、人事、業務、エンジニアリングなど) にわたって 33 人の既存ユーザーがいます。これらのユーザーを確認し、ラボのシナリオ用に追加のテスト ユーザーを作成し、部署とデバイスの種類に基づくポリシーのターゲット設定を可能にするために動的グループを構成します。

### タスク 1: 既存のユーザーとライセンスの確認

1. **SEA-DEV1** の日本語版 **Microsoft Edge** で **https://admin.cloud.microsoft.com** を開きます。

2. **グローバル管理者**アカウントでサインインします:
   - **ユーザー名:** `admin@<TenantPrefix>.onmicrosoft.com`
   - **パスワード:** (ラボ環境で提供されます)

3. サインインの維持を求められた場合は **いいえ** を選択し、モバイル デバイスで MFA の要求を承認します。

4. 講師の指示に従い、Microsoft 365 管理センターを日本語表示に変更してください。

5. 左側のナビゲーションで **ユーザー** を展開し、**アクティブなユーザー** を選択します。

6. ユーザーの一覧を確認します。次のユーザーを含む、約 **33 人のライセンス付きアクティブ ユーザー** が表示されるはずです:
   - Megan Bowen (マーケティング マネージャー)
   - Alex Wilber (マーケティング アシスタント)
   - Joni Sherman (パラリーガル、法務)
   - Allan Deyoung (IT 管理者)
   - Adele Vance (小売マネージャー)
   - その他、さまざまな部署のユーザー

   > [!NOTE]
   > 一覧には、ライセンスのないサービス アカウント (例: Automate Bot、Conf Room) が含まれる場合もあります。ライセンス付きの 33 人のユーザーだけを表示するには、一覧の上にあるビュー フィルターを **すべてのユーザー** から **ライセンス ユーザー** に変更します。これらのユーザーは Contoso テナントに事前プロビジョニングされており、Microsoft 365 E5 ライセンスが割り当てられています。ラボ全体を通じて、ポリシーのターゲット設定にこれらの既存ユーザーを使用します。

7. 一覧から **Megan Bowen** を選択します。

8. **Megan Bowen** のユーザー詳細ペインで、**ライセンスとアプリ** タブを選択します。

9. 次のライセンスが割り当てられていることを確認します:
   - **Microsoft 365 E5 (no Teams)**
   - **Microsoft Teams Enterprise**

10. ユーザー詳細ペインを閉じます。

**既存ユーザーの確認とライセンスの検証が正常に完了しました。**

---

### タスク 2: 追加シナリオ用のテスト ユーザーの作成

Contoso には 33 人の既存ユーザーがいますが、特定のラボ シナリオ用に 2 人の追加テスト ユーザーを作成します。

1. **Microsoft 365 管理センター** の **アクティブなユーザー** ページで、上部のツールバーから **ユーザーの追加** を選択します。

2. **基本設定** ページで、次の情報を入力します:
   - **名:** `Lab`
   - **姓:** `User1`
   - **表示名:** `Lab User1`
   - **ユーザー名:** `LabUser1`
   - **ドメイン:** `<TenantPrefix>.onmicrosoft.com` を選択します
   - **パスワード:** **パスワードを自動的に作成する** のチェックを外し、事前に提供された `<UserPassword>` などの強力なパスワード (または任意の安全なパスワード) をパスワード フィールドに入力します
   - **このユーザーに初回サインイン時のパスワード変更を要求する:** このチェック ボックスをオフにします

3. **次へ** を選択します。

4. **製品ライセンスの割り当て** ページで、両方のライセンスをオフのままにして **製品ライセンスなしでユーザーを作成する** を選択し、続いて **次へ** を選択します。

   > [!NOTE]
   > Lab User1 と Lab User2 にはライセンスは必要ありません。これらは、(1) タスク 3 で sg-Intune-Pilot-Users にメンバーを追加すること、および (2) 演習 2 タスク 5 でスコープ設定された Intune 管理者ロールを受け取ることのみを目的として存在し、いずれもライセンスが必要なワークロード (Teams、Exchange、Intune デバイス登録) には触れません。ライセンスを割り当てないことで、実際の容量の問題も回避できます。Contoso ラボ テナントの試用版 SKU は 33 人の既存ユーザーによって完全に消費されており (**Microsoft 365 E5 (no Teams): 20/20 割り当て済み**、**Microsoft Teams Enterprise: 20/20 割り当て済み**)、新しいユーザーに割り当てられる空きシートはありません。

5. **オプション設定** ページで、**プロファイル情報** を展開します。

6. 次の項目を設定します:
   - **役職:** `Test User`
   - **部署:** `IT`

7. **次へ** を選択します。

8. **確認と完了** ページで、設定を確認して **追加の完了** を選択します。

9. 確認ページで **閉じる** を選択します。

10. 手順 1～9 を繰り返して、2 人目のテスト ユーザーを作成します:
    - **名:** `Lab`
    - **姓:** `User2`
    - **表示名:** `Lab User2`
    - **ユーザー名:** `LabUser2`
    - **ドメイン:** `<TenantPrefix>.onmicrosoft.com` を選択します
    - **パスワード:** **パスワードを自動的に作成する** のチェックを外し、事前に提供された `<UserPassword>` などの強力なパスワードを入力します
    - **役職:** `Test User`
    - **部署:** `Engineering`
    - **ライセンス:** 割り当てないままにします (Lab User1 と同じ理由)

**2 人の追加テスト ユーザーが正常に作成されました。**

---

### タスク 3: 割り当て済みセキュリティ グループの作成

Intune のポリシー ターゲット設定用に、割り当て済み (静的メンバーシップ) のセキュリティ グループを作成します。

以前のラボ実行で sg-Intune-Pilot-Users がすでに存在する場合は、作成しようとすると名前の競合エラーが表示されます。その場合は作成手順をスキップし、手順 9 に進んで既存のグループにメンバーを追加してください。

1. **Microsoft 365 管理センター** の左側のナビゲーションで、**チームとグループ** を展開し、**アクティブなチームとグループ** を選択します。

2. **セキュリティ グループ** タブを選択します。

3. **セキュリティ グループの追加** を選択します。

4. **基本設定** ページで、次の情報を入力します:
   - **名前:** `sg-Intune-Pilot-Users`
   - **説明:** `Pilot users for Intune policy testing`

5. **次へ** を選択します。

6. **設定の編集** ページで、既定の設定のままにして **次へ** を選択します。

7. **グループの追加の確認と完了** ページで、**グループの作成** を選択します。

8. 確認ページで **閉じる** を選択します。

9. **セキュリティ グループ** タブで、一覧から **sg-Intune-Pilot-Users** を選択します。

10. グループ詳細ペインで、**メンバー** タブを選択します。

11. **すべてのメンバーの表示と管理** を選択します。

12. **メンバーの追加** を選択します。

13. 次のユーザーを検索して選択します:
    - **Megan Bowen**
    - **Alex Wilber**
    - **Joni Sherman**
    - **Lab User1**
    - **Lab User2**

14. **追加 (5)** を選択します。

15. グループ詳細ペインを閉じます。

**5 人のパイロット ユーザーを含む割り当て済みセキュリティ グループが正常に作成されました。**

---

### タスク 4: 複合ルールを持つ動的ユーザー グループの作成

動的グループは、ユーザー属性に基づいてメンバーシップを自動的に更新します。薬局の臨床ワークロード向けに、2 つの条件を -and で組み合わせた **複合ルール** を使用する動的グループを作成し、米国に所在する薬局部署のユーザーをターゲットにします。複合ルールは、規制対応やリージョン単位でのスコープ設定 (たとえば、Contoso Healthcare は米国の臨床ワークロードに対してより厳格なコンプライアンスを適用する) の標準的なパターンです。

1. ブラウザーで **https://entra.microsoft.com** に移動します。

2. **Microsoft Entra 管理センター** の左側のナビゲーションで **グループ** を選択し、続いて **すべてのグループ** を選択します。

3. 上部のツールバーから **新しいグループ** を選択します。

4. **新しいグループ** ペインで、次の項目を構成します:
   - **グループの種類:** セキュリティ
   - **グループ名:** `dyn-Pharmacy-Users`
   - **グループの説明:** `Dynamic group for Pharmacy department users located in the US`
   - **グループに Microsoft Entra ロールを割り当てることができる:** いいえ
   - **メンバーシップの種類:** 動的ユーザー

5. **動的ユーザー メンバー** の下で、**動的クエリの追加** を選択します。

6. **動的メンバーシップ ルール** ページの **ルールの構成** タブで、ページ下部にある **ルール構文** ボックスを見つけ、その右側にある **編集** を選択します。複合ルールでは、上部のプロパティ/演算子/値ビルダーよりも、構文エディターで直接ルールを記述する方が読みやすくなります。

7. **ルール構文の編集** エディターで、次の複合ルールを正確に入力します:

   ```text
   (user.department -eq "Pharmacy") -and (user.country -eq "US")
   ```

   > [!NOTE]
   > -and 演算子は、ユーザーが含まれるためには **両方** の条件が真である必要があることを意味します。いずれかの条件に一致するユーザーを含めるには -or を使用することもでき、より複雑なロジックのためにかっこでサブ式をグループ化できます。ルール構文エディターは式を検証します。保存する前に、赤い下線をすべて修正してください。MemberOf 演算子に関するプレビュー バナーが表示される場合もありますが、このルールは MemberOf ではなく -eq を使用しているため、閉じてかまいません。

8. **OK** を選択してエディターを閉じ、続いてページ上部の **保存** を選択します。

9. **新しいグループ** ページに戻り、**作成** を選択します。

10. グループが作成されたら、グループの一覧から **dyn-Pharmacy-Users** を選択します。

11. グループの **概要** ページの **フィード** セクションで、**動的ルールの処理状態** カードを見つけ、**成功** と表示されていることを確認します。

    > [!NOTE]
    > 動的グループのメンバーシップ評価には 5～15 分かかることがあります。完了すると、グループには department 属性が Pharmacy で **かつ** country 属性が US に等しいユーザーのみが含まれます。両方の属性に一致する Contoso のサンプル ユーザーが現在いない場合、グループは空になります。これはこのラボ テナントでは想定どおりであり、後続の演習には影響しません。

12. **メンバー** タブを選択して、グループ メンバーを表示します。

**複合メンバーシップ ルールを持つ動的ユーザー グループが正常に作成されました。**

---

### タスク 5: 動的デバイス グループの作成

Intune に登録されているすべての Windows デバイスを自動的に含める動的グループを作成します。

1. **Microsoft Entra 管理センター** の **すべてのグループ** ページで、**新しいグループ** を選択します。

2. **新しいグループ** ページで、次の項目を構成します:
   - **グループの種類:** セキュリティ
   - **グループ名:** `dyn-Windows-Devices`
   - **グループの説明:** `Dynamic group for all Windows devices`
   - **メンバーシップの種類:** 動的デバイス

3. **動的デバイス メンバー** の下で、**動的クエリの追加** を選択します。

4. **動的メンバーシップ ルール** ページで、次のルールを構成します:
   - **プロパティ:** `deviceOSType`
   - **演算子:** Equals
   - **値:** `Windows`

5. **保存** を選択します。

6. **新しいグループ** ページに戻り、**作成** を選択します。

   > [!NOTE]
   > このグループは、デバイスが Intune に登録された後 (演習 5)、自動的に Windows デバイスが設定されます。

**Windows デバイス用の動的デバイス グループが正常に作成されました。**

---

### タスク 6: Windows Autopilot 用の動的デバイス グループの作成

2 つ目の動的デバイス グループを作成します。これは Windows Autopilot 登録用です。SEA-DEV3 を Autopilot に登録して展開プロファイルを割り当てる **演習 6** で使用します。

1. **Microsoft Entra 管理センター** の **すべてのグループ** ページで、**新しいグループ** を選択します。

2. **新しいグループ** ページで、次の項目を構成します:
   - **グループの種類:** セキュリティ
   - **グループ名:** `dyn-Autopilot-Devices`
   - **グループの説明:** `Dynamic group for all Windows Autopilot-registered devices`
   - **メンバーシップの種類:** 動的デバイス

3. **動的デバイス メンバー** の下で、**動的クエリの追加** を選択します。

4. **動的メンバーシップ ルール** ページの **ルールの構成** タブで、ページ下部にある **ルール構文** ボックスを見つけ、その右側の **編集** を選択します。**ルール構文の編集** エディターで、次のルールを正確に入力してから **OK** を選択します:

   ```text
   (device.devicePhysicalIds -any _ -startsWith "[ZTDId]")
   ```

5. **保存** を選択し、続いて **新しいグループ** ページに戻って **作成** を選択します。

   > [!NOTE]
   > [ZTDId] (Zero Touch Deployment ID) は、デバイスのハードウェア ハッシュが Windows Autopilot に登録されるとすぐに、そのデバイスのディレクトリ オブジェクトに設定されます。これは、デバイスが OOBE を通過するよりかなり前の段階です。(すでに登録済みのデバイスのみに一致する) dyn-Windows-Devices とは異なり、このルールでは登録された瞬間にデバイスをターゲットにできます。これはまさに Autopilot 展開プロファイルの割り当てに必要なものです。

**Windows Autopilot 用の動的デバイス グループが正常に作成されました。**

---

## 演習 2: 管理委任の構成

### シナリオ

Intune とデバイス管理のさまざまな側面を管理するチーム メンバーに、管理アクセスを委任する必要があります。アクセス許可を適切にスコープ設定するために、Microsoft Entra ID のロールと管理単位を使用します。

### タスク 1: Intune 管理者ロールの割り当て

1. **Microsoft Entra 管理センター** の左側のナビゲーションで、**Entra ID** の下の **ユーザー** を選択し、続いて **すべてのユーザー** を選択します。

2. ユーザーの一覧から **Allan Deyoung** を検索して選択します。

3. Allan Deyoung のユーザー詳細で、左側のナビゲーションから **割り当てられたロール** を選択します。

   > [!NOTE]
   > 一部の Contoso ラボ テナントでは、Allan Deyoung に **グローバル管理者** ロールが事前に割り当てられています (**アクティブな割り当て** タブに表示されます)。グローバル管理者の上に Intune 管理者ロールを追加することは機能的には冗長です。グローバル管理者はすでにすべての Intune アクセス許可を継承しているためです。それでも、ロール割り当てのワークフローを練習するために手順を実行してください。

4. 上部のツールバーから **割り当ての追加** を選択します。

5. **割り当ての追加** ページの **メンバーシップ** タブで、**Intune 管理者** を検索して選択します。

6. **設定** タブを選択し、次の項目を構成します:
   - **割り当ての種類:** **アクティブ** (**対象** ではありません。**対象** の場合、Allan Deyoung が後で PIM を通じてロールを手動で有効化するまで使用できません。**アクティブ** はアクセス許可を直ちに付与します)
   - **永続的に割り当てられています:** チェックを入れたままにします
   - **割り当ての開始:** 自動入力された現在の日時のままにします
   - **割り当ての終了:** 空欄のままにします (恒久チェック ボックスがオンの間はグレー表示されます)

7. **理由** ボックスに理由を入力します (例: `Lab 01 role delegation exercise — assigning Intune Administrator to the IT admin`)。PIM では、恒久的なものであっても、すべてのアクティブな割り当てに理由が必要です。

8. **割り当て** を選択します。

   > [!NOTE]
   > **メンバーシップ**/**設定** の 2 タブ形式は、Contoso ラボ テナントで Microsoft Entra ID P2 と Privileged Identity Management (PIM) が有効になっているために表示されます。ディレクトリ ロールの割り当ては既定ですべて PIM を経由します。**アクティブ** + **恒久的に割り当て済み** は、従来の常時オンのロール割り当てを再現します。
   >
   > [!NOTE]
   > Intune 管理者ロールは、デバイス構成、コンプライアンス ポリシー、アプリケーション、登録設定など、Microsoft Intune のすべての側面を管理するアクセス許可を付与します。これはグローバル管理者よりも権限の低いロールです。
   >
   > [!NOTE]
   > **このラボでは、PIM のジャストインタイム (対象) モデルではなく、恒久的なアクティブ割り当てを使用します。これは意図的な簡略化であり、推奨される運用パターンではありません。** Microsoft のガイダンス (およびこのコース自体のユニット コンテンツである [デバイス管理のための Microsoft Entra ロールの割り当て](../Learning%20Path%201%20-%20Prepare%20infrastructure%20for%20devices%20using%20Microsoft%20Intune%20and%20Microsoft%20Entra%20ID/configure-entraid-device-management/includes/04-assign-entra-id-roles-device-management.md)) では、機密性の高いロールには **対象** 割り当てを推奨しています。この場合、管理者は必要なときにのみロールを有効化し、有効化は自動的に期限切れになります。ここで **アクティブ** を使用するのは、Allan、Joni、Lab User1 が、このラボ シリーズの残りの部分で毎回追加の有効化手順なしに動作するアクセス許可を持てるようにするためだけです。**このシリーズのどのラボでも、受講者に対象ロールの自己有効化 (アクセス要求 → 理由の入力 → 期間限定の有効化) を実行させることはありません**。そのワークフローは、ユニット コンテンツで概念的にのみ説明されています。最も近い実践的な関連付けは **Lab 06 演習 5** (LP6 ユニット 05) で、これは有効化の実行ではなく PIM 有効化の監査ログを確認するものです。

**Allan Deyoung への Intune 管理者ロールの割り当てが正常に完了しました。**

---

### タスク 2: クラウド デバイス管理者ロールの割り当て

1. **Microsoft Entra 管理センター** の **Entra ID** > **ユーザー** > **すべてのユーザー** で、**Joni Sherman** を検索して選択します。

2. Joni Sherman のユーザー詳細で、**割り当てられたロール** を選択します。

3. **割り当ての追加** を選択します。

4. **割り当ての追加** ページの **メンバーシップ** タブで、**クラウド デバイス管理者** を検索して選択します。

5. **設定** タブを選択し、タスク 1 と同じ方法で構成します:
   - **割り当ての種類:** アクティブ
   - **永続的に割り当てられています:** チェックを入れたままにします
   - **割り当ての開始 / 終了:** 既定値のままにします (開始日時は自動入力、終了日なし)

6. **理由** ボックスに理由を入力します (例: `Lab 01 role delegation exercise — assigning Cloud Device Administrator to help desk staff`)。

7. **割り当て** を選択します。

   > [!NOTE]
   > クラウド デバイス管理者ロールでは、デバイスの有効化、無効化、削除など、Microsoft Entra ID 内のデバイス ID を管理できます。このロールは、完全な Intune アクセスを持たずにデバイス オブジェクトを管理する必要があるヘルプデスク スタッフに役立ちます。

**Joni Sherman へのクラウド デバイス管理者ロールの割り当てが正常に完了しました。**

---

### タスク 3: 管理単位の作成

管理単位を使用すると、管理アクセス許可をユーザーまたはデバイスのサブセットに制限できます。IT 部署用の管理単位を作成します。

1. **Microsoft Entra 管理センター** の左側のナビゲーションで、**Entra ID** の下の **役割と管理者** を選択し、続いて **管理単位** を選択します。

2. 上部のツールバーから **追加** を選択します。

3. **管理単位の追加** ページで、次の情報を入力します:
   - **名前:** `IT Department`
   - **説明:** `Administrative unit for IT department users and devices`

4. **次へ: ロールの割り当て** を選択します。

5. **ロールの割り当て** ページで、**次へ: 確認および作成** を選択します (メンバーを追加した後にロールを割り当てます)。

6. **確認と作成** ページで、**作成** を選択します。

**IT 部署用の管理単位が正常に作成されました。**

---

### タスク 4: 管理単位へのメンバーの追加

1. **管理単位** ページで、一覧から **IT Department** を選択します。

2. **IT Department** 管理単位の詳細で、左側のナビゲーションから **ユーザー** を選択します。

3. 上部のツールバーから **メンバーの追加** を選択します。

4. **Allan Deyoung** (IT 管理者) を検索して選択します。

5. **選択** を選択します。

6. **IT Department** 管理単位の詳細で、左側のナビゲーションから **グループ** を選択します。

7. 上部のツールバーから **追加** を選択します。

8. 既存の **sg-IT** セキュリティ グループ (IT 部署ユーザー用の既存グループ) を検索して選択します。

9. **選択** を選択します。

   > [!NOTE]
   > ユーザーとグループを管理単位に追加することで、これらのオブジェクトのみを管理するように管理ロールをスコープ設定できます。これは、リージョン単位または部署単位の管理を委任する場合に役立ちます。

**IT Department 管理単位へのメンバーの追加が正常に完了しました。**

---

### タスク 5: 管理単位へのスコープ設定されたロールの割り当て

IT Department 管理単位のみにスコープ設定されたヘルプデスク管理者ロールを割り当てます。

**Intune 管理者は、管理単位のスコープを設定して割り当てることはできません。** 管理単位のスコープ設定をサポートする Microsoft Entra ロールは固定のセットのみです。認証管理者、属性割り当て管理者/閲覧者、クラウド デバイス管理者、グループ管理者、**ヘルプデスク管理者**、ライセンス管理者、パスワード管理者、プリンター管理者、特権認証管理者、SharePoint 管理者、Teams 管理者、Teams デバイス管理者、ユーザー管理者、およびすべてのカスタム ロールが対象で、Intune 管理者はその 1 つではありません。これはまさに、Intune が独自の個別のスコープ タグ システム (タスク 6) を持つ理由です。これが、Intune 固有の管理をデバイス/ポリシーのサブセットに委任するためのサポートされた方法です。ヘルプデスク管理者は AU スコープ設定された委任に関する Microsoft 自身の標準的な例であるため、ここでは Entra レイヤーのスコープ設定の仕組みを示すために使用します。

1. **IT Department** 管理単位の詳細で、左側のナビゲーションから **ロールと管理者** を選択します。

2. ロールの一覧で、名前をクリックして **ヘルプデスク管理者** を検索して選択します (このページには管理単位のスコープをサポートするロールのみが一覧表示されます。任意のロールを検索できるツールバーの **追加** ボタンはなく、表示された一覧から選択します)。

3. ロールの割り当てページで、**割り当ての追加** を選択します。

4. **割り当ての追加** ページの **メンバーシップ** タブで、**メンバーの選択** を選択し、**Lab User1** (演習 1 で作成) を検索して選択します。その後、画面下の **選択** を選択します。

5. **設定** タブを選択して構成します:
   - **割り当ての種類:** アクティブ
   - **永続的に割り当てられています:** チェックを入れたままにします
   - **割り当ての開始 / 終了:** 既定値のままにします

6. **理由** ボックスに理由を入力します (例: `Lab 01 role delegation exercise — scoped Helpdesk Administrator for IT Department AU`)。

7. **割り当て** を選択します。

   > [!NOTE]
   > Lab User1 はヘルプデスク管理者のアクセス許可を持つようになりましたが、IT Department 管理単位内のユーザーとデバイスに対してのみ有効です。これは、Microsoft Entra レイヤーでのロールベースのアクセス制御 (RBAC) スコープ設定を示しています。

**スコープ設定されたヘルプデスク管理者ロールの割り当てが正常に完了しました。**

---

### タスク 6: 薬局の臨床ワークロード用のカスタム Intune ロールとスコープ タグの作成

Microsoft Entra ID ロール (タスク 1～5) は、Entra レベルのアクセス許可を委任します。Intune 自体には、独自のカスタム ロールと **スコープ タグ** を持つ **個別の RBAC システム** があります。Contoso Healthcare は、薬局のヘルプデスクがテナント全体ではなく薬局の臨床デバイスのみを参照および操作できるようにしたいと考えています。そのため、今、薬局スコープ タグと薬局ヘルプデスク カスタム Intune ロールを作成します。**Labs 2～4** では、薬局スコープ タグを特定の構成、コンプライアンス、アプリ、セキュリティのポリシーに適用します。**Lab 05 演習 3** では、薬局ヘルプデスク ロールを委任された管理者 (Lee Gu) に割り当て、薬局スコープのオブジェクトのみが表示されることをエンドツーエンドで確認します。

**パート A — 薬局スコープ タグの作成**

1. ブラウザーで **https://intune.microsoft.com** (Microsoft Intune 管理センター) に移動します。

2. 左側のナビゲーションで、**テナント管理** を選択し、続いて **ロール** を選択します。

3. **ロール** ページで、**スコープタグ** を選択します。

4. **+ 作成** を選択します。

5. **基本** タブで、次を入力します:
   - **名前:** `Pharmacy`
   - **説明:** `Pharmacy clinical devices and policies (Contoso Healthcare)`

6. **次へ** を選択します。

7. **割り当て** タブで、**グループ** は今のところ空のままにします。**Lab 02 演習 1** から、(グループではなく) 特定のポリシーにタグを付けます。**次へ** を選択します。

8. **確認と作成** タブで、**作成** を選択します。

**パート B — 薬局ヘルプデスク カスタム Intune ロールの作成**

1. **テナント管理** で、**ロール** を選択し、続いて **すべてのロール** を選択します。

2. **+ 作成** → **Intune の役割** を選択します。

3. **基本** ページで、次の情報を入力します:
   - **名前:** `Pharmacy Helpdesk`
   - **説明:** `Delegated helpdesk role scoped to Pharmacy clinical devices. Read + remote actions on devices; no policy authoring.`

4. **次へ** を選択します。

5. **アクセス許可** ページで、次のアクセス許可に対して **はい** を選択します (その他はすべて **いいえ** のままにします。これは最小特権の原則です)。ポータルのラベルは、**管理対象デバイス**、**リモート タスク**、**組織**、**ロール** などのカテゴリにアクセス許可をグループ化します。ポータルで最も近いラベルに合わせてください:
   
   - **Managed devices:** Read、Set primary user、Update
   - **Remote tasks:** Sync devices、Reboot now、Collect diagnostics
   - **Organization:** Read
   - **Roles:** Read
   - **Remote Help app:** Take full control、View screen
   
   > [!IMPORTANT]
   > **アプリ**、**デバイス コンプライアンス ポリシー**、**デバイス構成**、**エンドポイント保護**、**登録プログラム**、**ポリシー セット** のすべてのアクセス許可を **いいえ** に設定したままにします。薬局ヘルプデスクはデバイスを操作できる必要がありますが、いかなるポリシーの作成または変更も **できない** ようにする必要があります。これは、幅広い読み取りの上に狭いリモート操作ロールを重ねる、中上級の委任パターンです。
   
6. **次へ** を選択します。

7. **スコープ タグ** ページで、**+ スコープ タグの選択** を選択し、パート A で作成した **Pharmacy** スコープ タグを追加します。**選択** を選択します。

8. **既定** スコープ タグのチップを削除して (省略記号 **...** → **削除** を選択)、**Pharmacy** のみが選択された状態にします。

   > [!NOTE]
   > この手順では、割り当てられた管理者が管理できる対象ではなく、**ロール定義自体** をスコープ設定します。これらは 2 つの異なるものです。Microsoft によると: _「ロールに追加されたスコープ タグは、ロール自体の可視性を制御します。ロール割り当てに追加されたスコープ タグは、ポリシー、アプリ、デバイスなどの Intune オブジェクトの可視性を、そのロール割り当ての管理者のみに制限します」_。ここで **既定** を削除すると、薬局ヘルプデスク ロール定義は、すでに Pharmacy スコープ タグを持つ管理者にのみ表示されるようになります (グローバル/Intune 管理者は引き続きすべてを参照できます。スコープ タグは Entra ロールには適用されません)。Lee Gu が日常的に管理できる対象を実際に制限する手順は、**Lab 05 演習 3** で、このロールを Pharmacy を割り当てのスコープとして割り当てるときに行われます。

9. **次へ** を選択します。

10. **レビューと作成** ページで、**作成** を選択します。

作成したばかりの薬局スコープ タグと薬局ヘルプデスク ロールは、このラボ シリーズの残り全体にわたる委任管理の基盤となります。**Labs 02～04** では、薬局スコープ タグを構成プロファイル、コンプライアンス ポリシー、LOB アプリ、セキュリティ ベースラインに適用します。**Lab 05 演習 3** では、薬局ヘルプデスク ロールを委任された管理者 (Lee Gu) に割り当て、薬局スコープのオブジェクトのみが表示されることを確認します。**Lab 06 演習 2** では、薬局ヘルプデスク管理者が薬局デバイスで Remote Help を使用します。

**薬局スコープ タグと薬局ヘルプデスク カスタム Intune ロールの作成が正常に完了しました。**

---

## 演習 3: デバイスの登録と設定の構成

### シナリオ

デバイスを Intune に登録する前に、デバイスを登録できるユーザー、デバイスの制限、追加のローカル管理者アカウントなど、Microsoft Entra ID でデバイス登録設定を構成する必要があります。また、ローカル管理者パスワードの管理のために Microsoft Entra LAPS を有効にします。

### タスク 1: デバイス参加設定の構成

1. **Microsoft Entra 管理センター** の左側のナビゲーションで、**Entra ID** の下の **デバイス** を選択し、続いて **概要** を選択します。

2. 左側のナビゲーションから **デバイスの設定** を選択します。

3. **デバイスの設定** ページの **Microsoft Entra 参加と登録の設定** で、次の項目を構成します:
   - **ユーザーはデバイスを Microsoft Entra に参加させることができます:** **すべて** を選択します _(オプション: すべて / 選択済み / なし)_
   - **ユーザーはデバイスを Microsoft Entra に登録できます:** すでに **すべて** と表示されており、コントロールは **グレー表示/操作不可** になっているはずです。これは想定どおりであり、バグではありません
   - **デバイスを Microsoft Entra に登録または参加させるために多要素認証を要求する:** **いいえ** を選択します
   - **ユーザーあたりのデバイスの最大数:** `50`

   > [!NOTE]
   > **「ユーザーはデバイスを登録できます」が すべて でグレー表示されている** のは、バグではなく想定どおりです。このテナントでは Intune/MDM の自動登録がすでに有効になっており、MDM 登録には登録が必要なため、Entra はトグルをロックします。ここで構成するものはありません。
   >
   > このトグルではなく条件付きアクセスを使用して MFA を要求することを推奨する、黄色の推奨バナーが表示されます。このラボでは、MFA トグルを **いいえ** に設定したままにします。条件付きアクセスの適用は Lab 04 で扱います。運用環境では、デバイス登録を特定のグループに制限し、MFA を要求します。ラボの目的上、登録を簡素化するために、すべてのユーザーが MFA なしでデバイスを登録できるようにしています。

4. 変更を行った場合は、ページ上部の **保存** を選択します。

**デバイス参加設定の構成が正常に完了しました。**

---

### タスク 2: Microsoft Entra 参加済みデバイスへの追加のローカル管理者の構成

既定では、Microsoft Entra 参加を実行したユーザーがデバイスのローカル管理者になります。追加のユーザーまたはグループをローカル Administrators グループに追加できます。

1. **デバイスの設定** ページで、**ローカル管理者設定** セクションまでスクロールします。

   > [!NOTE]
   > ここには 2 つのプレビュー トグルが表示されます。**グローバル管理者ロールが Microsoft Entra 参加中にデバイスのローカル管理者として追加される (プレビュー)** と **登録するユーザーが Microsoft Entra 参加中にデバイスのローカル管理者として追加される (プレビュー)** です。このラボでは、両方とも既定値のままにします。

2. **管理 Microsoft Entra に参加済みのすべてのデバイスの追加のローカル管理者** リンクを選択します。

3. **デバイス管理者 | 割り当て** ページで、**割り当ての追加** を選択します。

4. **Allan Deyoung** を検索して選択します。

5. **追加** を選択します。

   > [!NOTE]
   > Allan Deyoung が、任意の Microsoft Entra 参加済みデバイスのローカル Administrators グループに追加されました。これは、管理対象デバイスでローカル管理者権限が必要なヘルプデスク スタッフに役立ちます。個々のユーザーではなくグループを介してこれを委任するには、ロール割り当て可能なグループ (**このグループに Microsoft Entra ロールを割り当てることができる: はい** を作成時に設定) が必要です。

**Microsoft Entra 参加済みデバイスの追加のローカル管理者の構成が正常に完了しました。**

---

### タスク 3: Microsoft Entra Local Administrator Password Solution (LAPS) の有効化

Microsoft Entra LAPS は、Microsoft Entra 参加済みデバイスのローカル管理者パスワードを自動的に管理およびローテーションします。

詳細な LAPS ポリシー構成 (パスワードの複雑さ、長さ、有効期間、管理対象アカウント名) は、Entra 管理センターから Microsoft Intune のエンドポイント セキュリティ ポリシーに移動しました。Entra の設定は、テナントに対して LAPS 機能を有効にする単一のオン/オフ トグルになり、パスワード ポリシー自体は Intune で構成します。

**パート A — テナント レベルで LAPS を有効化する (Entra 管理センター):**

1. **Microsoft Entra 管理センター** の左側のナビゲーションで、**Entra ID** の下の **デバイス** を選択し、続いて **デバイスの設定** を選択します。

2. **ローカル管理者設定** セクションまでスクロールします。

3. **Microsoft Entra Local Administrator Password Solution (LAPS) を有効にする** を **はい** に設定します。

4. ページ上部の **保存** を選択します。

**パート B — LAPS パスワード ポリシーを構成する (Intune 管理センター):**

1. ブラウザーで **https://intune.microsoft.com** に移動します。

2. **Microsoft Intune 管理センター** の左側のナビゲーションで **エンドポイント セキュリティ** を選択し、続いて **アカウント保護** を選択します。

3. **+ ポリシーの作成** を選択します。

4. **プロファイルの作成** ペインで、次の項目を構成します:
   - **プラットフォーム:** Windows
   - **プロファイル:** ローカル管理者パスワード ソリューション (Windows LAPS)

5. **作成** を選択します。

6. **Basics** タブで、次を入力します:
   
   - **名前:** `Contoso LAPS Policy`
   - **説明:** `Manages and rotates the local Administrator password on Microsoft Entra joined devices`
   
7. **次へ** を選択します。

8. **Configuration settings** タブで、次の項目を構成します:
   
   - **バックアップ ディレクトリ:** パスワードをMicrosoft Entra ID のみにバックアップする
   - **パスワードの有効期間 (日数):** `30`
   - **管理者アカウント名:** **構成されていません** のままにします (組み込みの `Administrator` を使用)
   - **パスワードの複雑さ:** 大文字 + 小文字 + 数字 + 特殊文字 (既定)
   - **パスワードの長さ:**  (「構成されていません」のトグルをスライドさせて) `14`
   - **認証後のアクション:** パスワードをリセットし、マネージドアカウントをログオフします。猶予期間がすぎると、～
   - **認証後のリセットの遅延:** (「構成されていません」のトグルをスライドさせて) `24`
   - **自動アカウント管理が有効です:** ターゲットアカウントは自動的に管理されません (既定)
   
   > [!NOTE]
   > **自動アカウント管理** (Windows 11 24H2 以降のみ) を使用すると、LAPS がローカル管理者アカウント自体を作成/有効化できます。ここでは **対象アカウントは自動的に管理されません (既定)** のままにします。ラボの仮想マシンは 24H2 であることが保証されておらず、また既存の組み込みの Administrator アカウントをすでに使用しているため、必要ありません。**認証後のアクション** のオプションは、_パスワードをリセットする_ / _パスワードをリセットして管理対象アカウントをログオフする_ (既定であり、ここで使用するもの) / _パスワードをリセットして再起動する_ です。
   
9. **次へ** を選択します。

10. **Scope tags** タブで、**次へ** を選択します。

11. **Assignments** タブの **グループ** で、**すべてのデバイス** を選択します。

12. **次へ** を選択します。

13. **Review + create** タブで、設定を確認して **作成** を選択します。

    > [!NOTE]
    > Microsoft Entra LAPS は、ローカル管理者パスワードを 30 日ごとに自動的にローテーションし、パスワードを Microsoft Entra ID 内に安全に保存します。承認された管理者は、Entra 管理センターの **デバイス** > **ローカル管理者パスワードの復旧** からパスワードを取得できます。

**Microsoft Entra LAPS の有効化と Intune でのパスワード ポリシーの構成が正常に完了しました。**

---

## 演習 4: Windows 登録ポリシーの構成

### シナリオ

演習 5 では、同僚が **SEA-DEV1** と **SEA-DEV2** にサインインして Microsoft Entra 参加を実行し、演習 6 では **SEA-DEV3** を Windows Autopilot に登録します。それらが行われる前に、初回実行エクスペリエンスが適切になるようにテナントが構成されていることを確認する必要があります。つまり、デバイスが Intune に自動的に登録され、重要なアプリとポリシーが適用されるまでユーザーが作業を開始できず、各ユーザーが登録できるデバイス数にガードレールが設けられている状態です。

この演習では、次のことを行います:
- 新しいテナントに対して Intune への自動登録が正しく構成されていることを確認する
- アプリとポリシーが適用されるまでデバイスをブロックするように **登録状態ページ (ESP)** を構成する。これは、Autopilot の展開を洗練されたものに感じさせるのと同じゲートです
- パイロット グループ用に、ターゲットを絞ったより厳格な ESP プロファイルを作成する
- 既定のプラットフォーム制限ポリシーを確認し、デバイス数の制限ポリシーを作成する

**自動 MDM 登録がもはや焦点ではない理由。** 最新のクラウドのみの Microsoft 365 テナントでは、**Intune への自動登録は全ユーザー スコープに対して既定で有効になっています**。従来の「MDM ユーザー スコープの構成」手順は、オンプレミス同期されたどのユーザーを自動登録するかをスコープ設定する必要がある **ハイブリッド ID** および **Configuration Manager 共同管理** のシナリオで最も関連性があります。タスク 1 でこの設定を確認してから、ユーザーの初回実行エクスペリエンスを実際に形作るポリシーに進みます。

### タスク 1: 自動 MDM 登録の確認

1. ブラウザーで **https://intune.microsoft.com** に移動します。

2. **Microsoft Intune 管理センター** の左側のナビゲーションで **デバイス** を選択します。

   > [!NOTE]
   > 1 回限りの **「デバイスが変更されました」** ツアー バナーが表示される場合があります。**スキップ** を選択して閉じます。

3. **デバイスのオンボード** の下で、**登録** を選択します。

4. **Windows** タブの **登録オプション** で、**自動登録** を選択します。

5. **MDM ユーザー スコープ** が **すべて** に変更します。

7. **MDM 使用条件 URL**、**MDM 検出 URL**、**MDM コンプライアンス URL** は、自動入力された既定値のままにします。

8. 変更を行った場合は **保存** を選択します。それ以外の場合は、**自動登録** ペインを閉じます。

**テナントに対して自動 MDM 登録が構成されていることを確認しました。**

---

### タスク 2: 既定の登録状態ページの構成

**登録状態ページ (ESP)** は、Windows 登録 (Microsoft Entra 参加、Autopilot、またはデバイス登録) 中にユーザーに表示されます。構成されたアプリとポリシーが適用されるまでデバイスの使用をブロックするため、ユーザーが半分しかプロビジョニングされていないデバイスにサインインすることを防ぎます。**既定** の ESP プロファイルはすべてのユーザーとすべてのデバイスを対象とし、無効の状態で提供されます。Contoso のベースラインを設定するために、これを有効にします。

1. **Microsoft Intune 管理センター** の **登録** ページで、**Windows** タブにいることを確認します。**Windows Autopilot** の下で、**登録状態ページ** を選択します。

2. **登録状態ページ** の一覧で、**すべてのユーザーおよびすべてのデバイス** リンク (**既定** の優先度で割り当て済み) を選択します。

3. **すべてのユーザーおよびすべてのデバイス** ページで、左側のナビゲーションから **管理 > プロパティ** を選択し、続いて **設定** の横にある **編集** を選択します。

4. 次の設定を構成します:
   - **アプリとプロファイルの構成の進行状況を表示します:** はい
   - **インストールに要する時間が指定された分数を超えたらエラーを表示します:** `60`
   - **時間制限またはエラーの発生時にカスタム メッセージを表示する:** はい
   - **カスタム メッセージ:** `Contoso device setup is taking longer than expected. Contact the Service Desk at x4040 if this persists.`
   - **エンド ユーザー向けにログ収集と診断ページを有効にする:** はい
   - **out-of-box experience (OOBE) でプロビジョニングされたデバイスにのみページを表示する:** いいえ
   - **すべてのアプリとプロファイルがインストールされるまでデバイスの使用をブロックする:** いいえ

   > [!NOTE]
   > 既定のプロファイルで **すべてのアプリとプロファイルがインストールされるまでデバイスの使用をブロックする** を **いいえ** に設定すると、ブロックしないポリシーがバックグラウンドで完了する間、標準ユーザーはすばやくサインインできます。タスク 3 では、パイロット グループ用により厳格なブロック型 ESP を作成します。

5. **確認して保存** を選択し、続いて **保存** を選択します。

**既定の登録状態ページの構成が正常に完了しました。**

---

### タスク 3: パイロット グループ用のブロック型 ESP プロファイルの作成

Contoso Healthcare のパイロット ユーザーは、臨床ワークフロー用に事前準備された社用ノート PC を受け取ります。必要なアプリがインストールされるまでデバイスの使用をブロックするより厳格な ESP プロファイルを作成し、既定より優先されるように sg-Intune-Pilot-Users に割り当てます。

1. **Microsoft Intune 管理センター** の左側のナビゲーションで **デバイス** を選択します。 **デバイスのオンボード** の下で、**登録** を選択します。**Windows Autopilot** の下で、**登録状態ページ** を選択します。

2. **登録状態ページ** の一覧で、**+ 作成** を選択します。

3. **基本** タブで、次を入力します:
   - **名前:** `ESP - Pilot - Blocking`
   - **説明:** `Blocks pilot devices from use until clinical apps and security baseline are installed`

4. **次へ** を選択します。

5. **設定** ページで、次を構成します:
   - **アプリとプロファイルの構成の進行状況を表示します:** はい
   - **インストールに要する時間が指定された分数を超えたらエラーを表示します:** `60`
   - **時間制限またはエラーの発生時にカスタム メッセージを表示する:** はい
   - **カスタム メッセージ:** `Contoso pilot device setup is in progress. Contact the Service Desk at x4040 if this persists.`
   - **エンド ユーザー向けにログ収集と診断ページを有効にする:** はい
   - **out-of-box experience (OOBE) でプロビジョニングされたデバイスにのみページを表示する:** いいえ
   - **すべてのアプリとプロファイルがインストールされるまでデバイスの使用をブロックする:** はい
   - **インストール エラーが発生した場合にデバイスのリセットをユーザーに許可する:** はい
   - **インストール エラーが発生した場合にデバイスの使用をユーザーに許可する:** いいえ
   - **これらの必要なアプリがユーザーまたはデバイスに割り当てられている場合、それらがインストールされるまでデバイスの使用をブロックします:** すべて

6. **次へ** を選択します。

7. **割り当て** ページの **包含されたグループ** で、**グループの追加** を選択します。

8. **sg-Intune-Pilot-Users** を検索して選択し、続いて **選択** を選択します。

9. **次へ** を選択し、続いてもう一度 **次へ** を選択して **スコープ タグ** をスキップします。

10. **確認と作成** タブで、**作成** を選択します。

11. **登録状態ページ** の一覧に戻り、ESP - Pilot - Blocking が **優先度 1** (**既定** より上) で表示されていることを確認します。ユーザー/デバイスが最初に一致したプロファイルが適用されます。

    > [!NOTE]
    > ESP プロファイルは優先度によって評価されます。ESP - Pilot - Blocking は sg-Intune-Pilot-Users に割り当てられ、より高い優先度に位置しているため、パイロット ユーザーはブロック型のエクスペリエンスを受け取り、それ以外のユーザーは **既定** にフォールスルーします。

**パイロット ユーザー向けにターゲットを絞った登録状態ページ プロファイルが正常に作成されました。**

---

### タスク 4: 既定の登録制限の確認

登録制限は、どのデバイス プラットフォームが Intune に登録できるかを制御します。既定値を確認することで、演習 5 で SEA-DEV1 と SEA-DEV2 が登録する前に、Contoso テナントが何を受け入れるかを理解できます。

1. **Microsoft Intune 管理センター** の **登録** ページで、**Windows** タブにいることを確認します。**登録オプション** の下で、**デバイス プラットフォームの制限** を選択します。

2. **登録制限** ページの **デバイスの種類の制限** の下で、**すべてのユーザー** リンク (**既定** の優先度) を選択します。

3. 左側のナビゲーションで **管理 > プロパティ** を選択します。**プラットフォーム設定** の横にある **編集** を選択します。

4. **既定** の制限ポリシーで、現在の **プラットフォーム設定** を確認します。どのデバイス プラットフォーム (例: Windows、Android、iOS/iPadOS、macOS) が許可されているか、および個人所有、バージョン、デバイス製造元の設定など、各プラットフォームに構成された登録制限を確認します。

   > [!NOTE]
   > 既定のポリシーは、すべてのプラットフォームと個人所有デバイスを許可します。運用環境では、個人所有の Windows デバイスをブロックしたり、特定の OS バージョンを制限したりする場合がありますが、ラボでは、演習 5 で SEA-DEV1 と SEA-DEV2 が登録できるように既定値のままにします。

5. 変更を行わずにポリシー詳細ペインを閉じます。

**既定の登録制限の確認が正常に完了しました。**

---

### タスク 5: デバイス数の制限ポリシーの作成

各ユーザーが登録できるデバイス数を制限するポリシーを作成します。これにより、ライセンスの乱発や資格情報の盗難による悪用から Contoso を保護します。

1. **Microsoft Intune 管理センター** の **登録** ページ (**デバイス** > **デバイスのオンボード** > **登録**) で、**Windows** タブにいることを確認します。**登録オプション** の下で、**デバイスの上限数の制限** を選択します。

2. **制限を作成** を選択します。

3. **制限を作成** ページで、次を入力して **次へ** を選択します:
   
   - **名前:** `Device Limit - 10 Devices`
   - **説明:** `Limit users to 10 enrolled devices`
   
4. **デバイスの制限** の下で、`10` を入力して **次へ** を選択します。

5. **次へ** を選択して **スコープ タグ** をスキップします。

6. **割り当て** の下で、**グループの追加** を選択します。

7. **sg-Intune-Pilot-Users** を検索して選択します。

8. **選択** を選択します。

9. **次へ** を選択します。

10. **レビューと作成** の下で、**作成** を選択します。

    > [!NOTE]
    > このポリシーは、すべてのユーザーを 10 台の登録済みデバイスに制限します。ユーザーが制限に達すると、新しいデバイスを登録する前に既存のデバイスの登録を解除する必要があります。

**デバイス数の制限ポリシーの作成と割り当てが正常に完了しました。**

---

### タスク 6: 個人所有の Android デバイスのブロック

Contoso Healthcare は、個人所有の Android スマートフォンが Intune に登録されることを望んでいません。社用の Android Enterprise デバイス (Samsung Knox / 会社支給) のみが許可されます。これは主に、Contoso の臨床データ取り扱い規則により、ネットワークに接続するすべてのデバイスに会社所有が求められるためです。個人所有の Android の登録をブロックしつつ、社用の Android Enterprise を許可したままにする **デバイス プラットフォームの制限** を作成します。

1. **Microsoft Intune 管理センター** の **登録** ページ (**デバイス** > **デバイスのオンボード** > **登録**) で、**Android** タブを選択します。

   > [!NOTE]
   > プラットフォーム制限はプラットフォームごとに構成します。**既定** の Android プラットフォーム制限は、すべての Android サブタイプ (個人用の仕事用プロファイル、会社所有の仕事用プロファイル、フル マネージド、専用) を許可します。個人所有のサブタイプをブロックする、より優先度の高いカスタム制限を作成します。

2. **登録オプション** の下で、**デバイス プラットフォームの制限** を選択します。

3. **Android の制限** → **+ 制限を作成** を選択します。

4. **基本情報** タブで、次を入力します:
   
   - **名前:** `Android - Block personal`
   - **説明:** `Block personally owned Android enrollment; allow corporate-owned Android Enterprise only`
   
5. **次へ** を選択します。

6. **プラットフォーム設定** タブには、2 つの行 (**Android Enterprise (仕事用プロファイル)** と **Android デバイス管理者**) を持つ表が表示されます。各行には独自の **プラットフォーム** (許可/ブロック) と **個人所有** (許可/ブロック) のトグルに加えて、オプションのバージョン範囲とデバイス製造元のフィルターがあります。「会社所有」用の別の行やトグルはなく、所有権は行ごとに **個人所有** で設定します。**プラットフォーム** を **許可** に設定したまま **個人所有** を **ブロック** に設定すると、その行は会社所有の場合にその種類を引き続き許可します。次のように構成します:
   
   - **Android Enterprise (仕事用プロファイル) → プラットフォーム:** 許可
   - **Android Enterprise (仕事用プロファイル) → 個人所有:** ブロック
   - **Android デバイス管理者 → プラットフォーム:** ブロック (レガシの DA 登録はサポート終了です。Intune が GMS デバイスでの Android デバイス管理者管理のサポートを 2024 年 12 月 31 日に終了したことを示すバナーも表示されます)
   - **Android デバイス管理者 → 個人所有:** ブロック (行がすでにブロックされているためグレー表示されます)
   
   両方の行でバージョン範囲とデバイス製造元は空白のままにします。
   
   > [!NOTE]
   > 正味の効果: 個人所有の Android Enterprise 仕事用プロファイル (BYOD) デバイスはブロックされ、会社所有の Android Enterprise 仕事用プロファイル デバイスは許可されます (プラットフォーム = 許可 は、個人所有のサブセットのみがブロックされるためこれらを対象とします)。Android デバイス管理者は、所有権に関係なく完全にブロックされます。
   
7. **次へ** を選択します。

8. **スコープ タグ** タブで、**既定** のスコープ タグのままにします (この制限は薬局スコープではなくテナント全体です)。**次へ** を選択します。

9. **割り当て** タブの **包含されたグループ** で、**グループの追加** を選択し、**sg-Intune-Pilot-Users** を検索して選択し、画面下の **選択** を選択した後、 **次へ** を選択します。

10. **レビューと作成** タブで、**作成** を選択します。

11. **登録の制限** ページで、Android - Block personal が優先度 **1** (**既定** より上) で一覧に表示されていることを確認します。優先度の高い制限が最初に評価されます。

    > [!NOTE]
    > 運用環境では、通常、管理しないすべてのプラットフォームに対してこれを行います (個人用 iOS のブロック、Linux のブロックなど)。Contoso の薬局臨床ワークロードは、Contoso が所有していないデバイスでは暗号化、脱獄検出、アプリ保護ベースラインを適用できないため、ネットワーク上の個人用デバイスを明示的に禁止しています。

**個人所有の Android デバイスの登録のブロックが正常に完了しました。**

---

## 演習 5: Windows デバイスの登録

### シナリオ

ここでは、Microsoft Entra 参加を実行して、2 台の Windows 11 デバイス (SEA-DEV1 と SEA-DEV2) を Intune に登録します。これは、従業員が自分のデバイスを社用テナントに参加させる、ユーザー主導の登録シナリオをシミュレートします。

### タスク 1: SEA-DEV1 での Microsoft Entra 参加と登録の実行

1. **SEA-DEV1** で、サインインしている場合は現在のセッションからサインアウトします。

2. Windows のサインイン画面で、左下隅のアカウント一覧から **Admin** を選択します。(**Admin** が一覧にない場合は、**Other user** を選択してユーザー名 `Admin` を入力します。)

3. ローカル管理者アカウントでサインインします:
   - **Username:** `Admin`
   - **Password:** (ラボ環境で提供されます)

4. サインインした後、(WindowsのStart から) **Settings** を開きます。

5. **Accounts** → **Access work or school** に移動します。

6. **Connect** を選択します。

7. **Set up a work or school account** ダイアログで、**Join this device to Microsoft Entra ID** を選択します。

8. **Sign in** ダイアログで、次を入力して **Next** を選択します:
   - **Email address:** `MeganB@<TenantPrefix>.OnMicrosoft.com`

9. **Enter password** ダイアログで、Megan Bowen のパスワードを入力して **Sign in** を選択します。

10. **Make sure this is your organization** ダイアログで、テナントが `<TenantPrefix>.onmicrosoft.com` であることを確認して **Join** を選択します。

11. **You're all set!** ページで、**Done** を選択します。

    > [!NOTE]
    > これでデバイスは Microsoft Entra に参加し、自動的に Intune に登録されました。Megan には、アプリとポリシーが適用される間、演習 4 で構成した登録状態ページが表示されます。Megan は sg-Intune-Pilot-Users グループに属していないため、より厳格な **ESP - Pilot - Blocking** プロファイルではなく、ブロックしない **既定** プロファイルが適用されます。

**SEA-DEV1 の Microsoft Entra と Intune への登録が正常に完了しました。**

---

### タスク 2: Intune 管理センターでの SEA-DEV1 登録の確認

1. **Microsoft Edge** で **https://intune.microsoft.com** に移動します。

2. **admin@\<TenantPrefix\>.onmicrosoft.com** としてサインインします (まだサインインしていない場合)。

3. **Microsoft Intune 管理センター** で、**デバイス** を選択し、続いて **すべてのデバイス** を選択します。

4. **SEA-DEV1** がデバイスの一覧に次の状態で表示されていることを確認します:
   - **管理者:** Intune
   - **所有権:** 企業
   - **準拠状況:** 準拠 (最初は「未評価」と表示される場合があります)

5. 一覧から **SEA-DEV1** を選択して、デバイスの詳細を表示します。

6. 次のメニューを確認します:
   - **概要:** デバイス名、オペレーティング システム、準拠状況、最終チェックイン時刻。
   - **ハードウェア:** シリアル番号、TPM バージョン、合計ストレージ容量
   - **検出されたアプリ:** (アプリ インベントリが同期されるにつれて、時間の経過とともに設定されます)

7. 後続のラボで委任された **薬局ヘルプデスク** 管理者がこのデバイスを参照および操作できるように、このデバイスに薬局の臨床デバイスとしてタグを付けます。**SEA-DEV1** のデバイス ページで、**管理** の下の **プロパティ** を選択します。

8. **スコープ タグ** の横にある **開く** を選択して、**タグの選択** ペインを開きます。

9. **タグの選択** ペインで、**Pharmacy** (**演習 2 タスク 6** で作成したスコープ タグ) を選択し、続いて **選択** を選択します。

10. **保存** を選択します。

**Intune での SEA-DEV1 登録の確認が正常に完了しました。**

---

### タスク 3: SEA-DEV2 での Microsoft Entra 参加と登録の実行

1. **SEA-DEV2** に切り替えます。

2. ローカル管理者アカウントでサインインします:
   - **Username:** `Admin`
   - **Password:** (ラボ環境で提供されます)

3. スタートメニューから **Settings** を開きます。

4. **Accounts** → **Access work or school** に移動します。

5. **Connect** を選択します。

6. **Set up a work or school account** ダイアログで、**Join this device to Microsoft Entra ID** を選択します。

7. **Sign in** ダイアログで、次を入力します:
   - **Email address:** `JoniS@<TenantPrefix>.OnMicrosoft.com`

   続いて **Next** を選択します。

8. **Enter password** ダイアログで、Joni Sherman のパスワードを入力して **Sign in** を選択します。

9. **Make sure this is your organization** ダイアログで、**Join** を選択します。

10. **You're all set!** ダイアログで、**Done** を選択します。

11. **SEA-DEV2** を再起動します。

12. 再起動後、(Windowsサインイン画面で左下の Other user を選択して)次のアカウントでサインインします:
    - **User:** `JoniS@<TenantPrefix>.OnMicrosoft.com`
    - **Password:** (Joni Sherman のパスワード)

**SEA-DEV2 の Microsoft Entra と Intune への登録が正常に完了しました。**

---

### タスク 4: 両方のデバイスが登録されていることの確認

1. **Microsoft Intune 管理センター** で、**デバイス** → **すべてのデバイス** に移動します。

2. **SEA-DEV1** と **SEA-DEV2** の両方がデバイスの一覧に表示されていることを確認します。

3. **dyn-Windows-Devices** 動的グループに両方のデバイスが含まれるようになったことを確認します:
   - **Microsoft Intune 管理センター** で、**グループ** → **すべてのグループ** に移動します。
   - **dyn-Windows-Devices** を選択します。
   - **メンバー** タブを選択します。
   - **SEA-DEV1** と **SEA-DEV2** が一覧に表示されていることを確認します (動的グループのメンバーシップが更新されるまで 5～10 分かかる場合があります)。

**両方のデバイスが登録され、動的デバイス グループに自動的に追加されたことの確認が正常に完了しました。**

---

## 演習 6: Windows Autopilot の構成

### シナリオ

Windows Autopilot は、開封時のエクスペリエンス (OOBE) 中にデバイスを Microsoft Entra ID に自動的に参加させて Intune に登録することで、デバイスのプロビジョニングを効率化します。SEA-DEV3 を Autopilot に登録し、展開プロファイルを作成して、それをデバイスに割り当てます。

ラボの時間制約により、完全な Autopilot OOBE (デバイスのリセットが必要) は実行しません。Autopilot 展開ワークフローを理解するために、登録と構成の手順を完了します。

**これは (ハードウェア ハッシュ ベースの) クラシック Autopilot であり、Windows Autopilot デバイス準備ではありません。** デバイス準備は、そのサポート対象シナリオ (ユーザー主導、物理デバイス) では手動のハッシュ登録を完全にスキップする、新しくシンプルな再設計です。デバイスは登録されるだけで、登録時にセキュリティ グループに追加されます。ただし、事前プロビジョニング済み、自己展開型、既存デバイス、ハイブリッド参加、Autopilot リセットのシナリオはまだサポートしていません。これらには引き続きクラシック Autopilot が必要です。手動のハードウェア ハッシュ登録 (ここで行っていること) は、**テストと評価** に関する Microsoft 自身の文書化された方法であり、まさにこのラボのコンテキストです。運用環境での登録は、通常、代わりに OEM/リセラー/CSP を介して自動的に行われます。

### タスク 1: SEA-DEV3 の Autopilot ハードウェア ハッシュの生成

Autopilot ハードウェア ハッシュはデバイスを一意に識別するもので、Autopilot 登録に必要です。

1. **SEA-DEV3** に切り替えます。

2. ローカル管理者アカウントでサインインします:
   - **Username:** `Admin`
   - **Password:** (ラボ環境で提供されます)

3. **Start** ボタンを右クリックして **Terminal (Admin)** を選択します。

4. **Do you want to allow this app to make changes to your device?** ダイアログで、**Yes** を選択します。

5. PowerShell セッションで、出力ファイル用のフォルダーを作成し、**Get-WindowsAutopilotInfo** スクリプトをインストールします (これは PowerShell ギャラリーの **スクリプト** であり、モジュールではありません):

   ```powershell
   New-Item -ItemType Directory -Path C:\Autopilot -Force
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
   Install-Script -Name Get-WindowsAutopilotInfo -Force
   ```

   途中で応答が要求されたら **Y** 等を入力して受け入れてください。

6. インストールが完了したら、Autopilot ハードウェア ハッシュを生成して CSV ファイルにエクスポートします:

   ```powershell
   Get-WindowsAutopilotInfo -OutputFile C:\Autopilot\SEA-DEV3-AutopilotHash.csv
   ```

7. CSV ファイルが作成されたことを確認します:

   ```powershell
   Test-Path C:\Autopilot\SEA-DEV3-AutopilotHash.csv
   ```

   出力は **True** を返すはずです。

8. CSV ファイルを開いて、ハードウェア ハッシュがキャプチャされたことを確認します:

   ```powershell
   notepad C:\Autopilot\SEA-DEV3-AutopilotHash.csv
   ```

9. CSV の内容を確認します。次が含まれているはずです:
   - **Device Serial Number**
   - **Windows Product ID**
   - **Hardware Hash** (長い base64 エンコード文字列)

10. Notepad を閉じます。


    次のタスクに向けてファイルを転送するには、SEA-DEV3上でスタートメニューからOneDriveを起動し、ユーザー名 `admin@<TenantPrefix>.OnMicrosoft.com`でサインインします。OneDriveの初期設定が完了すると、.csvファイル(C:\Autopilot\SEA-DEV3-AutopilotHash.csv) をOneDriveフォルダーに保存できるようになります。

**SEA-DEV3 の Autopilot ハードウェア ハッシュの生成が正常に完了しました。**

---

### タスク 2: ハードウェア ハッシュの Intune へのアップロード

1. **SEA-DEV1** に切り替えます。

   SEA-DEV1上でスタートメニューからOneDriveを起動し、ユーザー名 `admin@<TenantPrefix>.OnMicrosoft.com`でサインインします。OneDriveの初期設定が完了すると、.csvファイルにアクセスできるようになります。.csvファイルをいったんOneDriveからSEA-DEV1のデスクトップにコピーしてください。

2. **Microsoft Edge** で **https://intune.microsoft.com** に移動します (必要に応じて管理者としてサインインします)。

   英語表記となりますので、日本語表記に変更した方が進めやすくなります。画面右上の歯車のマーク「Settings」をクリックした後、左の「Language + region」をクリックし、Language と Regional format でいずれも日本語を選択し、[Apply] - [OK] をクリックします。その後、画面左上の「Microsoft Intune 管理センター」のロゴをクリックして、Home 画面に戻ってください。

3. **Microsoft Intune 管理センター** で、**デバイス** を選択し、**デバイスのオンボーディング** の下で **登録** を選択します。

4. **Windows Autopilot** の下で、**デバイス** を選択します。

5. 上部のツールバーから **インポート** を選択します。

6. **Autopilot デバイスの追加** ペインで、フォルダー アイコンを選択して CSV ファイルを参照します。

7. `\\SEA-DEV3\C$\Autopilot\` に移動します (または、共有フォルダーや USB を使用して SEA-DEV3 から SEA-DEV1 に CSV ファイルをコピーします)。

   > [!NOTE]
   > その.csvファイルにアクセスするには、SEA-DEV1上でスタートメニューからOneDriveを起動し、ユーザー名 `admin@<TenantPrefix>.OnMicrosoft.com`でサインインします。OneDriveの初期設定が完了すると、.csvファイルにアクセスできるようになります。

8. **SEA-DEV3-AutopilotHash.csv** を選択して **Open** を選択します。

9. **Autopilot デバイスの追加** ペインで、**インポート** を選択します。

10. インポートが完了するのを待ちます。インポートが完了すると通知が表示されます (通常は 1～2 分)。

11. インポートが完了したら、**デバイス** ページを更新します。**SEA-DEV3** が Autopilot デバイスの一覧に表示されるはずです。

    > [!NOTE]
    > デバイスが完全に同期されて一覧に表示されるまで、5～10 分かかる場合があります。デバイスがすぐに表示されない場合は、ページを定期的に更新してください。

**SEA-DEV3 ハードウェア ハッシュの Intune へのアップロードが正常に完了しました。**

---

### タスク 3: Windows Autopilot 展開プロファイルの作成

Autopilot 展開プロファイルは、OOBE エクスペリエンスを定義し、ユーザーがセットアップ中に構成できる設定を決定します。

1. **Microsoft Intune 管理センター** で、**デバイス**   → **登録** に移動します。

2. **Microsoft Intune 管理センター** の **デバイス | 登録** ページで、(Windows Autopilot の下の) **デプロイプロファイル** を選択します。

3. **プロファイルの作成** → **Windows PC** を選択します。

4. **基本情報** タブで、次を入力します:

   - **名前:** `Autopilot User Driven Profile`
   - **説明:** `User driven Microsoft Entra join profile for Windows Autopilot`
   - **対象のすべてのデバイスを Autopilot に変換:** いいえ

5. **次へ** を選択します。

6. **Out-of-box experience (OOBE)** タブで、次の項目を構成します:

   - **配置モード:** ユーザードリブン
   - **次のように Microsoft Entra ID に参加:** Microsoft Entra 参加済み
   - **Microsoft ソフトウェア ライセンス条項:** 非表示
   - **プライバシー設定:** 非表示
   - **アカウントの変更オプションを非表示にする:** 非表示
   - **ユーザー アカウントの種類:** 標準
   - **事前プロビジョニングされた展開を許可:** いいえ
   - **デバイス名テンプレートを適用:** いいえ

   > [!NOTE]
   > この構成は、不要なプロンプトを非表示にすることで OOBE を簡素化します。ユーザーは Microsoft Entra の資格情報でサインインし、デバイスは自動的に構成されます。

7. **次へ** を選択し、続いてもう一度 **次へ** を選択して **スコープ タグ** をスキップします。

8. **割り当て** タブの **包含されたグループ** で、**グループの追加** を選択します。

9. **dyn-Autopilot-Devices** を検索して選択します。

10. **選択** を選択します。

   > [!NOTE]
   > プロファイルを dyn-Autopilot-Devices に割り当てることで、Autopilot に登録されたすべてのデバイスは (登録済みかどうかにかかわらず) この展開プロファイルを自動的に受け取ります。これがまさに、タスク 4 で SEA-DEV3 のプロファイルの状態が **割り当て済み** に達することを可能にするものです。

11. **次へ** を選択します。

12. **レビューと作成** タブで、設定を確認して **作成** を選択します。

**Windows Autopilot 展開プロファイルの作成が正常に完了しました。**

---

### タスク 4: SEA-DEV3 の Autopilot プロファイルの状態の確認

1. **Microsoft Intune 管理センター** で、**デバイス** → **登録** → **デバイス** (Windows Autopilot の下) に移動します。

2. Autopilot デバイスの一覧から **SEA-DEV3** を選択します。

3. デバイスの詳細を確認します:
   - **プロファイルの状態:** **割り当て済み** と表示されるはずです (動的グループが設定され、プロファイルの割り当てが同期されるまで数分かかる場合があります)
   - **グループ タグ:** なし
   - **ユーザー:** 未割り当て

   > [!NOTE]
   > 数分経ってもまだ「割り当てられていません」と表示される場合は、**デバイス** 一覧のツールバーから **同期** を選択して同期を強制します。また、dyn-Autopilot-Devices に SEA-DEV3 が実際にメンバーとして表示されていることを再確認します (**グループ** → dyn-Autopilot-Devices → **メンバー**)。SEA-DEV3 がそこにない場合は、タスク 3 のルール構文を再確認してください。

4. デバイスのプロパティ ペインを閉じます。

**Autopilot 展開プロファイルの SEA-DEV3 への割り当てが正常に完了しました。**

---

### タスク 5: (オプション) Autopilot OOBE フローの理解

運用環境では、次の手順は SEA-DEV3 をリセットして Autopilot OOBE を実行することになります。次のようなことが起こります:

1. **デバイスの起動:** SEA-DEV3 の電源が入ります (工場出荷時にリセットされたデバイス、または新しいデバイス)。

2. **Autopilot の認識:** OOBE 中に、Windows は Autopilot サービスに接続し、ハードウェア ハッシュによってデバイスを認識します。

3. **プロファイルのダウンロード:** デバイスは、割り当てられた Autopilot プロファイル (`Autopilot User-Driven Profile`) をダウンロードします。

4. **簡素化された OOBE:** ユーザーには、Microsoft のブランドが表示された簡素化された OOBE が表示されます:
   - ライセンス条項やプライバシーのプロンプトはありません (プロファイル設定に従って非表示)
   - ユーザーは Microsoft Entra の資格情報でサインインします (例: `AlexW@<TenantPrefix>.OnMicrosoft.com`)
   - デバイスは自動的に Microsoft Entra ID に参加し、Intune に登録されます

5. **ポリシーの適用:** 登録後、ユーザーがデスクトップに到達する前に、Intune ポリシー (構成プロファイル、コンプライアンス ポリシー、アプリ) が適用されます。

6. **ユーザー デスクトップ:** ユーザーは、完全に構成されたデバイスでデスクトップに到達します。

> [!NOTE]
> SEA-DEV3 をリセットしてライブの Autopilot OOBE を完了するには 20～30 分かかり、このラボの範囲を超えています。ただし、Autopilot 展開に必要なすべての前提条件 (ハードウェア ハッシュの登録、プロファイルの作成、割り当て) を完了しました。

**これで、Windows Autopilot の展開ワークフローを理解できました。**

---

## ラボのまとめ

おめでとうございます。ラボ 01: 基盤 — ID、登録、Autopilot を完了しました。

このラボでは、次のことを達成しました:

**演習 1: ユーザーとグループの構成**
- 既存の Contoso ユーザーを確認し、ライセンスを検証した
- 2 人の追加テスト ユーザーを作成した
- パイロット ユーザー用の割り当て済みセキュリティ グループを作成した
- ポリシー ターゲット設定用の動的ユーザー グループとデバイス グループを作成した

**演習 2: 管理委任の構成**
- Intune 管理者ロールを割り当てた
- クラウド デバイス管理者ロールを割り当てた
- 管理単位を作成し、管理アクセスをスコープ設定した
- `Pharmacy` Intune スコープ タグと `Pharmacy Helpdesk` カスタム Intune ロールを作成した (Labs 02～06 にわたって関連付け)

**演習 3: デバイスの登録と設定の構成**
- Microsoft Entra ID でデバイス参加設定を構成した
- Microsoft Entra 参加済みデバイス用に追加のローカル管理者を追加した
- ローカル管理者パスワード管理のために Microsoft Entra LAPS を有効にした

**演習 4: Windows 登録ポリシーの構成**
- テナントに対する Intune への自動登録を確認した
- 初回実行エクスペリエンスをゲートするために既定の登録状態ページを構成した
- パイロット グループ用に、より厳格なブロック型の登録状態ページ プロファイルを作成した
- 既定の登録制限を確認した
- デバイス数の制限ポリシーを作成して割り当てた
- カスタム プラットフォーム制限で個人所有の Android の登録をブロックした

**演習 5: Windows デバイスの登録**
- Microsoft Entra 参加を使用して SEA-DEV1 (Megan Bowen として) を登録した
- Microsoft Entra 参加を使用して SEA-DEV2 (Joni Sherman として) を登録した
- Intune で両方のデバイスと動的グループのメンバーシップを確認した

**演習 6: Windows Autopilot の構成**
- SEA-DEV3 の Autopilot ハードウェア ハッシュを生成した
- ハードウェア ハッシュを Intune にアップロードした
- Windows Autopilot 展開プロファイルを作成した
- プロファイルを SEA-DEV3 に割り当てた

**重要なポイント:**
- Microsoft Entra ID は最新のデバイス管理の基盤です。デバイスは Intune に登録する前に参加または登録されている必要があります
- 動的グループは、ユーザーまたはデバイスの属性に基づいてポリシー ターゲット設定を自動化します。`-and`/`-or` を使用する複合ルールは、規制対応またはリージョン単位のスコープ設定の標準的なパターンです
- Microsoft Entra ID ロール + 管理単位は Entra レベルのアクセス許可を委任します。Intune には、ポリシーとデバイスの管理を委任するための **カスタム ロール + スコープ タグ** を持つ **個別の** RBAC システムがあります
- 初日に作成したスコープ タグ (Pharmacy) は、後で作成するすべての Intune オブジェクトに関連付けられます。委任された管理モデルを維持するために、ポリシー作成時にそれらを適用してください
- 最新のクラウドのみのテナントでは、Intune への自動登録が既定で有効になっています。明示的な MDM スコープの構成は、主にハイブリッド ID と共同管理の関心事です
- 登録状態ページは、ユーザーの初回実行エクスペリエンスを形作るものです。ターゲットを絞った優先度付きのプロファイルを使用して、パイロット ユーザーにはより厳格なブロック型のエクスペリエンスを、標準ユーザーにはより速いサインインを提供してください
- デバイス プラットフォームの制限は、承認されていないプラットフォームや所有権の種類に対するセーフティ ネットです (例: 個人用 Android はブロック、社用 Android Enterprise は許可)
- Windows Autopilot は、デバイスを事前登録し、OOBE 中に展開プロファイルを適用することで、デバイスのプロビジョニングを効率化します

**次のステップ:**
このラボで登録したデバイス (SEA-DEV1 と SEA-DEV2) は、後続のラボで構成プロファイル、コンプライアンス ポリシー、アプリケーション、セキュリティ ベースラインを展開するために使用されます。Lab 02 では、Intune ポリシーを使用してこれらのデバイスを管理および保守することに焦点を当てます。

---

**ラボ終了**
