---
lab:
   title: 'Lab 06: 機能の拡張'
   description: 'このラボでは、Endpoint Privilege Management を構成し、Remote Help を展開し、Advanced Analytics を確認し、Windows 365 Cloud PC と Azure Virtual Desktop のシナリオを確認します。'
   duration: 90 minutes
   level: 200
   islab: true
   primarytopics:
      - Microsoft Intune
      - Endpoint Privilege Management
      - Remote Help
      - Windows 365
      - Azure Virtual Desktop
---

# Lab 06: 機能の拡張

## ラボ シナリオ

あなたは Contoso Healthcare のモダン エンドポイント管理者である **Jordan Chen** です。包括的な Intune 環境 (ラボ 01 〜 05) を実装済みであり、ここからは **Microsoft Intune Suite** のプレミアム機能を活用します。アプリケーションの昇格を制御するために Endpoint Privilege Management (EPM) を構成し、セキュリティで保護されたリモート支援のために Remote Help を展開し、Advanced Analytics ダッシュボードを確認し、クラウド ホスト型デスクトップのシナリオ (Windows 365 と Azure Virtual Desktop) を確認します。

このラボの終了までに、次を完了します。
- テナント設定で Endpoint Privilege Management を有効化する
- Windows 昇格設定ポリシーと昇格ルール ポリシーを作成する
- EPM の昇格シナリオ (自動、ユーザー確認、サポート承認) をテストする
- まずパイロット コホートへ EPM を展開し、その後フリート全体へ拡大する (典型的なパイロット優先パターン)
- Remote Help を有効化してライセンスを割り当てる
- Remote Help アプリをデバイスへ展開する
- デバイス間で Remote Help セッションを開始する
- Pharmacy ヘルプデスクの委任管理者 (**ラボ 05 の演習 3** の Lee Gu) が、Pharmacy スコープのデバイスに対してのみ Remote Help を起動できることを実証する
- Advanced Analytics ダッシュボードを確認する (デモンストレーション)
- Windows 365 Cloud PC のプロビジョニングを確認する (デモンストレーション)
- Azure Virtual Desktop のセッション ホスト登録を確認する (デモンストレーション)

---

## ラボの所要時間

**推定所要時間:** 90 分

---

## 手順

### 開始する前に

このラボには次が必要です。
- **ラボ 01** の完了 (デバイスが登録済みであること)
- **Microsoft Intune Suite の試用版が有効** (**ラボ 01** の前提条件で有効化済み) — Endpoint Privilege Management、Remote Help、および Advanced Analytics に必要です
- Contoso の Microsoft 365 テナント (`<TenantPrefix>.onmicrosoft.com`) へのアクセス
- グローバル管理者または Intune 管理者の資格情報
- **SEA-DEV1**、**SEA-DEV2**、および **SEA-DEV3** (登録済みの Windows 11 デバイス)

---

## 演習 1: Endpoint Privilege Management (EPM) の構成

### シナリオ

Endpoint Privilege Management を使用すると、完全な管理者権限を付与することなく、標準ユーザーが特定のアプリケーションを昇格された特権で実行できます。Intune Suite の試用版が有効 (**ラボ 01** の前提条件から) な状態では、EPM はテナント内で既にプロビジョニングされています。ここでは EPM が利用可能であることを確認し、昇格設定ポリシーと昇格ルール ポリシーを作成し、さまざまな昇格シナリオをテストします。

### タスク 1: Endpoint Privilege Management が利用可能であることを確認する

**Endpoint Privilege Management** は、Microsoft Intune Suite のアドオン機能の 1 つです。**ラボ 01** の前提条件で Suite 試用版を有効化した状態では、EPM はテナント内で既にプロビジョニングされており、テナント全体に対する個別の "EPM を有効にする" トグルは存在しません。代わりに、**Endpoint Privilege Management** 設定を **Enabled** に設定した **Windows 昇格設定ポリシー**を展開することで、個々のデバイス上で EPM を有効化します (これはタスク 2 で行います)。

1. **Microsoft Intune 管理センター**で、**エンドポイント セキュリティ** → **Endpoint Privilege Management** の順に移動します。

1. EPM ブレードが読み込まれ、上部に次のタブが表示されることを確認します。
   - **概要** (インサイト ダッシュボード — 新しいテナントでは "データが不足しています" と表示される場合があります)
   - **レポート** (昇格レポート、昇格要求、ファイルの傾向)
   - **ポリシー** (昇格設定ポリシーと昇格ルール ポリシーを作成する場所)
   - **再利用可能な設定** (ルール全体で使用する共有証明書グループ)
   - **昇格要求** (現在および過去のサポート承認された昇格要求)

   > [!NOTE]
   > EPM ブレードに、この機能がライセンスされていないことを示すバナーが表示される場合は、**ラボ 01 の前提条件**に戻り、続行する前に Microsoft Intune Suite の試用版の有効化を完了してください。EPM には、有効な試用版または購入済みの Microsoft Intune Suite ライセンスのいずれかが必要です。

**Endpoint Privilege Management が利用可能であることを正常に確認しました。**

---

### タスク 2: 昇格設定ポリシーを作成する

昇格設定ポリシーは、デバイスの既定の昇格動作を定義するものであり、そもそも**デバイス上で EPM を有効にする**ものでもあります。このポリシー内の `Endpoint Privilege Management` 設定が、EPM エージェントをインストールし、昇格要求の評価を開始します。

1. **Microsoft Intune 管理センター**の **Endpoint Privilege Management** ページで、**ポリシー**タブを選択します。

1. 上部のツールバーから **+ 作成**を選択します。

1. **プロファイルの作成**ペインで、次を設定します。
   - **プラットフォーム:** Windows
   - **プロファイル:** Windows 昇格設定ポリシー

1. **作成**を選択します。

1. **基本情報**ページで、次を入力します。
   - **名前:** `EPM Settings - Default Behavior`
   - **説明:** `Default EPM settings for the Contoso fleet`

1. **次へ**を選択します。

1. **構成設定**ページで、次を構成します。
   - **Endpoint Privilege Management:** Enabled (これはデバイス上で実際に EPM を有効にするトグルです。無効のままにするかポリシーを削除すると、7 日後にそれらのデバイスで EPM が無効になります)
   - **既定の昇格応答:** ユーザーの確認を必須にする
      - **検証:** ビジネス上の理由 (ユーザーに理由の入力を求める)
   - **昇格データをレポート用に送信:** はい
      - **レポートの範囲:** 診断データと管理された昇格のみ (**レポート**タブの下の **昇格レポート**にデータが表示されるようにします)

   > [!NOTE]
   > 4 つの昇格応答は、**構成されていません**、**すべての要求を拒否**、**サポートの承認を必須にする**、および**ユーザーの確認を必須にする**です。"構成されていません" は "すべての要求を拒否" と同じ動作になります。このラボでは、後のタスク 6 で EPM プロンプトを確認できるように `ユーザーの確認を必須にする` を選択してください。運用環境では、未知のファイルに対するセキュリティ上より強固な選択は、明示的な昇格ルールと組み合わせた `すべての要求を拒否` です。

1. **次へ**を選択します。

1. **スコープ タグ**タブで、**次へ**を選択します。

1. **割り当て**タブの**包含されたグループ**で、**グループの追加**を選択します。

1. **dyn-Windows-Devices** を検索して選択します。

1. **選択** → **次へ** → **作成**の順に選択します。

**昇格設定ポリシーを正常に作成しました。**

---

### タスク 3: 自動昇格ルールを作成する

自動昇格ルールを使用すると、特定のアプリケーションをユーザーへのプロンプトなしで常に昇格された状態で実行できます。

1. **Endpoint Privilege Management** ページの**ポリシー**タブで、**+ 作成**を選択します。

1. **プロファイルの作成**ペインで、**プラットフォーム**を **Windows** に、**プロファイル**を **昇格ルール ポリシー**に設定します。**作成**を選択します。

1. **基本情報**ページで、次を入力します。
   - **名前:** `EPM Rules - Automatic Elevation`
   - **説明:** `Allows Registry Editor to always run elevated`

1. **次へ**を選択します。

1. **構成設定**タブで、**特権管理**を展開し、**追加**を選択して新しい昇格ルールを追加します。

1. **+ インスタンスの編集**を選択し、**ルールのプロパティ**ペインで次を構成します。
   - **ルール名:** `Elevate Registry Editor`
   - **説明:** `Automatically elevates regedit.exe without user prompt`
   - **昇格の種類:** 自動
   - **ファイル名:** `regedit.exe`
   - **ファイル パス:** `C:\Windows\regedit.exe`
   - **署名ソース:** 構成されていません

1. **SEA-DEV1** で、昇格された PowerShell ウィンドウを開き、次のコマンドを実行して `regedit.exe` の SHA256 ハッシュを取得し、そのハッシュ値をルールのプロパティ ペインの **ファイル ハッシュ**フィールドにコピーします。
     ```powershell
     Get-FileHash -Path "C:\Windows\regedit.exe" -Algorithm SHA256
     ```

   > [!NOTE]
   > ファイルベースのルールは、パスによって特定の実行可能ファイルを対象とします。より正確に対象を絞るために、ファイル ハッシュ、発行者証明書、または製品名に基づくルールを作成することもできます。

1. **保存**を選択してルールを追加します。

1. **スコープ タグ**タブで、**次へ**を選択します。

1. **包含されたグループ**の下で **グループの追加**を選択し、`sg-Intune-Pilot-Users` (EPM のパイロット優先の展開 — 以前のラボのブロック ESP、パイロット更新リング、ブロック モードの ASR ルールと同じコホート) を検索して選択し、**次へ** → **作成**の順に選択します。

**パイロット コホートを対象とする自動昇格ルールを正常に作成しました。**

---

### タスク 4: ユーザー確認の昇格ルールを作成する

ユーザー確認の昇格ルールは、ユーザーに昇格の承認を求めます (ビジネス上の理由の入力を任意で求めます)。

1. **Endpoint Privilege Management** ページの**ポリシー**タブで、**+ 作成**を選択します。

1. **プロファイルの作成**ペインで、**プラットフォーム**を **Windows** に、**プロファイル**を **昇格ルール ポリシー**に設定します。**作成**を選択します。

1. **基本情報**タブで、次を入力します。
   - **名前:** `EPM Rules - User Confirmed Elevation`
   - **説明:** `Prompts user to approve elevation for MSConfig`

1. **次へ**を選択します。

1. **構成設定**タブで、**特権管理**を展開し、**追加**を選択して新しい昇格ルールを追加します。

1. **ルールのプロパティ**ペインで、次を構成します。
   - **ルール名:** `Elevate MSConfig with User Confirmation`
   - **昇格の種類:** ユーザー確認
   - **検証:** ビジネス上の理由 (ユーザーに理由の入力を求める)
   - **ファイル名:** `msconfig.exe`
   - **ファイル パス:** `C:\Windows\System32\msconfig.exe`
   - **署名ソース:** 構成されていません

1. **SEA-DEV1** で、昇格された PowerShell ウィンドウを開き、次のコマンドを実行して `msconfig.exe` の SHA256 ハッシュを取得し、そのハッシュ値をルールのプロパティ ペインの **ファイル ハッシュ**フィールドにコピーします。
     ```powershell
     Get-FileHash -Path "C:\Windows\System32\msconfig.exe" -Algorithm SHA256
     ```

1. **保存** → **次へ**の順に選択します。

1. **スコープ タグ**タブで、**次へ**を選択します。

1. **割り当て**タブの**包含されたグループ**で、**グループの追加**を選択し、`sg-Intune-Pilot-Users` (EPM のパイロット優先の展開 — 以前のラボのブロック ESP、パイロット更新リング、ブロック モードの ASR ルールと同じコホート) を検索して選択し、**次へ** → **作成**の順に選択します。

**パイロット コホートを対象とするユーザー確認の昇格ルールを正常に作成しました。**

---

### タスク 5: サポート承認の昇格ルールを作成する

サポート承認の昇格ルールでは、ヘルプ デスクの担当者がリモートで昇格要求を承認する必要があります。

1. **Endpoint Privilege Management** ページの**ポリシー**タブで、**+ 作成**を選択します。

1. **プロファイルの作成**ペインで、**プラットフォーム**を **Windows** に、**プロファイル**を **昇格ルール ポリシー**に設定します。**作成**を選択します。

1. **基本情報**タブで、次を入力します。
   - **名前:** `EPM Rules - Support Approved Elevation`
   - **説明:** `Requires help desk approval for CMD elevation`

1. **次へ**を選択します。

1. **構成設定**タブで、**特権管理**を展開し、**追加**を選択して新しい昇格ルールを追加します。

1. **ルールのプロパティ**ペインで、次を構成します。
   - **ルール名:** `Elevate Command Prompt with Support Approval`
   - **昇格の種類:** サポート承認
   - **ファイル名:** `cmd.exe`
   - **ファイル パス:** `C:\Windows\System32\cmd.exe`
   - **署名ソース:** 構成されていません

1. **SEA-DEV1** で、昇格された PowerShell ウィンドウを開き、次のコマンドを実行して `cmd.exe` の SHA256 ハッシュを取得し、そのハッシュ値をルールのプロパティ ペインの **ファイル ハッシュ**フィールドにコピーします。
     ```powershell
     Get-FileHash -Path "C:\Windows\System32\cmd.exe" -Algorithm SHA256
     ```

1. **保存** → **次へ**の順に選択します。

1. **スコープ タグ**タブで、**次へ**を選択します。

1. **割り当て**タブの**包含されたグループ**で、**グループの追加**を選択し、`sg-Intune-Pilot-Users` (EPM のパイロット優先の展開 — 以前のラボのブロック ESP、パイロット更新リング、ブロック モードの ASR ルールと同じコホート) を検索して選択し、**次へ** → **作成**の順に選択します。

> [!NOTE]
> **パイロット優先の EPM 展開。** 3 つの昇格ポリシーはすべて、最初はパイロット コホートを対象とします。**Endpoint privilege management** → **レポート** → **昇格の概要**を 1 週間監視します。自動ルールが悪用されていないこと (正当なレジストリ編集のみであること)、ユーザー確認ルールのビジネス上の理由が妥当に見えること、およびサポート承認ルールの要求量が管理可能であることを確認します。その後、各ポリシーの割り当てを `dyn-Windows-Devices` (`sg-Intune-Pilot-Users` を除外) に拡大します。ラボ 04 の演習 2 の ASR 展開と同じパターンです。

**パイロット コホートを対象とするサポート承認の昇格ルールを正常に作成しました。**

---

### タスク 6: SEA-DEV3 で EPM の昇格をテストする

1. **SEA-DEV3** に切り替えます (このデバイスは、標準ユーザー アカウント (例: Alex Wilber) で登録されている必要があります)。

   > [!NOTE]
   > ユーザーがまだ登録されていない場合は、**Admin** アカウントでサインインし、**Settings** → **Accounts** → **Access work or school** → **Connect** → **Join this device to Microsoft Entra ID** の順に選択して **AlexW@<TenantPrefix>.OnMicrosoft.com** でサインインします。**Join** と **Done** を選択して登録を完了します。その後、サインアウトし、**AlexW@<TenantPrefix>.OnMicrosoft.com** として再度サインインします。PIN の設定を求められた場合は、設定します。

1. **AlexW@<TenantPrefix>.OnMicrosoft.com** (ローカル管理者ではない標準ユーザー) としてサインインします。

1. EPM ポリシーを適用するために、デバイスの同期を強制します。
   - **Settings** → **Accounts** → **Access work or school** → **Connected to Contoso** → **Info** → **Sync**

1. ポリシーが適用されるまで 10 〜 15 分待ちます。

   > [!NOTE]
   > Windows 11 では、右クリックするとコンパクトなコンテキスト メニューが表示されます。**Run with elevated access** (EPM エージェントによって追加される) は、通常そこに直接表示されます。表示されない場合は、**Show more options** を選択する (または **Shift+F10** を押す) と、フル メニューが開きます。それでも表示されない場合は、昇格設定ポリシーのデバイスへの適用がまだ完了していません。デバイスを同期し、数分待ってから再試行してください。

1. **自動昇格** (Registry Editor) をテストします。
   - **Start** メニューを開き、`regedit` を検索します
   - **Open file location** を選択します
   - **Registry Editor** を選択して右クリックし、**Run with elevated access** を選択して **Registry Editor** を起動します
   - ビジネス上の理由 (例: "Testing automatic elevation") を入力し、**Continue** を選択して承認します
   - **期待される動作:** アプリはプロンプトなしで昇格された状態で起動します (自動昇格ルールが適用される)

1. **ユーザー確認の昇格** (MSConfig) をテストします。
   - **Start** メニューを開き、`msconfig` を検索します
   - **Open file location** を選択します
   - **System Configuration** を選択して右クリックし、**Run with elevated access** を選択して **System Configuration** を起動します
   - **期待される動作:** ユーザーに昇格の確認とビジネス上の理由の入力を求めるプロンプトが表示されます
   - 理由 (例: "Troubleshooting startup issues") を入力し、**Continue** を選択して承認します

1. **サポート承認の昇格** (Command Prompt) をテストします。
   - **Start** メニューを開き、`cmd` を検索します
   - **Open file location** を選択します
   - **Command Prompt** を右クリックし、**Run with elevated access** を選択します
   - ビジネス上の理由 (例: "Need elevated command prompt for script execution") を入力し、**Continue** を選択します
   - **期待される動作:** 要求がヘルプ デスクの承認待ちであることを示すプロンプトが表示されます。

   > [!NOTE]
   > 運用環境では、ヘルプ デスクの担当者が **Endpoint privilege management** ダッシュボードで昇格要求を確認し、リモートで承認または拒否します。
   >
   > 承認を求めるプロンプトが表示されずに Command Prompt が昇格された状態で開いた場合は、サポート承認ルールがまだデバイスに到達していません。**EPM Rules - Support Approved Elevation** ポリシーが SEA-DEV3 に対して **成功**のチェックインを示していることを確認し (**ポリシー** → ポリシーを選択 → **レポートの表示**)、デバイスを同期して再度テストします。数分待っても動作しない場合は、次のタスクに進みます。

**EPM の昇格シナリオを正常にテストしました。**

---

### タスク 7: EPM の昇格レポートを監視する

1. **SEA-DEV1** で、**Microsoft Intune 管理センター**の**エンドポイント セキュリティ** → **Endpoint Privilege Management** → **レポート**の順に移動します。

1. **昇格レポート**タイルを選択します。

1. レポート データを確認します。
   - **合計昇格数:** すべての昇格要求の数
   - **自動昇格数:** 自動承認の数
   - **ユーザー確認の昇格数:** ユーザーが承認した要求の数
   - **サポート承認の昇格数:** ヘルプ デスクが承認した要求の数
   - **拒否された昇格数:** ブロックされた要求の数

1. **昇格の詳細**を選択して、個々の昇格イベントを表示します。
   - **デバイス名**
   - **ユーザー名**
   - **アプリケーション名**
   - **昇格の種類**
   - **タイムスタンプ**
   - **ビジネス上の理由** (指定されている場合)

**EPM の昇格レポートを正常に監視しました。**

---

## 演習 2: Remote Help の展開

### シナリオ

Remote Help は、登録済みデバイスに対して、セキュリティで保護され監査されたリモート支援を提供します。IT 管理者は、問題をトラブルシューティングするためにデバイスをリモートで表示および制御できます。ここでは Remote Help を有効化し、ライセンスを割り当て、アプリを展開し、リモート セッションを開始します。

### タスク 1: Remote Help を有効にする

1. **Microsoft Intune 管理センター**で、**テナント管理**を選択し、**Remote Help** を選択します。

1. **Remote Help** ページで、**設定**タブを選択します。

1. **構成**を選択し、次の設定を構成します。
   - **Remote Help を有効にする:** 有効
   - **未登録デバイスへの Remote Help を許可:** 許可しない
   - **チャットを無効にする:** いいえ

1. **保存**を選択します。

**Remote Help を正常に有効にしました。**

---

### タスク 2: Remote Help のライセンスを割り当てる

Remote Help には Microsoft Intune Suite のライセンスが必要です。

1. **Microsoft Edge** で、**https://admin.cloud.microsoft.com** に移動します。

1. **admin@<TenantPrefix>.onmicrosoft.com** としてサインインします。

1. **Microsoft 365 管理センター**で、**ユーザー**を展開し、**アクティブなユーザー**を選択します。

1. **Megan Bowen** (ヘルパーの役割 — ヘルプ デスクまたは管理者) を選択します。

1. **ライセンスとアプリ**タブを選択します。

1. **Microsoft Intune Suite** が割り当てられていることを確認します (存在しない場合は割り当てます)。

1. **Joni Sherman** (共有者の役割 — 支援を受けるエンド ユーザー) と **Lee Gu** (Pharmacy ヘルプデスクの役割) についても同じ操作を繰り返します。

   > [!NOTE]
   > ヘルパー (IT 管理者) と共有者 (エンド ユーザー) の両方に Remote Help のライセンスが必要です。

**Remote Help のライセンスを正常に割り当てました。**

---

### タスク 3: Remote Help アプリを展開する

1. **Microsoft Intune 管理センター**で、**アプリ** → **すべてのアプリ**の順に移動します。

1. 上部のツールバーから **+ 作成**を選択します。

1. **アプリの種類の選択**ペインで、**プラットフォーム**を **Windows** に、**アプリの種類**を **Windows app (Win32)** に設定します。**選択**を選択します。

   > [!NOTE]
   > Remote Help は、Microsoft Store アプリとして展開することも、OEM/イメージによって事前インストールすることもできます。このラボの目的のため、Win32 アプリとして展開します。

1. **アプリ情報**ページで、**アプリ パッケージ ファイルの選択**を選択します。

1. **アプリ パッケージ ファイル**ペインで、フォルダー アイコンを選択し、Remote Help インストーラー (ラボ環境で提供されるもの、または **https://aka.ms/downloadremotehelp** からダウンロード) を探します。

1. `.intunewin` パッケージ (事前にパッケージ化されている場合) または `.msi` インストーラーをアップロードします。

1. **OK** を選択します。

1. **アプリ情報**タブで、次を入力します。
   - **名前:** `Remote Help`
   - **説明:** `Secure remote assistance app for enrolled devices`
   - **発行元:** Microsoft Corporation

1. **次へ**を選択します。

1. **プログラム**タブで、次を構成します。
   - **インストール コマンド:** `remotehelpinstaller.exe /quiet acceptTerms=1`
   - **アンインストール コマンド:** `remotehelpinstaller.exe /uninstall /quiet acceptTerms=1`
   - **インストールの動作:** システム

1. **要件**タブで、次を構成します。
   - **オペレーティング システム アーキテクチャの確認:** **はい。アプリをインストールできるシステムを指定します**を選択します。**x64 システムにインストール**をオンにします
   - **最小オペレーティング システム:** Windows 10 1607

1. **次へ**を選択します。**検出規則**タブで、**規則の形式**を **検出規則を手動で構成する**に設定し、**追加**を選択します。

1. **検出規則**ペインで、次を構成します。
   - **規則の種類:** ファイル
   - **パス:** `C:\Program Files\Remote help`
   - **ファイルまたはフォルダー:** `RemoteHelp.exe`
   - **検出方法:** ファイルまたはフォルダーが存在する
   - **64 ビット クライアント上の 32 ビット アプリに関連付ける:** いいえ

1. **OK** → **次へ**の順に選択し、**割り当て**タブに到達するまで進みます。

1. **割り当て**タブで、**+ グループの追加**を選択し、**dyn-Windows-Devices** グループを **必須**として割り当てます。

1. **次へ** → **作成**の順に選択します。

**Remote Help アプリを正常に展開しました。**

---

### タスク 4: Remote Help セッションを開始する

> [!NOTE]
> **Remote Help** のサインイン プロンプトに **"デバイスが組織のコンプライアンス要件に準拠している必要があります":** と表示される場合、TPM のないラボ VM では **デバイスのデータ ストレージの暗号化を必須にする**コンプライアンス設定が失敗し、デバイスが非準拠としてマークされます。**Graph API - Windows Compliance Policy** と **Compliance - Windows Security Baseline** の**両方**で、この設定を **構成されていません**に設定してください (**デバイス** → **コンプライアンス** → **Graph API - Windows Compliance Policy** → **プロパティ** → **コンプライアンス設定** → **システム セキュリティ** → **デバイスのデータ ストレージの暗号化を必須にする**を **構成されていません**に設定 → **確認と保存**)。その後、各デバイスを**同期**し、**準拠**と表示されることを確認します。

1. **SEA-DEV1** (ヘルパー デバイス — Megan Bowen) で、Remote Help がインストールされるのを待ちます。

1. インストール後、**Start** メニューから **Remote Help** を起動します。

1. **MeganB@<TenantPrefix>.OnMicrosoft.com** (ヘルパーの役割) としてサインインします。

1. **About your privacy** プロンプトで、**Accept** を選択します。

1. Remote Help アプリの **Give help** で、**Get a security code** を選択します。

1. 6 桁の支援コードが表示されます (例: `123-456`)。

1. **SEA-DEV2** (共有者デバイス — Joni Sherman) に切り替え、**JoniS@<TenantPrefix>.OnMicrosoft.com** としてサインインします。

1. **Start** メニューから **Remote Help** を起動します。

1. **JoniS@<TenantPrefix>.OnMicrosoft.com** (共有者の役割) としてサインインします。

1. **About your privacy** プロンプトで、**Accept** を選択します。

1. Remote Help アプリの **Get Help** で、**Security code from assistant** ボックスに SEA-DEV1 の 6 桁のコードを入力し、**Submit** を選択します。

1. **SEA-DEV1** に戻ります。Remote Help アプリに、Joni が支援を受ける準備ができていることを示すプロンプトが表示されます。**View screen** を選択します。

1. **SEA-DEV2** で、Joni にリモート セッションの承認を求める同意プロンプトが表示されます。

1. **SEA-DEV2** で、**Allow** を選択します。これで Megan は Joni のデスクトップを表示できます。

   > [!NOTE]
   > 既定では、Remote Help は表示専用のアクセスを提供します。Megan は Remote Help ツールバーの **Request control** を選択して、フル コントロールを要求できます。Joni はフル コントロールの要求を承認する必要があります。

1. **SEA-DEV1** に戻り、リモート操作をテストします。
   - Megan はチャットを使用して Joni とやり取りできます
   - Megan は制御を要求してアプリケーションを操作できます
   - Megan はいつでもセッションを終了できます

1. テスト後、**Leave** を選択して切断します。

**Remote Help セッションを正常に開始してテストしました。**

---

### タスク 5: Remote Help のセッション ログを確認する

1. **Microsoft Intune 管理センター**で、**テナント管理** → **Remote Help** の順に移動します。

   > [!NOTE]
   > このページは既定で **監視**タブが開きます。3 つのタブは、**監視** (現在のセッション、平均セッション時間、合計セッション数)、**設定** (タスク 1 で Remote Help を有効にした場所)、および **Remote Help セッション** (セッションごとの監査ログ) です。

1. **Remote Help セッション**タブを選択します。

1. セッション ログの列を確認します。
   - **プロバイダー ID** (ヘルパー — 例: `MeganB@<TenantPrefix>.onmicrosoft.com`)
   - **受信者 ID** (共有者 — 例: `JoniS@<TenantPrefix>.onmicrosoft.com`)
   - **受信者名**
   - **デバイス名**
   - **OS**
   - **セッション開始**
   - **セッション終了**

   > [!NOTE]
   > セッション ログは、コンプライアンスとセキュリティのレビューのための監査証跡を提供します。セッション中のすべての操作が記録されます。

**Remote Help のセッション ログを正常に確認しました。**

---

### タスク 6: Pharmacy ヘルプデスクの Remote Help スコープを実証する

**ラボ 05 の演習 3** で **Lee Gu** に割り当てられた `Pharmacy Helpdesk` ロールは、**Pharmacy** のタグが付いたオブジェクトにスコープが設定された、読み取り + リモート タスクの権限 (**Sync devices**、**Restart now**、**Collect diagnostics** を含む) を付与します。Remote Help は同じスコープを継承します。つまり、Lee Gu は Pharmacy のタグが付いたデバイスに対して Remote Help セッションを開始できますが、彼女のスコープ外のデバイスに対しては開始できません。これは上級中級レベルの "委任されたリモート支援" パターンです。

1. 新しい **InPrivate** または **Incognito** のブラウザー ウィンドウを開きます。**https://intune.microsoft.com** に移動します。

1. **LeeG@<TenantPrefix>.OnMicrosoft.com** (Pharmacy ヘルプデスクの委任管理者である Lee Gu) としてサインインします。

1. Lee Gu として Intune 管理センターで、**デバイス** → **すべてのデバイス**の順に移動します。

   > [!NOTE]
   > Lee Gu には、`dyn-Windows-Devices` に含まれ、かつ Pharmacy スコープ タグが付いたデバイスのみが表示されます (ラボ 05 の演習 3 のタスク 3 で構成したロールの割り当てに基づく)。デバイスの作成時に Pharmacy のタグを付けたデバイスによっては、この一覧はグローバル管理者に表示されるものより少なくなる場合があります。

1. Pharmacy のタグが付いたデバイス (例: **Pharmacy** のタグが付いた **SEA-DEV1**) を選択します。

1. デバイス ブレードで、**新しいリモート アシスタンス セッション**オプション (ツールバーまたはデバイス操作メニュー) を探します。

1. Lee Gu が Remote Help セッションを開始できることを確認します。セッションは Remote Help クライアントで開きます。これは上記のタスク 4 と同じフローです。

1. セッションを終了します。

1. 次に、Pharmacy のタグが付いていないデバイスを選択してみます (Lee Gu の一覧には表示されないため、これは思考実験です)。仮にそのようなデバイスが彼女のビューに存在したとしても、ロールのスコープ タグの積集合から除外されるため、**新しいリモート アシスタンス セッション**オプションは表示されません。

   > [!NOTE]
   > **要点。** カスタム ロールのスコープ タグは、構成 / コンプライアンス / アプリの領域だけのものではなく、Sync、Restart、Remote Help などの**リモート タスク操作**にも適用されます。これこそが、スコープ タグ ベースの委任を実際に安全にするものです。つまり、Pharmacy ヘルプデスクは、自身の担当領域外のデバイスを物理的に支援 (または誤って中断) することができません。

1. InPrivate ウィンドウからサインアウトします。

**ラボ 01 で作成し、ラボ 05 で割り当てた Pharmacy ヘルプデスクの委任ロールが、設計どおりに Remote Help 操作にスコープを設定することを、エンド ツー エンドで正常に実証しました。スレッド A (カスタム RBAC + スコープ タグの委任) が、6 つのラボすべてにわたって完了しました。**

---

## 演習 3: Advanced Analytics と Device Query の使用

### シナリオ

**Advanced Analytics** (Intune Suite の一部) は、デバイスのパフォーマンス、異常検出、リソース使用率に関する ML を活用したインサイトを提供します。**Device Query** は、Kusto 照会言語 (KQL) を使用して Windows デバイスのテレメトリに対してアドホック クエリを実行します。対象は単一のデバイス (ライブ) でも、多数のデバイスにまたがってでも可能です。これは "リモート デスクトップを実行して確認してください" に代わる、上級中級レベルの手法です。委任管理者は、ユーザーのデバイスに一切触れることなく、実際のサポートに関する質問に回答できます。

**ラボ 01** の前提条件で有効化した Intune Suite の試用版には Advanced Analytics が含まれているため、この演習は完全にハンズオンです。

> [!IMPORTANT]
> **Device Query のデバイス前提条件。** デバイスは、Device Query の結果に表示される前に、**Endpoint Analytics に登録**されている必要があります。Endpoint Analytics の登録は、**レポート** → **エンドポイント分析** → **設定**からテナント全体で有効化します。**ラボ 02 の演習 5 のタスク 1** (エンドポイント分析の有効化) を完了している場合、デバイスは既に登録され、準備が整っています。

> [!NOTE]
> **新しいテナントでは空の結果が正常です。** 少なくとも 1 台の Windows デバイスが実際に Endpoint Analytics にチェックインするまでは、タスク 3 のすべての複数デバイス Device Query は **0 件**を返します。Device Query ページの [はじめに] → [前提条件] ペインでもこれが繰り返し説明されています。*"デバイスがデバイス クエリに表示されるためには、Endpoint Analytics に登録されている必要があります。"* SEA-DEV1/SEA-DEV2 がまだチェックインしていない場合は、代わりにデバイス ブレードに対して単一デバイス クエリ (タスク 2) を実行してください。これらはライブで実行され、Endpoint Analytics のカタログに依存しません。

> [!NOTE]
> **テレメトリの待ち時間。** Advanced Analytics ダッシュボード (異常検出、リソース パフォーマンス、バッテリーの正常性) には、有意義にデータが表示されるまでに約 **24 時間のデバイス テレメトリ**が必要です。一方、Device Query はデバイスの**ライブ状態**に対して実行され、数秒以内に結果を返します。SEA-DEV1/SEA-DEV2 デバイスの登録から 24 時間未満の場合、タスク 1 のダッシュボードには "データが不足しています" と表示される場合がありますが、タスク 2 とタスク 3 (Device Query) は引き続き機能します。

### タスク 1: Advanced Analytics ダッシュボードを確認する

1. **Microsoft Intune 管理センター**で、**レポート** → **分析** → **エンドポイント分析**の順に移動します。

1. **開始**を選択して **Advanced Analytics** を開きます。

1. **異常**ダッシュボードを確認します。
   - **デバイスの異常:** 異常な動作を示すデバイス (高い CPU 使用率、頻繁なクラッシュ、アプリのハング)
   - **ユーザーの異常:** エクスペリエンス スコアが低下しているユーザー
   - **アプリケーションの異常:** クラッシュ率が高い、または起動時間が遅いアプリ

   > [!NOTE]
   > 異常検出は ML を使用して、各デバイス自身の過去のベースライン (フリート全体のしきい値ではない) から外れ値を特定します。履歴が限られている新しいラボ デバイスでは、空のパネルまたはステータス バナーが表示される場合があります。これは想定される動作です。

1. **リソース パフォーマンス**ダッシュボードを確認します。
   - **CPU パフォーマンス:** 持続的に高い CPU 使用率のデバイス
   - **メモリ パフォーマンス:** メモリ負荷 (ページ フォールト、ワーキング セットの負荷) があるデバイス
   - **ディスク パフォーマンス:** ディスク I/O が遅いデバイス

1. **バッテリーの正常性**ダッシュボードを確認します (モバイル デバイスが登録されている場合)。
   - **バッテリー容量の劣化:** 設計容量と比較してバッテリーの正常性が低下しているデバイス
   - **充電動作:** 頻繁な充電サイクル

**Advanced Analytics ダッシュボードを正常に確認しました。**

---

### タスク 2: 単一デバイスでライブの Device Query を実行する

単一デバイスの Device Query は、1 台の Windows デバイスのライブ状態に対して KQL クエリを実行します。これは、サービス、レジストリ値、またはインストール済みアプリのバージョンを確認するためだけにリモート制御セッションを開くことに代わる典型的な手法です。

1. **Microsoft Intune 管理センター**で、**デバイス** → **Windows** の順に移動し、**SEA-DEV1** を選択します。

1. **監視**セクションで、**デバイス クエリ**を選択します。

1. クエリ エディターで、次のクエリを入力して実行し、SEA-DEV1 の CPU 情報を一覧表示します。

   ```kusto
   Cpu
   | project ProcessorId, Model, Architecture, CpuStatus, CoreCount, LogicalProcessorCount, Manufacturer
   ```

1. **実行**を選択します。結果は数秒以内に**結果**タブに表示されます。

   > [!NOTE]
   > 単一デバイスの Device Query には、管理者あたり **1 分間に 15 クエリ**のレート制限と、**2048 文字**のクエリ入力制限があります。結果セットは 128 KB に制限されます。

1. クエリを次のものに置き換えて、SEA-DEV1 のドライブの BitLocker 暗号化状態を確認します。

   ```kusto
   EncryptableVolume
   | project Device, DriveLetter, ProtectionStatus, ConversionStatus, EncryptionMethod
   | join LogicalDrive on Device
   ```

1. **実行**を選択します。SEA-DEV1 の OS ドライブに **PROTECTED** と表示されることを確認します。これにより、**ラボ 04 の演習 3** の BitLocker ポリシーが、(Intune ポータルで単に "割り当て済み" になっているだけでなく) 実際にドライブを暗号化していることが確認できます。

1. クエリを次のものに置き換えて、デバイスの OS バージョンを確認します。

   ```kusto
   OsVersion
   | project Device, OsVersion, OsBuildNumber, OsArchitecture
   ```

1. **実行**を選択します。SEA-DEV1 が、**ラボ 02 の演習 4** の機能更新プログラム プロファイルで固定した Windows 11 24H2 ビルドを実行していることを確認します。

**単一デバイスに対してライブの Device Query を正常に実行しました。**

---

### タスク 3: 複数デバイスの Device Query を実行し、結果からセキュリティ グループを構築する

複数デバイスの Device Query は、スコープ内のすべての Windows デバイスに対して 1 つの KQL クエリを実行し、デバイスごとに 1 行を返します。目玉機能は、**クエリの結果から直接 Microsoft Entra セキュリティ グループを作成できる**ことです。これにより、クエリで見つかったデバイスをまさに対象として、Intune ポリシーと条件付きアクセスを動的にターゲットにできます。

1. **Microsoft Intune 管理センター**で、**デバイス** → **デバイス クエリ**の順に移動します。

   > [!NOTE]
   > これは**複数デバイス**の Device Query の画面です (**デバイスの管理**グループの先頭にある [デバイス] → [デバイス クエリ] ではなく、**デバイス**の下のトップレベル項目です)。これは、タスク 2 で使用した単一デバイスの Device Query (デバイス → Windows → *デバイス* → 監視 → デバイス クエリ) とは異なります。

1. 左側の**はじめに**の下にある**サンプル クエリ**セクションを展開し、事前構築済みのサンプルを参照します。Microsoft がこの一覧を管理しています。これは、サポートされているテーブルと演算子を学ぶ最も早い方法です。

1. 次のクエリを入力して実行し、BitLocker で暗号化されて**いない**すべての Windows デバイスを見つけます。これは "これらのデバイスは今すぐ対応が必要" という典型的なクエリです。

   ```kusto
   EncryptableVolume
   | where ProtectionStatus != "PROTECTED"
   | join LogicalDrive on Device
   ```

1. **実行**を選択します。結果タブには、影響を受けるデバイスごとに 1 行が返されます。

1. 結果が画面に表示された状態で、結果タブの上部から**すべての項目をグループに追加**を選択します。ダイアログで、新しいグループに `sg-Devices-Unencrypted` という名前を付けます (説明: *Devices identified by Device Query as not BitLocker-encrypted*)。**グループの作成**を選択します。

   > [!NOTE]
   > **これが上級中級レベルの手法です。** 大まかな属性 (例: "deviceCategory eq 'Laptop'") に基づいて動的デバイス グループを構築する代わりに、実際のデバイス上の状態を照会し、その結果を実際にアドレス指定可能な Microsoft Entra セキュリティ グループに変換できます。これを使用して、修復スクリプト、より厳格なコンプライアンス ポリシー、または条件付きアクセスの "準拠するまでブロック" の適用を対象にできます。

1. 2 つ目のクエリを実行して、フリートの目標 (Windows 11 24H2 — ビルド番号 `26100`) より古い OS ビルドを実行しているデバイスを見つけます。

   ```kusto
   OsVersion
   | where OsBuildNumber < 26100
   | project Device, OsVersion, OsBuildNumber
   | order by OsBuildNumber asc
   ```

1. **実行**を選択します。これは "まだ機能更新プログラムを適用していない" 作業リストであり、**ラボ 02 の演習 4** で作成した機能更新プログラム プロファイルに遅れているデバイスを追跡するのに役立ちます。

1. 3 つ目のクエリを実行して、プロセッサ アーキテクチャ別にフリートを集計します (簡易的な "誰が ARM64 デバイスを持っているか" のインベントリ)。

   ```kusto
   Cpu
   | summarize DeviceCount = count() by Architecture
   ```

1. **実行**を選択します。結果タブには、アーキテクチャごとに 1 つの集計行が表示されます。

1. **エクスポート**を選択して、結果セットを CSV として保存します。これは、ハードウェア インベントリを調達部門へ渡したり、チケット システムへインポートしたりするのに役立ちます。

   > [!NOTE]
   > 複数デバイスの Device Query の結果は**スコープ タグ**を尊重します。Lee Gu (**ラボ 05 の演習 3** で割り当てられた **Pharmacy ヘルプデスク**の委任管理者) がこれらと同じクエリを実行すると、結果は彼女のスコープ内の Pharmacy のタグが付いたデバイスのみに自動的にフィルター処理されます。委任管理者は、テナントの残りの部分を一切見ることなく、自身のデバイスに関するサポートの質問に回答できます。

**複数デバイスの Device Query を正常に実行し、クエリ結果を Microsoft Entra セキュリティ グループに変換しました。**

---

## 演習 4: Windows 365 Cloud PC のプロビジョニングの確認

### シナリオ

Windows 365 は、ユーザーがブラウザーまたはリモート デスクトップ クライアントを介してアクセスする、クラウド ホスト型の Windows デスクトップ (Cloud PC) を提供します。ここでは、プロビジョニング プロセスを確認し、Cloud PC が Intune とどのように統合されるかを理解します。

> [!NOTE]
> Windows 365 のプロビジョニングには、Azure サブスクリプションと追加のライセンスが必要です。この演習は、プロビジョニング ワークフローの**ガイド付きデモンストレーション**です。

### タスク 1: Windows 365 プロビジョニング ポリシーを確認する (デモンストレーション)

1. **Microsoft Intune 管理センター**で、**デバイス** → **Windows** → **Windows 365** → **プロビジョニング ポリシー**の順に移動します。

   > [!NOTE]
   > Windows 365 がテナントで利用できない場合は、次の手順を概念的に確認してください。

1. **プロビジョニング ポリシーの作成**ワークフローを理解します。
   - **基本:**
     - ポリシー名
     - 参加の種類: Microsoft Entra 参加またはハイブリッド Microsoft Entra 参加
     - ネットワーク: Microsoft ホスト型ネットワークまたは Azure ネットワーク接続
   - **イメージ:**
     - ギャラリー イメージ (例: Windows 11 Enterprise + Microsoft 365 Apps)
     - カスタム イメージ (Azure Compute Gallery にアップロード)
   - **構成:**
     - ライセンスの種類: Enterprise、Business、または Frontline
     - リージョン: Cloud PC を展開する Azure リージョン
     - シングル サインオンを有効にする: はい/いいえ

1. **割り当て**ワークフローを理解します。
   - プロビジョニング ポリシーを Microsoft Entra グループに割り当てます
   - ポリシーが割り当てられると、グループ内のユーザーは自動的に Cloud PC を受け取ります
   - Cloud PC は Azure でプロビジョニングされます (通常 15 〜 30 分)

1. **ユーザー エクスペリエンス**を理解します。
   - ユーザーは **https://windows365.microsoft.com** にサインインします
   - Cloud PC がユーザーのダッシュボードに表示されます
   - ユーザーはブラウザーまたはリモート デスクトップ クライアントを介して Cloud PC を起動できます
   - Cloud PC は Intune によって管理されます (物理デバイスと同じポリシー)

**Windows 365 プロビジョニング ポリシーがクラウド ホスト型の Windows デスクトップをどのように展開するかを理解しました。**

---

### タスク 2: Intune での Cloud PC 管理を確認する (デモンストレーション)

1. **Microsoft Intune 管理センター**で、**デバイス** → **すべてのデバイス**の順に移動します。

1. Cloud PC が次の内容でデバイス一覧に表示されることを理解します。
   - **デバイス名:** CloudPC-<username>
   - **管理者:** Intune
   - **所有権:** 企業
   - **OS:** Windows 11 Enterprise

1. Cloud PC が物理デバイスと同じ Intune ポリシーを受け取ることを理解します。
   - 構成プロファイル
   - コンプライアンス ポリシー
   - アプリケーション
   - セキュリティ ベースライン

1. Cloud PC 固有の操作を理解します。
   - **再起動:** Cloud PC を再起動します
   - **サイズ変更:** Cloud PC の SKU (vCPU、RAM) を変更します
   - **復元:** バックアップ スナップショットから Cloud PC を復元します
   - **再プロビジョニング:** Cloud PC を消去し、イメージから再プロビジョニングします

**Cloud PC が物理デバイスと同様に Intune で管理される方法を理解しました。**

---

## 演習 5: Azure Virtual Desktop のセッション ホスト登録の確認

### シナリオ

Azure Virtual Desktop (AVD) は、仮想デスクトップ インフラストラクチャ (VDI) のシナリオ向けに、マルチセッションの Windows デスクトップを提供します。ここでは、AVD セッション ホストをポリシー管理のために Intune へ登録する方法を確認します。

> [!NOTE]
> AVD セッション ホストの登録には、Azure サブスクリプションと AVD の展開が必要です。この演習は**ガイド付きデモンストレーション**です。

### タスク 1: AVD セッション ホストの登録を理解する (デモンストレーション)

1. AVD アーキテクチャを理解します。
   - **ホスト プール:** 同一のセッション ホスト (VM) のコレクション
   - **セッション ホスト:** Windows 11 または Windows 10 のマルチセッション VM
   - **ワークスペース:** デスクトップとアプリにアクセスするためのユーザー向けインターフェイス

1. セッション ホストが Intune へ登録される方法を理解します。
   - セッション ホストは Microsoft Entra 参加 (またはハイブリッド参加) です
   - 自動 MDM 登録が有効になっています (ラボ 01 の物理デバイスと同じ)
   - セッション ホストは、初期プロビジョニング中に Intune へ登録されます

1. AVD 固有のポリシーに関する考慮事項を理解します。
   - **ユーザー ベースのポリシー:** ユーザー セッションに適用されます (例: OneDrive 同期、アプリ設定)
   - **デバイス ベースのポリシー:** セッション ホスト VM に適用されます (例: BitLocker、ファイアウォール、ウイルス対策)
   - **マルチセッションの最適化:** ポリシーは複数の同時ユーザーを考慮する必要があります (例: FSLogix プロファイル コンテナー)

**AVD セッション ホストが Intune へ登録される方法と、マルチセッション環境でポリシーが適用される方法を理解しました。**

---

### タスク 2: Intune での AVD セッション ホストを確認する (デモンストレーション)

1. **Microsoft Intune 管理センター**で、**デバイス** → **すべてのデバイス**の順に移動します。

1. AVD セッション ホストが次の内容でデバイス一覧に表示されることを理解します。
   - **デバイス名:** AVD-SessionHost-<number>
   - **OS:** Windows 11 Enterprise multi-session または Windows 10 Enterprise multi-session
   - **管理者:** Intune

1. AVD セッション ホストが Intune ポリシーを受け取ることを理解します。
   - 構成プロファイル (例: スタート メニューのレイアウト、Edge ポリシー)
   - コンプライアンス ポリシー (例: ウイルス対策、ファイアウォール)
   - アプリケーション (例: Microsoft 365 Apps、業務アプリ)

1. AVD 固有の考慮事項を理解します。
   - セッション ホストには BitLocker を適用しないでください (マネージド ディスクは Azure で保存時に暗号化されます)
   - ユーザー プロファイル管理には FSLogix を使用します (OneDrive の既知のフォルダーの移動ではなく)
   - Windows Update ポリシーは慎重に適用します (AVD のメンテナンス期間と調整します)

**AVD セッション ホストが Intune でどのように管理されるかを理解しました。**

---

## ラボのまとめ

おめでとうございます。MD-102 シリーズの最終ラボである Lab 06: 機能の拡張を完了しました。

このラボでは、次を達成しました。

**演習 1: Endpoint Privilege Management (EPM) の構成**
- テナント設定で Endpoint Privilege Management を有効化した
- 既定の昇格動作を定義する昇格設定ポリシーを作成した
- 自動、ユーザー確認、およびサポート承認の昇格ルールを作成した — すべて最初にパイロット コホートを対象とした
- 標準ユーザー デバイスで EPM の昇格シナリオをテストした
- 監査とコンプライアンスのために昇格レポートを監視した
- EPM 展開のためのパイロット → フリートの拡大計画を確立した

**演習 2: Remote Help の展開**
- Remote Help を有効化し、ヘルパーと共有者にライセンスを割り当てた
- Remote Help アプリを管理対象デバイスへ展開した
- Remote Help セッションを開始し、表示専用およびフル コントロールのアクセスをテストした
- 監査目的で Remote Help のセッション ログを確認した
- Pharmacy ヘルプデスクの委任管理者 (**ラボ 05 の演習 3** の Lee Gu) が、Pharmacy スコープのデバイスに対してのみ Remote Help を起動できることを実証した — これにより、6 つのラボすべてにわたって **スレッド A (カスタム RBAC + スコープ タグの委任)** をエンド ツー エンドで完了した

**演習 3: Advanced Analytics と Device Query の使用**
- 異常検出、リソース パフォーマンス、バッテリーの正常性に関する Advanced Analytics ダッシュボードを確認した
- Device Query を介してライブの単一デバイス KQL クエリを実行した (CPU 情報、BitLocker 状態の確認、OS バージョン)
- 複数デバイスの KQL クエリを実行して、暗号化されていないデバイスと 24H2 の機能更新プログラムの目標を下回るデバイスを見つけた
- Device Query の結果セットから直接 Microsoft Entra セキュリティ グループ (`sg-Devices-Unencrypted`) を構築した

**演習 4: Windows 365 Cloud PC のプロビジョニングの確認**
- Windows 365 プロビジョニング ポリシーのワークフローを確認した
- Cloud PC が自動的にプロビジョニングされ、Intune で管理される方法を理解した

**演習 5: Azure Virtual Desktop のセッション ホスト登録の確認**
- AVD アーキテクチャとセッション ホストの登録を理解した
- AVD セッション ホストが、ユーザー ベースおよびデバイス ベースのポリシーで Intune でどのように管理されるかを確認した

**要点:**
- Endpoint Privilege Management を使用すると、完全な管理者権限を付与することなく、アプリケーションの昇格をきめ細かく制御できます。パイロット優先の展開 (ESP、更新リング、ASR と同じコホート) により影響範囲を限定します
- Remote Help は、コンプライアンスのためのセッション ログ記録を伴う、セキュリティで保護され監査されたリモート支援を提供します
- カスタム ロールのスコープ タグは、Remote Help を含むリモート タスク操作に適用されます — Pharmacy ヘルプデスクは、Pharmacy スコープ外のデバイスに対して物理的に Remote Help を開始できません
- Advanced Analytics は、各デバイス自身の過去のベースライン (フリートのしきい値ではない) に対する ML 主導の異常検出を提供します。Device Query を使用すると、リモート制御なしで、1 台のデバイスまたはフリート全体に対してライブの KQL の質問を行えます
- **"クエリ結果から Microsoft Entra セキュリティ グループを構築する"** パターンは上級中級レベルの手法です。現実を照会し、見つかったものをまさに対象としてポリシーをターゲットにします
- Windows 365 Cloud PC は、Intune ポリシーを介してプロビジョニングされ、物理デバイスと同様に管理されます
- Azure Virtual Desktop のセッション ホストは Intune へ登録され、マルチセッション環境向けのポリシーを受け取ります

**コースの修了:**
MD-102: Microsoft 365 エンドポイント管理者認定コースの 6 つのラボすべてを完了しました。これで、次を実行する準備が整いました。
- クラウド専用環境で Microsoft Intune を展開および管理する
- Microsoft Entra 参加と Windows Autopilot を使用してデバイスを登録する
- デバイス ポリシー、コンプライアンス、Windows Update の管理を構成する
- 複数の方法 (Store、Win32、Microsoft 365 Apps、Enterprise App Catalog) を使用してアプリケーションを展開する
- Microsoft Defender for Endpoint、BitLocker、およびエンドポイント セキュリティ ポリシーを使用してエンドポイント セキュリティを実装する
- Microsoft Graph PowerShell で管理を自動化する
- 委任管理のためにスコープ タグを使用して RBAC を実装する
- Microsoft Intune Suite (EPM、Remote Help、Cloud PKI、Microsoft Tunnel) で機能を拡張する
- Windows 365 と Azure Virtual Desktop でクラウド ホスト型デスクトップをサポートする

**次のステップ:**
- MD-102 の試験目標を確認し、ラボの経験を測定される試験スキルにマッピングする
- 独自のテスト テナントで追加のシナリオを練習する
- 追加の概念的知識のために、MD-102 の Microsoft Learn モジュールを確認する
- 準備ができたら MD-102 認定試験を予約する

MD-102 のハンズオン ラボ シリーズを完了いただき、ありがとうございました。

---

**ラボ終了**
