### lab:
title: '演習ラボ 0501: 多要素認証の構成'
description: このラボでは、ユーザーごとの多要素認証 (MFA) を構成し、条件付きアクセス ポリシーを使用して MFA を適用します。
duration: 160 minutes
level: 200
islab: true

## 演習ラボ 0501: 多要素認証の構成

### 概要

このラボでは、ユーザーごとの多要素認証 (MFA) を構成し、条件付きアクセス ポリシーを使用して MFA を適用します。

#### 前提条件

Entra ID への Windows Hello サインイン認証を保護するためのテキスト メッセージを受信できる携帯電話が必要です。

### 演習 1: ユーザーごとの多要素認証の構成

#### シナリオ

ユーザーのサインイン イベントに追加のセキュリティを提供するため、多要素認証 (MFA) を構成してテストする必要があります。最初にユーザーごとの MFA をテストすることにしました。Alex Wilber が設定の検証に協力します。

#### タスク 1: MFA を有効にする前のサインインの検証

1. **SEA-WS3** に **Admin** としてパスワード **Pa55w.rd** を使用してサインインします。

2. タスク バーで **Microsoft Edge** を選択します。

3. アドレス バーに **outlook.office.com** と入力し、**Enter** キーを押します。

4. **サインイン** ページで **AlexW@yourtenant.onmicrosoft.com** と入力し、**次へ**を選択します。

5. **パスワードの入力**ページで、以前のラボで Joni Sherman に使用したものと同じユーザー パスワードを入力し、**サインイン**を選択します。Microsoft Edge にパスワードの保存を求められたら、**保存**を選択します。

6. **サインインの状態を維持しますか?** と表示されたら、**いいえ**を選択します。

   Outlook on the Web が開きます。Outlook on the Web へのサインインにはパスワードだけが必要だったことを確認します。

7. 右上隅で **Alex Wilber のアカウント マネージャー**を選択し、**サインアウト**を選択します。

8. Microsoft Edge を閉じます。

#### タスク 2: ユーザーに対する MFA の有効化

1. 手元 PC で Microsoft Edge を開き、[**https://entra.microsoft.com**](https://entra.microsoft.com) にアクセスします。

2. **Admin@yourtenant.onmicrosoft.com** とテナント管理者のパスワードを使用してサインインします。**サインインの状態を維持しますか?** と表示されたら、**いいえ**を選択します。

   Microsoft Entra 管理センターが開きます。

3. Web ページ上部のリソース検索ボックスに「多要素認証」と入力し、**多要素認証**を選択します。

   多要素認証ページが開きます。

4. **追加のクラウドベースの多要素認証設定**を選択します。

5. **ユーザーごとの多要素認証**ページで、**サービス設定**を選択します。

6. **サービス設定**ページを下へスクロールし、**信頼できるデバイスで多要素認証を記憶することをユーザーに許可する**チェック ボックスをオンにします。

7. **ユーザーがデバイスを信頼できる日数**に **30** と入力し、**保存**を選択します。

8. 保存後、**サービス設定**ページの先頭まで戻り、**ユーザー**を選択します。

9. ユーザー一覧で、**Alex Wilber** の横にあるチェック ボックスをオンにします。

10. ユーザー一覧の上にある **MFA を有効にする**を選択します。

11. **多要素認証を有効にする**メッセージで、**有効にする**を選択します。

12. 有効化されたらページを更新し、Alex Wilber の**状態**が**有効**になっていることを確認します。

#### タスク 3: MFA の登録と検証

1. **SEA-WS3** に切り替えます。

2. タスク バーで **Microsoft Edge** を選択します。

3. アドレス バーに **outlook.office.com** と入力し、**Enter** キーを押します。

4. **アカウントの選択**ページで、**AlexW@yourtenant.onmicrosoft.com** を選択します。

5. 必要な場合は、**パスワードの入力**ページでユーザー パスワードを入力し、**サインイン**を選択します。

6. **アカウントのセキュリティ保護**ページで、**次へ**を選択します。

7. **Microsoft Authenticator のインストール**ページで、**別の方法を設定します**を選択します。

   **注**: 正しいリンクを選択してください。

8. **サインイン方法の追加**ダイアログ ボックスで、**電話**を選択します。

9. **電話番号の追加**ページで**国コード**を選択し、**電話番号**フィールドにテキスト メッセージを受信できる携帯電話番号を入力して、**次へ**を選択します。

10. 確認コードを受信したら、**電話番号の確認**ページにコードを入力し、**次へ**を選択します。

11. **電話番号が追加されました**ページで、**完了**を選択します。

12. **サインインの状態を維持しますか?** と表示されたら、**いいえ**を選択します。

    Outlook on the Web で Alex Wilber の受信トレイが開きます。

13. **アカウントを保護する**ダイアログが表示された場合は、**今はスキップ (残り _x_ 回)**を選択します。

14. 右上隅で **Alex Wilber のアカウント マネージャー**を選択し、**サインアウト**を選択します。

15. Microsoft Edge を閉じます。

#### タスク 4: ユーザーごとの MFA の削除

1. 手元 PC で Microsoft Edge を開き、[**https://entra.microsoft.com**](https://entra.microsoft.com) にアクセスします。

2. **Admin@yourtenant.onmicrosoft.com** とテナント管理者のパスワードを使用してサインインします。**サインインの状態を維持しますか?** と表示されたら、**いいえ**を選択します。

   Microsoft Entra 管理センターが開きます。

3. ナビゲーション ペインで、**ユーザー**を選択します。

4. **すべてのユーザー**を選択し、結果ペイン上部の**ユーザーごとの MFA**を選択します。

   ユーザーごとの MFA ページが開きます。

5. ユーザー一覧で、**Alex Wilber** の横にあるチェック ボックスをオンにします。

6. ユーザー一覧の上にある **MFA を無効にする**を選択します。

7. **多要素認証を無効にする**メッセージで、**無効にする**を選択します。

8. 無効化されたらページを更新し、Alex Wilber の**状態**が**無効**になっていることを確認します。

9. Microsoft Edge を閉じます。

**結果**: この演習を完了すると、ユーザーごとの多要素認証が正常に構成されます。

### 演習 2: 条件付きアクセスを使用した多要素認証の構成

#### シナリオ

ユーザーのサインイン イベントに追加のセキュリティを提供するため、多要素認証 (MFA) を構成してテストする必要があります。MFA 要件には条件付きアクセス ポリシーを使用する方が柔軟性に優れていると判断しました。Alex Wilber が設定の検証に協力します。

#### タスク 1: MFA を使用する条件付きアクセスを有効にする前のサインインの検証

1. **SEA-WS3** に **Admin** としてパスワード **Pa55w.rd** を使用してサインインします。

2. タスク バーで **Microsoft Edge** を選択します。

3. アドレス バーに **outlook.office.com** と入力し、**Enter** キーを押します。

4. **アカウントの選択**ページで、**AlexW@yourtenant.onmicrosoft.com** を選択します。

5. **パスワードの入力**ページでテナントのパスワードを入力し、**サインイン**を選択します。

6. **サインインの状態を維持しますか?** ページで、**いいえ**を選択します。

   Outlook で Alex の受信トレイが開きます。前の演習で MFA を削除したため、Outlook on the Web へのサインインにはパスワードだけが必要だったことを確認します。

7. 右上隅で **Alex Wilber のアカウント マネージャー**を選択し、**サインアウト**を選択します。

8. Microsoft Edge を閉じます。

#### タスク 2: MFA を使用する条件付きアクセスの構成

1. 手元 PC で Microsoft Edge を開き、[**https://entra.microsoft.com**](https://entra.microsoft.com) にアクセスします。

2. **Admin@yourtenant.onmicrosoft.com** とテナント管理者のパスワードを使用してサインインします。**サインインの状態を維持しますか?** と表示されたら、**いいえ**を選択します。

   Microsoft Entra 管理センターが開きます。

3. ナビゲーション ペインの **Entra ID** で、**条件付きアクセス**を選択します。

4. **条件付きアクセス**ページで、**ポリシー**、**新しいポリシー**の順に選択します。

5. **新しい条件付きアクセス ポリシー**ページの**名前**ボックスに **Contoso MFA Policy** と入力します。

6. **割り当て**で、**0 人のユーザーまたはエージェント (プレビュー) が選択されました**を選択します。

7. **ユーザーとグループ**ペインで、**ユーザーとグループの選択**の横にあるオプションを選択し、**ユーザーとグループ**チェック ボックスをオンにします。

8. **ユーザーとグループの選択**ページで **Alex Wilber** を選択し、**選択**を選択します。

   通常はグループを指定しますが、この演習では Alex Wilber に対してのみ設定をテストします。

9. **対象リソースが選択されていません**を選択し、**リソースの選択**を選択します。

   クライアント アプリに基づくアクセス制御の設定を確認します。この設定では、リソースへのアクセスに使用するクライアント アプリを指定できます。たとえば、Exchange Online へのアクセスに Outlook アプリだけを使用できるように指定できます。

10. **特定のリソースの選択**で、**なし**を選択します。

11. 開いた**リソース**ページで **Office 365** の横にあるチェック ボックスをオンにし、**選択**を選択します。

12. **アクセス制御**の**許可**セクションで、**0 個のコントロールが選択されました**を選択します。

13. **許可**ページで、**アクセス権の付与**を選択し、**多要素認証を要求する**チェック ボックスをオンにして、**選択**を選択します。

14. **ポリシーの有効化**で、**オン**を選択します。

15. **作成**を選択して **Contoso MFA Policy** を作成します。ポリシーが一覧に表示され、**状態**が**オン**になっていることを確認します。

16. Microsoft Edge を閉じます。

#### タスク 3: 条件付きアクセス MFA の検証

1. **SEA-WS3** に切り替えます。

2. タスク バーで **Microsoft Edge** を選択します。

3. アドレス バーに [**https://outlook.office.com**](https://outlook.office.com) と入力し、**Enter** キーを押します。

4. **アカウントの選択**ページで、**AlexW@yourtenant.onmicrosoft.com** を選択します。

5. 必要な場合は、**パスワードの入力**ページでユーザー パスワードを入力し、**サインイン**を選択します。

6. **本人確認**プロンプトで、自分の電話番号を選択します。

   **コードの入力**ダイアログ ボックスが開きます。

7. **コードの入力**ページで、携帯電話に送信されたコードを入力し、**確認**を選択します。

8. **アカウントを保護する**ダイアログが表示された場合は、**今はスキップ (残り _x_ 回)**を選択します。

9. **サインインの状態を維持しますか?** と表示されたら、**いいえ**を選択します。

   Outlook on the Web で Alex Wilber の受信トレイが開きます。

10. 右上隅で **Alex Wilber のアカウント マネージャー**を選択し、**サインアウト**を選択します。

11. Microsoft Edge を閉じます。

#### タスク 4: 条件付きアクセス MFA の削除

1. 手元 PC で Microsoft Edge を開き、[**https://entra.microsoft.com**](https://entra.microsoft.com) にアクセスします。

2. **Admin@yourtenant.onmicrosoft.com** とテナント管理者のパスワードを使用してサインインします。**サインインの状態を維持しますか?** と表示されたら、**いいえ**を選択します。

   Microsoft Entra 管理センターが開きます。

3. ナビゲーション ペインの **Entra ID** で、**条件付きアクセス**を選択します。

4. **条件付きアクセス**ページで、**ポリシー**、**Contoso MFA Policy** の順に選択します。

5. **Contoso MFA Policy** ページで、**削除**を選択します。

6. **よろしいですか?** と表示されたら、**はい**を選択します。

7. Microsoft Edge を閉じます。

**結果**: この演習を完了すると、条件付きアクセス ポリシーを使用した多要素認証が正常に構成されます。

**ラボ終了**