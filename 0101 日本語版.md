---
lab:
  title: '演習ラボ 0101: Entra ID での ID の管理'
  description: このラボでは、Entra 管理センターを使用してユーザーの作成と変更、管理ロールの割り当て、グループの作成と変更、および Entra ID のライセンス割り当てを管理します。
  duration: 60 minutes
  level: 200
  islab: true
---

# 演習ラボ 0101: Entra ID での ID の管理

## WWL テナント - 利用規約

講師主導型トレーニングの一環としてテナントが提供される場合、そのテナントは講師主導型トレーニングのハンズオン ラボを支援する目的で提供されます。

テナントを共有したり、ハンズオン ラボ以外の目的で使用したりしないでください。このコースで使用するテナントは試用版であり、クラス終了後は使用またはアクセスできず、利用期間を延長することもできません。

テナントを有料サブスクリプションへ変換しないでください。このコースの一環として取得したテナントは Microsoft Corporation の所有物であり、Microsoft はいつでもアクセスして回収する権利を保有します。

## 概要

このラボでは、Entra 管理センターを使用してユーザーの作成と変更、管理ロールの割り当て、グループの作成と変更、および Entra ID のライセンス割り当てを管理します。

## 演習 1: Entra ID でのユーザーの作成

### シナリオ

来週入社する新しい従業員のユーザー アカウントを Azure AD に作成します。新しいユーザーは次の表のとおりです。

| 名前           | ユーザー名                             | パスワード | 役職             | 部署       |
| -------------- | ------------------------------------- | ---------- | ---------------- | ---------- |
| Edmund Reeve   | `ereeve@yourtenant.onmicrosoft.com`   | Pa55-w.rd! | HR Rep           | HR         |
| Miranda Snider | `msnider@yourtenant.onmicrosoft.com`  | Pa55-w.rd! | Helpdesk Manager | Operations |
| Cody Godinez   | `cgodinez@yourtenant.onmicrosoft.com` | Pa55-w.rd! | Sales Rep        | Sales      |

_注: For location use either your local region or United States._

今後数か月の間に、さらに複数の従業員を採用する予定です。多数の新規ユーザーを追加するにはスクリプトの方が効率的であるため、PowerShell スクリプトを作成し、Cody Godinez のアカウントを作成するときにテストします。

### タスク 1: Microsoft Entra 管理センターを使用してユーザーを作成する

1. **SEA-SVR1** で、パスワード **Pa55w.rd** を使用して **Contoso\\Administrator** としてサインインします。

2. **Server Manager** を閉じます。

3. タスク バーで **Microsoft Edge**。

4. 次の画面で address bar, **https://entra.microsoft.com**。

2. サインイン プロンプトで **admin@yourtenant.onmicrosoft.com** と入力し、**次へ**を選択します。

3. **パスワードの入力**ページで管理者アカウントのパスワードを入力し、**サインイン**を選択します。

   > 注: 管理者アカウントでのサインインに使用するパスワードは、講師に確認してください。

4. Edge のパスワード保存プロンプトで、**保存して有効にする**を選択します。

2. サインイン状態の維持を確認するプロンプトで、**はい**を選択します。

3. **Let's keep your account secure**ダイアログで **次へ**を選択します。

4. **Install Microsoft Authenticator**ダイアログで **次へ**を選択します。

2. **set up your account in app**ページで **次へ**を選択します。

3. モバイル デバイスの Authenticator アプリで QR コードをスキャンし、**次へ**を選択します。

4. 確認ダイアログに表示されたコードを確認し、Authenticator アプリへ入力します。

2. **Authenticator Added**ダイアログで **Done**を選択します。

3. Microsoft Entra 管理センターのナビゲーション ペインで **ユーザー**。

    > Microsoft Entra ID のメンバーとして既に存在するユーザーを確認します。現在のすべてのユーザーについて、**オンプレミスの同期が有効**列に **いいえ**と表示されています。これは、各ユーザーが Microsoft Entra ID で直接作成され、オンプレミスのディレクトリ サービスから同期されていないことを示します。

4. **ユーザー | すべてのユーザー**ページで **新しいユーザー** 続いて **新しいユーザーの作成**を選択します。

2. **新しいユーザーの作成**ページで、次の値を入力します。

    - User Principal Name: **ereeve**
    - Display Name: **Edmund Reeve**

3. Uncheck **パスワードの自動生成**

4. **パスワード**に **Pa55-w.rd!** と入力します。

    > 注: パスワードが弱い、または一般的に使用されているというエラーが表示された場合は、このラボ プロファイルの **Resources** タブにある受講者用パスワードを入力します。または、任意の複雑なパスワードを入力します。

2. ページ下部の **次へ: プロパティ**を選択します。

3. **名**に **Edmund** と入力します。

4. **姓**に **Reeve** と入力します。

2. Next to **ユーザーの種類**, make note that **メンバー** is selected。

    > 注: The **メンバー** user type is the default user type. This user type is used for most users in an organization.

3. **役職**に **HR Rep** と入力します。

4. **部署**に **HR** と入力します。

2. **利用場所**に **United States** と入力します。

3. ページ下部の **次へ: 割り当て**を選択します。

4. **割り当て**ページで、割り当てが選択されていないことを確認します。

    > 既定ではユーザーにグループは割り当てられません。グループを割り当てるまで、ユーザーはどのグループのメンバーにもなりません。

2. ページ下部の **次へ: 確認と作成**を選択します。
    > このページの情報が正しいことを確認します。

3. **作成** を選択します。

4. **ユーザー | すべてのユーザー**ページで **新しいユーザー** 続いて **新しいユーザーの作成**を選択します。

2. **新しいユーザーの作成**ページで、次の値を入力します。

    - User Principal Name: **msnider**
    - Display Name: **Miranda Snider**

3. Uncheck **パスワードの自動生成**

4. **パスワード**に **Pa55-w.rd!** と入力します。

    > 注: パスワードが弱い、または一般的に使用されているというエラーが表示された場合は、このラボ プロファイルの **Resources** タブにある受講者用パスワードを入力します。または、任意の複雑なパスワードを入力します。

2. ページ下部の **次へ: プロパティ**を選択します。

3. **名**に **Miranda** と入力します。

4. **姓**に **Snider** と入力します。

2. Next to **ユーザーの種類**, make note that **メンバー** is selected。

3. **役職**に **Helpdesk Manager** と入力します。

4. **部署**に **Operations** と入力します。

2. **利用場所**に **United States** と入力します。

3. ページ下部の **次へ: 割り当て**を選択します。

4. **割り当て**ページで、割り当てが選択されていないことを確認します。

2. ページ下部の **次へ: 確認と作成**を選択します。

3. **作成** を選択します。

### タスク 2: PowerShell を使用してユーザーを作成する

# SEA-SVR1 に PowerShell 7.5.4 をインストールする手順

1. On **SEA-SVR1**, in **Microsoft Edge**, open a new タブ。

2. 次の画面で address bar, **https://github.com/PowerShell/PowerShell/releases/download/v7.5.4/PowerShell-7.5.4-win-x64.msi**  

3. タスク バーで **File Explorer**, then navigate to your **Downloads** folder。

4. Double-click **PowerShell-7.5.4-win-x64.msi** to launch the setup wizard。

   - Select **Next**  
   - Leave the **Destination Folder** as is, 続いて **Next**  
   - Leave the **Optional Actions** as is, 続いて **Next**  
   - Leave the checkboxes blank under *Use Microsoft Update to help keep your computer secure and up to date*, 続いて **Next**  
   - Select **Install**  

5. 次の画面で **Installation completed successfully** ウィンドウ, check **Launch PowerShell**, 続いて **Finish**。

   > **注:** If the installer closed without launching PowerShell, click the **Windows Search** bar, **pwsh**, right-click **PowerShell 7**, and **Run as administrator**.  

6. 次の画面で **PowerShell 7** ウィンドウ, type the following command, and then press **Enter**. If prompted, **Y** at the NuGet and repository messages:

    ```powershell
    Install-Module Microsoft.Graph -Scope CurrentUser
    ```

7. PowerShell ウィンドウで次のコマンドを入力し、**Enter** キーを押します。

    ```powershell
    Connect-MgGraph -scopes "user.readwrite.all, group.readwrite.all"
    ```

8. When prompted in the **Let's get you signed in** ウィンドウ, **Work or School account** を選択し、続いて **Continue**。

9. 次の画面で **Sign in** ダイアログ, sign in as **`admin@yourtenant.onmicrosoft.com`** with the administrative password, を選択し、続いて **Sign in**。

10. 次の画面で **Permissions Requested** prompt that appears, check **Consent on behalf of your organization** を選択し、続いて **Accept**。

11. 次の画面で **Sign in to all apps, websites, and services on this device?** select, **No, this app only**。

12. 次の画面で **PowerShell 7** ウィンドウ, type the following code to create a new profile object, and then press **enter**. Replace **Pa55w.rd** with a complex password of your choice:

    ```powershell
    $PWProfile = @{
        Password = "Pa55w.rd";
        ForceChangePasswordNextSignIn = $false
    }
    ```
    
14. Next, type the following code to create a new user, and then press **Enter**. Ensure "yourtenant" matches your assigned tenant name:

    ```powershell
    New-MgUser `
        -DisplayName "Cody Godinez" `
        -GivenName "Cody" -Surname "Godinez" `
        -MailNickname "cgodinez" `
        -UsageLocation "US" `
        -UserPrincipalName "cgodinez@yourtenant.onmicrosoft.com" `
        -PasswordProfile $PWProfile -AccountEnabled `
        -Department "Sales" -JobTitle "Sales Rep"
    ```

15. To confirm that the user **Cody Godinez** was created, 次の画面で **PowerShell 7** ウィンドウ, type the following command and then press **Enter**:

    ```powershell
    Get-MgUser
    ```

> テナント内のユーザー一覧が表示されることを確認します。 

**結果**: この演習を完了すると、Entra ID に新しいユーザー アカウントが作成されます。

## 演習 2: Entra ID での管理ロールの割り当て

### シナリオ

テナントの現在の管理ロールを確認し、変更します。

次の表に示す管理ロールをユーザーへ割り当てます。  

| Name           | Must be able to:                         | Administrative Role needed: |
| -------------- | ---------------------------------------- | --------------------------- |
| Allan Deyoung  | Manage the tenant                        | Global administrator        |
| Edmund Reeve   | Manage users, group, and password resets | User administrator          |
| Miranda Snider | Manage password resets                   | Helpdesk administrator      |

### タスク 1: 管理ロールを確認して割り当てる

1. On SEA-SVR1, switch to Microsoft Edge。

2. 次の画面で Microsoft Entra admin center, in the Navigation ペイン, **ロールと管理者**。

    > 次を確認します: you can scroll down the 一覧 or use the search ボックス to find the **ロール** you are looking for.

3. 検索ボックスで検索します: **グローバル管理者**。

4. Select **グローバル管理者** (select the name, not the チェック ボックス)。

5. 次の画面で **グローバル管理者** ペイン, **+ Add assignments**。

6. 次の項目で **メンバーの選択**, **メンバーが選択されていません**, then search for and **Allan Deyoung**。

7. **選択**, then select **次へ**, and finally select **割り当て** を選択します。

8. 次の画面で navigation breadcrumbs, **ロールと管理者 | すべてのロール**。

9. 検索ボックスで検索します: **ユーザー管理者**。

10. Select **ユーザー管理者** (select the name, not the チェック ボックス)。

11. 次の画面で **ユーザー管理者** ペイン, **+ Add assignments**。

12. 次の項目で **メンバーの選択**, **メンバーが選択されていません**, then search for and **Edmund Reeve**。

13. **選択**, then select **次へ**, and finally select **割り当て** を選択します。

14. 次の画面で navigation breadcrumbs, **ロールと管理者 | すべてのロール**。

15. 検索ボックスで検索します: **ヘルプデスク管理者**。

16. Select **ヘルプデスク管理者** (select the name, not the チェック ボックス)。

17. 次の画面で **ヘルプデスク管理者** ペイン, **+ Add assignments**

18. 次の項目で **メンバーの選択**, **メンバーが選択されていません**, then search for and **Miranda Snider**。

19. **選択**, then select **次へ**, and finally select **割り当て** を選択します。

20. ナビゲーション ペインで **ホーム**。

**結果**: この演習を完了すると、管理ロールがユーザーへ割り当てられます。

## 演習 3: グループの作成と管理、およびライセンス割り当ての検証

### シナリオ

3人の新しいユーザーをセキュリティ グループに追加し、次の表に従ってライセンスを割り当てます。  

| Name           | Member of:       | License to assign                                            |
| -------------- | ---------------- | ------------------------------------------------------------ |
| Edmund Reeve   | Contoso_Managers | Office 365 E5, Enterprise Mobility + Security E5 via group membership |
| Miranda Snider | Contoso_Managers | Office 365 E5, Enterprise Mobility + Security E5 via group membership |
| Cody Godinez   | Contoso_Sales    | Office 365 E5, Enterprise Mobility + Security E5 via group membership direct assignment |

さらに、サインイン ページの会社ブランドを変更します。

### タスク 1: Microsoft Entra 管理センターを使用してグループを作成する

1. On **SEA-SVR1**, in the Microsoft Entra admin center, in the navigation ペイン, **グループ**。

2. **新しいグループ** を選択します。

3. **新しいグループ**ページで、次の値を入力します。

    - Group type: **セキュリティ**
    - Group name: **Contoso_Managers**
    - Membership type: **割り当て済み**

4. 次の項目で Members, **メンバーが選択されていません**。

5. 次の画面で Add members ページ add **Edmund Reeve**, **Miranda Snider**, and then click **選択**。

6. **作成** を選択します。

### タスク 2: PowerShell を使用してグループを作成する

1. On SEA-SVR1, switch to PowerShell 7。

2. PowerShell ウィンドウで次のコマンドを入力し、**Enter** キーを押します。

    ```powershell
    New-MgGroup -DisplayName "Contoso_Sales" -Description "Contoso Sales team users" -MailEnabled:$false -Mailnickname "Contoso_Sales" -SecurityEnabled
    ```

3. PowerShell ウィンドウで次のコマンドを入力し、**Enter** キーを押します。

    ```powershell
    Get-MgGroup
    ```

4. 次を確認します: you get the 一覧 of groups in your tenant, including the Contoso_Sales group you just created。

5. PowerShell ウィンドウで次のコマンドを入力し、**Enter** キーを押します。

    ```powershell
    $group = Get-MgGroup | Where-Object {$_.DisplayName -eq "Contoso_Sales"}
    ```

6. PowerShell ウィンドウで次のコマンドを入力し、**Enter** キーを押します。

    ```powershell
    $user = Get-MgUser | Where-Object {$_.DisplayName -eq "Cody Godinez"}
    ```

7. PowerShell ウィンドウで次のコマンドを入力し、**Enter** キーを押します。

    ```powershell
    New-MgGroupMember -GroupId $group.Id -DirectoryObjectId $user.Id
    ```

8. PowerShell ウィンドウで次のコマンドを入力し、**Enter** キーを押します。

    ```powershell
    Get-MgGroupMember -GroupId $group.Id | FL
    ```

9. 次を確認します: you see **Cody Godinez** as value in **AdditionalProperties**。

10. PowerShell 7 を閉じます。

### タスク 3: ライセンスを確認して会社のブランドを変更する

1. Microsoft Entra 管理センターのナビゲーション ペインで **課金** > **ライセンス**。

2. 次の画面で **ライセンス** ページ, in the center navigation ペイン, under **管理**, **すべての製品**。

   > 次を確認します: the current licenses available and assigned for **Enterprise Mobility + Security E5** and **Office 365 E5**.

3. 次の画面で Microsoft Entra admin center, in the Navigation ペイン,under **Entra ID**, **会社のブランド**。

4. 次の画面で **会社のブランド** ページ, under **既定のサインイン エクスペリエンス**, **カスタマイズ**。

5. 次の画面で **既定のサインイン エクスペリエンスのカスタマイズ** ページ, navigate to the **サインイン フォーム** タブ and configure the following settings:

   - Sign-in ページ text: **Contoso Corp. Sign-in ページ**

6. **確認と作成**, review the settings and then select **作成** を選択します。

7. 次の画面で Microsoft Entra admin center, in the Navigation ペイン, **ユーザー**。

8. 次の画面で user 一覧, **Cody Godinez** (select the name, not the チェック ボックス)。

9. 次の画面で Cody Godinez Profile ページ, in the center navigation menu, **ライセンス**。

   > 次を確認します: Cody does not have any current license assignments. And that licensing must now be performed in the 365 Admin center.

10. Open a new タブ in **Microsoft Edge**, in the address bar, **https://admin.microsoft.com**。

11. 次の画面で navigation ペイン on the left, **ユーザー** > **アクティブなユーザー**。

12. 次の画面で user 一覧, **Cody Godinez** (select the name, not the チェック ボックス)。

13. Select the **ライセンスとアプリ** タブ。

14. Select the check boxes next to **Enterprise Mobility + Security E5** and **Office 365 E5 (no Teams)**。

15. **変更の保存** を選択します。

16. Once the changes have been saved, select the **X** in the upper-right corner to close the **Cody Godinez** ペイン。

17. 次の画面で Microsoft 365 admin center, in the Navigation ペイン, **課金** > **ライセンス**。

18. 次の画面で **サブスクリプション** 一覧, **Enterprise Mobility + Security E5**。

19. Select the **グループ** タブ, を選択し、続いて **+ Assign licenses**。

20. Navigate into the **グループ名を入力** textbox, and select the **Contoso_Managers** group。

21. **ライセンスの割り当て** を選択します。

22. 次の画面で **Contoso_Managers にライセンスを割り当てました** ペイン, select the **X** in the upper-right corner to close it。

23. 次の画面で upper-left corner of the **Enterprise Mobility + Security E5** ページ, select the **ライセンスに戻る** リンク。

24. 次の画面で **サブスクリプション** 一覧, **Office 365 E5 (no Teams)**。

25. Select the **グループ** タブ, を選択し、続いて **+ Assign licenses**。

26. Navigate into the **グループ名を入力** textbox, and select the **Contoso_Managers** group。

27. **ライセンスの割り当て** を選択します。

28. 次の画面で **Contoso_Managers にライセンスを割り当てました** ペイン, select the **X** in the upper-right corner to close it。

29. 次の画面で Microsoft 365 admin center, in the Navigation ペイン, **課金** > **ライセンス**。

30. 次の画面で **サブスクリプション** 一覧, **Office 365 E5 (no Teams)**。

   > 次を確認します: the users that are assigned the Office 365 E5 license. Edmund and Miranda both receive their license assignment from their membership in the Contoso_Managers group. You can select the **グループ** タブ see if the licenses assigned correctly. It may take 3-5 minutes for the licenses to reprocess.

31. Microsoft Edge を閉じます。

**結果**: この演習を完了すると、 created and managed groups, modified company branding, and assigned licenses.

**ラボ終了**
